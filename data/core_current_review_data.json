{
  "schema_version": 1,
  "dataset": "core-current-nonarch-full-context",
  "toolchain": "nightly-2026-08-27",
  "rustc_version": "rustc 1.100.0-nightly (bff8e12ff 2026-08-26)",
  "model": "gpt-5.6-sol",
  "judge_model": "gpt-5.6-sol",
  "context_preset": "full",
  "diff_method": "independent LLM classification: Correct, strict-subset Missing, or semantic Incorrect",
  "records": [
    {
      "target_api_path": "core::alloc::Allocator",
      "source_api_path": "core::alloc::Allocator",
      "llm_generated_comment": "# Safety\n\nImplementors of `Allocator` must ensure that:\n\n- Every successful call to `allocate` returns a non-null block that is currently allocated by this allocator, is valid and suitably aligned for `layout`, and is at least `layout.size()` bytes long. The block may be uninitialized.\n- Every successful call to `allocate_zeroed` returns a block meeting the same requirements as `allocate`, with all bytes in the returned block initialized to zero.\n- Distinct blocks that are currently allocated do not overlap, and each block remains valid until it is invalidated by a successful `deallocate`, `grow`, `grow_zeroed`, or `shrink` operation.\n- A call to `deallocate` is made only with a pointer denoting a block currently allocated by this allocator and a `layout` that fits that block. The block must not have outstanding references, smart pointers, or other accesses that would be invalidated by deallocation, and deallocation must not invalidate the `&self` reference or the allocator object itself.\n- A call to `grow` or `grow_zeroed` is made only with a pointer denoting a block currently allocated by this allocator, an `old_layout` that fits that block, and a `new_layout` whose size is at least `old_layout.size()`.\n- A call to `shrink` is made only with a pointer denoting a block currently allocated by this allocator, an `old_layout` that fits that block, and a `new_layout` whose size is at most `old_layout.size()`.\n- After a successful `grow`, `grow_zeroed`, or `shrink`, the old pointer is no longer used to access the allocation; only the returned pointer is used. On failure, the original allocation remains valid and may continue to be used according to its original layout.",
      "diff": "Incorrect\n1. It adds caller preconditions for deallocation and reallocation that are not stated as implementor obligations in the ORIGINAL.\n2. It requires allocations to have no outstanding references or accesses before deallocation, an unsupported condition absent from the ORIGINAL.\n3. It narrows nonoverlap to distinct currently allocated blocks, while the ORIGINAL requires all returned blocks to be wholly disjoint.\n4. It omits the required address-bound invariant `p as usize + n <= usize::MAX`.\n5. It omits the prohibition on reading, writing, or deallocating memory that is currently allocated.\n6. It omits the prohibition on unwinding from allocating, deallocating, or dropping the allocator.\n7. It omits the requirement that allocator equivalences under the implementor's control preserve the currently-allocated memory invariant.\n8. It states that successful listed operations invalidate the old block, whereas the ORIGINAL only requires validity until invalidation and does not define those operations as universally invalidating.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::Allocator",
        "classification": "Incorrect",
        "items": [
          "It adds caller preconditions for deallocation and reallocation that are not stated as implementor obligations in the ORIGINAL.",
          "It requires allocations to have no outstanding references or accesses before deallocation, an unsupported condition absent from the ORIGINAL.",
          "It narrows nonoverlap to distinct currently allocated blocks, while the ORIGINAL requires all returned blocks to be wholly disjoint.",
          "It omits the required address-bound invariant `p as usize + n <= usize::MAX`.",
          "It omits the prohibition on reading, writing, or deallocating memory that is currently allocated.",
          "It omits the prohibition on unwinding from allocating, deallocating, or dropping the allocator.",
          "It omits the requirement that allocator equivalences under the implementor's control preserve the currently-allocated memory invariant.",
          "It states that successful listed operations invalidate the old block, whereas the ORIGINAL only requires validity until invalidation and does not define those operations as universally invalidating."
        ]
      },
      "original_safety_doc": "Implementors of `Allocator` must ensure that a memory block that\nis [*currently allocated*] by the allocator points to valid memory\nuntil that memory block is [*invalidated*]. The implementor must also\nnot violate this invariant of `Allocator` via allocator equivalences\nthat are in the implementor's control.\n\nAdditionally, any memory block returned by the allocator must\nsatisfy the allocation invariants described in `core::ptr`.\nIn particular, if a block has base address `p` and size `n`,\nthen `p as usize + n <= usize::MAX` must hold. These blocks must also\nbe wholly disjoint.\n\nThis ensures that pointer arithmetic within the allocation\n(for example, `ptr.add(len)`) cannot overflow the address space, and\nthat it is possible to perform nonoverlapping copies between allocations.\n\nNone of the allocating or deallocating methods may unwind. This restriction\nmay be lifted in the future by ensuring unwinding out of an allocating function always\naborts. If an implementor of `Allocator` also has drop glue or directly implements `Drop`,\ndropping the allocator must not result in an unwind.\n\nIt is undefined behavior for the allocator to read, write, or deallocate any memory that\nis currently allocated. This memory is owned by the user; the allocator must not touch it.\n\nLastly, the methods on this trait must be *correct*; in particular, the layout requested\nmust be respected, calls must zero out memory if the documentation so requires,\nreturning an `AllocError` from a reallocating method must indeed ensure that\nthe old pointer was not invalidated, and de/reallocating calls must accept layouts\nin the ranges defined by their documentation.\n\n[*currently allocated*]: #currently-allocated-memory\n[*invalidated*]: #invalidating-memory-blocks"
    },
    {
      "target_api_path": "core::alloc::Allocator::deallocate",
      "source_api_path": "core::alloc::Allocator::deallocate",
      "llm_generated_comment": "# Safety\n\n- `ptr` must denote a block of memory currently allocated by this allocator and not previously deallocated.\n- `layout` must be the same layout that was used to allocate the block denoted by `ptr`.",
      "diff": "Incorrect\n1. Generated requires `layout` to be identical to the allocation layout, while ORIGINAL only requires it to fit the allocated block.\n2. Generated omits the obligation that deallocation must not invalidate outstanding references, including the `&self` reference to an allocator stored in the deallocated memory.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::Allocator::deallocate",
        "classification": "Incorrect",
        "items": [
          "Generated requires `layout` to be identical to the allocation layout, while ORIGINAL only requires it to fit the allocated block.",
          "Generated omits the obligation that deallocation must not invalidate outstanding references, including the `&self` reference to an allocator stored in the deallocated memory."
        ]
      },
      "original_safety_doc": "* `ptr` must denote a block of memory [*currently allocated*] via this allocator, and\n* `layout` must [*fit*] that block of memory.\n\nNote that it is *immediate* language UB for a deallocation or reallocation to\ninvalidate any outstanding references, smart pointers, etc.; thus, notably, an\nallocator that has been moved into its own [*currently allocated*] memory may\nnot have its backing memory be freed, even if the allocator is never used again\nafterwards. This is due to the fact that such a deallocation would invalidate the\n`&self` reference passed to this method.\n\n[*currently allocated*]: #currently-allocated-memory\n[*fit*]: #memory-fitting"
    },
    {
      "target_api_path": "core::alloc::Allocator::grow",
      "source_api_path": "core::alloc::Allocator::grow",
      "llm_generated_comment": "# Safety\n\n- `ptr` must denote a currently allocated block obtained from this allocator using `old_layout`; it must not have been deallocated or otherwise invalidated, and the block must remain valid for reads of `old_layout.size()` bytes for the duration of this call.\n- `new_layout.size()` must be greater than or equal to `old_layout.size()`.",
      "diff": "Incorrect\n1. Generated requires the block to have been obtained using `old_layout`, while ORIGINAL only requires allocation via this allocator and that `old_layout` fit the block.\n2. Generated additionally requires read validity for the call duration, a condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::Allocator::grow",
        "classification": "Incorrect",
        "items": [
          "Generated requires the block to have been obtained using `old_layout`, while ORIGINAL only requires allocation via this allocator and that `old_layout` fit the block.",
          "Generated additionally requires read validity for the call duration, a condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "* `ptr` must denote a block of memory [*currently allocated*] via this allocator.\n* `old_layout` must [*fit*] that block of memory (The `new_layout` argument need not fit it.).\n* `new_layout.size()` must be greater than or equal to `old_layout.size()`.\n\nNote that `new_layout.align()` need not be the same as `old_layout.align()`.\n\n[*currently allocated*]: #currently-allocated-memory\n[*fit*]: #memory-fitting\n[*invalidated*]: #invalidating-memory-blocks"
    },
    {
      "target_api_path": "core::alloc::Allocator::grow_zeroed",
      "source_api_path": "core::alloc::Allocator::grow_zeroed",
      "llm_generated_comment": "# Safety\n\n- `ptr` must refer to a currently allocated block owned by this allocator, and `old_layout` must be the layout with which that block was allocated. The block must remain allocated and valid for reads of `old_layout.size()` bytes until this function returns.\n- `new_layout.size()` must be greater than or equal to `old_layout.size()`.",
      "diff": "Incorrect\n1. Generated requires `old_layout` to exactly match the allocation layout, while ORIGINAL only requires it to fit the block; it also adds an unsupported read-validity and lifetime obligation.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::Allocator::grow_zeroed",
        "classification": "Incorrect",
        "items": [
          "Generated requires `old_layout` to exactly match the allocation layout, while ORIGINAL only requires it to fit the block; it also adds an unsupported read-validity and lifetime obligation."
        ]
      },
      "original_safety_doc": "* `ptr` must denote a block of memory [*currently allocated*] via this allocator.\n* `old_layout` must [*fit*] that block of memory (The `new_layout` argument need not fit it.).\n* `new_layout.size()` must be greater than or equal to `old_layout.size()`.\n\nNote that `new_layout.align()` need not be the same as `old_layout.align()`.\n\n[*currently allocated*]: #currently-allocated-memory\n[*fit*]: #memory-fitting"
    },
    {
      "target_api_path": "core::alloc::Allocator::shrink",
      "source_api_path": "core::alloc::Allocator::shrink",
      "llm_generated_comment": "# Safety\n\n- `ptr` must refer to a currently allocated block of memory obtained from `self` using `old_layout`, and that block must remain valid and accessible for reads of `new_layout.size()` bytes until this call returns.\n- `new_layout.size()` must be less than or equal to `old_layout.size()`.\n- The block referred to by `ptr` must not be accessed, or have any pointers or references used to access it, after this call successfully returns, because it is deallocated by this function.",
      "diff": "Incorrect\n1. Generated requires the block to have been obtained using `old_layout`, while ORIGINAL only requires `old_layout` to fit the currently allocated block.\n2. Generated claims successful return deallocates the block and forbids subsequent access, but ORIGINAL does not impose this and `shrink` may leave an allocated block.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::Allocator::shrink",
        "classification": "Incorrect",
        "items": [
          "Generated requires the block to have been obtained using `old_layout`, while ORIGINAL only requires `old_layout` to fit the currently allocated block.",
          "Generated claims successful return deallocates the block and forbids subsequent access, but ORIGINAL does not impose this and `shrink` may leave an allocated block."
        ]
      },
      "original_safety_doc": "* `ptr` must denote a block of memory [*currently allocated*] via this allocator.\n* `old_layout` must [*fit*] that block of memory (The `new_layout` argument need not fit it.).\n* `new_layout.size()` must be smaller than or equal to `old_layout.size()`.\n\nNote that `new_layout.align()` need not be the same as `old_layout.align()`.\n\n[*currently allocated*]: #currently-allocated-memory\n[*fit*]: #memory-fitting\n[*invalidated*]: #invalidating-memory-blocks"
    },
    {
      "target_api_path": "core::alloc::GlobalAlloc",
      "source_api_path": "core::alloc::GlobalAlloc",
      "llm_generated_comment": "# Safety\n\n- When calling [`alloc`](Self::alloc) or [`alloc_zeroed`](Self::alloc_zeroed), ensure that `layout` has a non-zero size.\n- When calling [`dealloc`](Self::dealloc), ensure that `ptr` refers to a block currently allocated through this allocator and that `layout` is exactly the layout used to allocate that block.\n- When calling [`realloc`](Self::realloc), ensure that `ptr` refers to a block allocated through this allocator, that `layout` is exactly the layout used to allocate that block, that `new_size` is non-zero, and that rounding `new_size` up to a multiple of `layout.align()` does not overflow `isize` (the rounded size must be at most `isize::MAX`).\n- After [`realloc`](Self::realloc) returns a non-null pointer, do not access the original `ptr`; use only the returned pointer to access the memory, and use a layout with size `new_size` and the original alignment when deallocating it.",
      "diff": "Incorrect\n1. Generated adds unsupported caller preconditions for allocation, deallocation, and reallocation that are absent from ORIGINAL.\n2. Generated omits the implementor obligations prohibiting access to currently allocated memory, preventing unwinding, and preserving each method's contracts.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::GlobalAlloc",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported caller preconditions for allocation, deallocation, and reallocation that are absent from ORIGINAL.",
          "Generated omits the implementor obligations prohibiting access to currently allocated memory, preventing unwinding, and preserving each method's contracts."
        ]
      },
      "original_safety_doc": "The `GlobalAlloc` trait is an `unsafe` trait for a number of reasons, and\nimplementors must ensure that they adhere to these contracts:\n\n* It is undefined behavior for the allocator to read, write, or deallocate any memory that\n  is *currently allocated*. This memory is owned by the user, the allocator must not touch it.\n\n* It's undefined behavior if global allocators unwind. This restriction may\n  be lifted in the future, but currently a panic from any of these\n  functions may lead to memory unsafety.\n\n* Callers of this trait are allowed to rely on the contracts defined on each method, and\n  implementors must ensure such contracts remain true."
    },
    {
      "target_api_path": "core::alloc::GlobalAllocator",
      "source_api_path": "core::alloc::GlobalAllocator",
      "llm_generated_comment": "# Safety\n\nImplementors must uphold all safety invariants required by `StaticAllocator` and ensure that the implementation remains safe to use as a `GlobalAllocator` for its entire `'static` lifetime.",
      "diff": "Incorrect\n1. Generated requires implementors to uphold `StaticAllocator` invariants and maintain safety for the entire `'static` lifetime, conditions absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::GlobalAllocator",
        "classification": "Incorrect",
        "items": [
          "Generated requires implementors to uphold `StaticAllocator` invariants and maintain safety for the entire `'static` lifetime, conditions absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "When implementing a global allocator, one has to be careful not to create an infinitely\nrecursive implementation by accident, as many constructs in the Rust standard library may\nallocate in their implementation. For example, on some platforms, [`std::sync::Mutex`] may\nallocate, so using it is highly problematic in a global allocator.\n\nFor this reason, one should generally stick to library features available through\n[`core`], and avoid using [`std`] in a global allocator. A few features from [`std`] are\nguaranteed to not use `#[global_allocator]` to allocate:\n\n - [`std::thread_local`],\n - [`std::thread::current`],\n - [`std::thread::park`] and [`std::thread::Thread`]'s [`unpark`] method and\n[`Clone`] implementation.\n\n[`std`]: ../../std/index.html\n[`std::sync::Mutex`]: ../../std/sync/struct.Mutex.html\n[`std::thread_local`]: ../../std/macro.thread_local.html\n[`std::thread::current`]: ../../std/thread/fn.current.html\n[`std::thread::park`]: ../../std/thread/fn.park.html\n[`std::thread::Thread`]: ../../std/thread/struct.Thread.html\n[`unpark`]: ../../std/thread/struct.Thread.html#method.unpark"
    },
    {
      "target_api_path": "core::alloc::StaticAllocator",
      "source_api_path": "core::alloc::StaticAllocator",
      "llm_generated_comment": "# Safety\n\nImplementors must uphold all safety requirements of `Allocator` and ensure that their implementation remains valid for use as a `StaticAllocator` for its entire lifetime.",
      "diff": "Incorrect\n1. The generated section adds an obligation to uphold all `Allocator` safety requirements, which is not stated as a condition in the original.\n2. The generated section replaces the specific invalidation restriction and allocator invariants with a vague requirement to remain valid throughout the lifetime.\n3. The generated section omits obligations covering `Drop`, safe APIs, allocator lifetime expiration, and all equivalent allocators and subtypes.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::StaticAllocator",
        "classification": "Incorrect",
        "items": [
          "The generated section adds an obligation to uphold all `Allocator` safety requirements, which is not stated as a condition in the original.",
          "The generated section replaces the specific invalidation restriction and allocator invariants with a vague requirement to remain valid throughout the lifetime.",
          "The generated section omits obligations covering `Drop`, safe APIs, allocator lifetime expiration, and all equivalent allocators and subtypes."
        ]
      },
      "original_safety_doc": "Implementors must ensure that memory blocks are *only, ever* invalidated by a\ncall to a de/reallocating method on `Allocator`, and that this holds true for all\npossible instances of all subtypes of the implementor as well.\n\nThese requirements trivially apply to allocators that always maintain global state, such as\n`System` or `Global`. However, due to subtype coercion, it is *not* sound to implement\nfor an arbitrary `Allocator + 'static` due to [edge-case interactions][unsound] with e.g.\n`Pin::clone`. Namely, an impl of `StaticAllocator for MyAllocator + 'long` guarantees that any\nvalue of `MyAllocator + 'short` also fulfills the requirements of `StaticAllocator`.\n\nThe following must thus be guaranteed:\n- the `Drop` impl of the allocator does not invalidate any allocations;\n- the allocator does not expose a safe API surface that allows invalidating\n  its allocations;\n- the allocator's lifetime expiring does not invalidate any allocations;\n- the above also hold for all equivalent allocators (see [`Allocator`] docs).\n\n[`Pin`]: ../../core/pin/struct.Pin.html\n[unsound]: https://github.com/rust-lang/rust/issues/157089"
    },
    {
      "target_api_path": "core::alloc::global::GlobalAlloc::alloc",
      "source_api_path": "core::alloc::global::GlobalAlloc::alloc",
      "llm_generated_comment": "# Safety\n\n- `layout.size()` must be nonzero.",
      "diff": "Missing\n1. The generated section omits that the allocated memory block may or may not be initialized.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::alloc::global::GlobalAlloc::alloc",
        "classification": "Missing",
        "items": [
          "The generated section omits that the allocated memory block may or may not be initialized."
        ]
      },
      "original_safety_doc": "`layout` must have non-zero size. Attempting to allocate for a zero-sized `layout` will\nresult in undefined behavior.\n\n(Extension subtraits might provide more specific bounds on\nbehavior, e.g., guarantee a sentinel address or a null pointer\nin response to a zero-size allocation request.)\n\nThe allocated block of memory may or may not be initialized."
    },
    {
      "target_api_path": "core::alloc::global::GlobalAlloc::alloc_zeroed",
      "source_api_path": "core::alloc::global::GlobalAlloc::alloc_zeroed",
      "llm_generated_comment": "# Safety\n\n`layout` must have a non-zero size.",
      "diff": "Missing\n1. The allocated memory block is guaranteed to be initialized.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::alloc::global::GlobalAlloc::alloc_zeroed",
        "classification": "Missing",
        "items": [
          "The allocated memory block is guaranteed to be initialized."
        ]
      },
      "original_safety_doc": "The caller has to ensure that `layout` has non-zero size. Like `alloc`\nzero sized `layout` will result in undefined behavior.\nHowever the allocated block of memory is guaranteed to be initialized."
    },
    {
      "target_api_path": "core::alloc::global::GlobalAlloc::dealloc",
      "source_api_path": "core::alloc::global::GlobalAlloc::dealloc",
      "llm_generated_comment": "# Safety\n\n`ptr` must be non-null and denote a memory block that is currently allocated by this allocator. It must be the pointer returned for that allocation, not an interior or otherwise adjusted pointer, and the block must not have already been deallocated.\n\n`layout` must have nonzero size and be the same layout used to allocate the block.",
      "diff": "Incorrect\n1. The generated section requires `ptr` to be non-null, but ORIGINAL does not impose a non-null condition.\n2. The generated section requires `ptr` to be the exact returned pointer rather than an adjusted pointer, but ORIGINAL only requires a block currently allocated via this allocator.\n3. The generated section requires `layout` to have nonzero size, but ORIGINAL does not impose a nonzero-size condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::global::GlobalAlloc::dealloc",
        "classification": "Incorrect",
        "items": [
          "The generated section requires `ptr` to be non-null, but ORIGINAL does not impose a non-null condition.",
          "The generated section requires `ptr` to be the exact returned pointer rather than an adjusted pointer, but ORIGINAL only requires a block currently allocated via this allocator.",
          "The generated section requires `layout` to have nonzero size, but ORIGINAL does not impose a nonzero-size condition."
        ]
      },
      "original_safety_doc": "The caller must ensure:\n\n* `ptr` is a block of memory currently allocated via this allocator and,\n\n* `layout` is the same layout that was used to allocate that block of\n  memory.\n\nOtherwise the behavior is undefined."
    },
    {
      "target_api_path": "core::alloc::global::GlobalAlloc::realloc",
      "source_api_path": "core::alloc::global::GlobalAlloc::realloc",
      "llm_generated_comment": "# Safety\n\n- `ptr` must be non-null and denote a memory block that is currently allocated by this allocator.\n- `layout` must describe a layout that fits that allocation, and `layout.size()` must be nonzero.\n- `new_size` must be nonzero, and rounding `new_size` up to a multiple of `layout.align()` must not overflow or exceed `isize::MAX`.",
      "diff": "Incorrect\n1. `ptr` must be non-null adds a condition not required by ORIGINAL.\n2. `layout` must fit the allocation changes ORIGINAL's requirement that it be the exact layout used to allocate the block.\n3. `layout.size()` must be nonzero adds a condition not required by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::global::GlobalAlloc::realloc",
        "classification": "Incorrect",
        "items": [
          "`ptr` must be non-null adds a condition not required by ORIGINAL.",
          "`layout` must fit the allocation changes ORIGINAL's requirement that it be the exact layout used to allocate the block.",
          "`layout.size()` must be nonzero adds a condition not required by ORIGINAL."
        ]
      },
      "original_safety_doc": "The caller must ensure that:\n\n* `ptr` is allocated via this allocator,\n\n* `layout` is the same layout that was used\n  to allocate that block of memory,\n\n* `new_size` is greater than zero.\n\n* `new_size`, when rounded up to the nearest multiple of `layout.align()`,\n  does not overflow `isize` (i.e., the rounded value must be less than or\n  equal to `isize::MAX`).\n\nIf these are not followed, the behavior is undefined.\n\n(Extension subtraits might provide more specific bounds on\nbehavior, e.g., guarantee a sentinel address or a null pointer\nin response to a zero-size allocation request.)"
    },
    {
      "target_api_path": "core::alloc::layout::Layout::for_value_raw",
      "source_api_path": "core::alloc::layout::Layout::for_value_raw",
      "llm_generated_comment": "# Safety\n\nThe metadata of `val` must be valid for `T`:\n\n- If `T` is a slice, its length metadata must be initialized, and the slice’s size, rounded up to its alignment, must not exceed `isize::MAX`.\n- If `T` is a trait object, its vtable metadata must point to a valid vtable for `T` obtained through an unsizing coercion, and the value’s size, rounded up to its alignment, must not exceed `isize::MAX`.\n\nFor sized types, there are no additional requirements. For extern types, the call is safe, but the layout may be unavailable or incorrect.",
      "diff": "Incorrect\n1. The generated text requires initialized slice metadata, but ORIGINAL permits zero dynamic tail length without that additional requirement.\n2. The generated text requires trait-object vtable provenance from an unsizing coercion, which ORIGINAL does not require.\n3. The generated text limits conditions to directly unsized slices and trait objects, omitting unsized tails reached recursively through struct or tuple fields and `str`.\n4. The generated text requires size rounded up to alignment to fit `isize::MAX`, rather than requiring the entire value size to fit in `isize`.\n5. The generated text adds an extern-type safety claim, while ORIGINAL states no other qualifying unsized tail currently exists.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::alloc::layout::Layout::for_value_raw",
        "classification": "Incorrect",
        "items": [
          "The generated text requires initialized slice metadata, but ORIGINAL permits zero dynamic tail length without that additional requirement.",
          "The generated text requires trait-object vtable provenance from an unsizing coercion, which ORIGINAL does not require.",
          "The generated text limits conditions to directly unsized slices and trait objects, omitting unsized tails reached recursively through struct or tuple fields and `str`.",
          "The generated text requires size rounded up to alignment to fit `isize::MAX`, rather than requiring the entire value size to fit in `isize`.",
          "The generated text adds an extern-type safety claim, while ORIGINAL states no other qualifying unsized tail currently exists."
        ]
      },
      "original_safety_doc": "This function is safe to call if the pointer is safe to reborrow as `&T`\n(in which case you could also call [`for_value`][Self::for_value]).\nOtherwise, the following conditions must hold:\n\n- If `T` is `Sized`, this function is always safe to call.\n- If the unsized tail of `T` is:\n    - a [slice] `[U]`, `str`, or a [trait object] `dyn Trait`, then the size of the *entire value*\n      (dynamic tail length + statically sized prefix) must fit in `isize`.\n      For the special case where the dynamic tail length is 0, this function\n      is safe to call.\n    - No other kind of unsized tail currently exists that satisfies the trait bounds for this\n      function. If more kinds of unsized tails get introduced in the future, the documentation\n      of this function will have to be extended before it can be used for such types.\n\nHere, *unsized tail* refers to the type obtained by recursively descending through the last\nfield of a tuple or struct until we arrived at a built-in unsized type.\n\nAs a consequence of these rules, it is the case that whenever it is allowed to convert `val`\ninto a shared reference, then it is also allowed to invoke this function.\n\n[trait object]: ../../book/ch17-02-trait-objects.html\n[extern type]: ../../unstable-book/language-features/extern-types.html"
    },
    {
      "target_api_path": "core::alloc::layout::Layout::from_size_align_unchecked",
      "source_api_path": "core::alloc::layout::Layout::from_size_align_unchecked",
      "llm_generated_comment": "# Safety\n\n`align` must be a non-zero power of two, and `size` rounded up to the next multiple of `align` must not exceed `isize::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::alloc::layout::Layout::from_size_align_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This function is unsafe as it does not verify the preconditions from\n[`Layout::from_size_align`]."
    },
    {
      "target_api_path": "core::alloc::layout::Layout::from_size_alignment_unchecked",
      "source_api_path": "core::alloc::layout::Layout::from_size_alignment_unchecked",
      "llm_generated_comment": "# Safety\n\nThe allocation size obtained by rounding `size` up to a multiple of `alignment` must not exceed `isize::MAX`. Equivalently, `size + alignment.as_usize() - 1` must not overflow and must be at most `isize::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::alloc::layout::Layout::from_size_alignment_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This function is unsafe as it does not verify the preconditions from\n[`Layout::from_size_alignment`]."
    },
    {
      "target_api_path": "core::array::as_ascii_unchecked",
      "source_api_path": "core::array::as_ascii_unchecked",
      "llm_generated_comment": "# Safety\n\nEvery byte in `self` must be valid ASCII (in the range `0x00..=0x7F`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::array::as_ascii_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Every byte in the array must be in `0..=127`, or else this is UB."
    },
    {
      "target_api_path": "core::array::iter::IntoIter::new_unchecked",
      "source_api_path": "core::array::iter::IntoIter::new_unchecked",
      "llm_generated_comment": "# Safety\n\n- `initialized` must be a valid range within `buffer`: `initialized.start <= initialized.end` and `initialized.end <= N`.\n- Every element of `buffer` in `initialized` must be initialized with a valid value of `T` when this function is called, and must remain valid until the returned iterator consumes or drops it.",
      "diff": "Incorrect\n1. GENERATED requires each initialized element to remain valid until consumed or dropped; ORIGINAL imposes no such lifetime obligation.\n2. GENERATED requires initialized elements to contain valid values of T; ORIGINAL requires only that they be initialized.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::array::iter::IntoIter::new_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires each initialized element to remain valid until consumed or dropped; ORIGINAL imposes no such lifetime obligation.",
          "GENERATED requires initialized elements to contain valid values of T; ORIGINAL requires only that they be initialized."
        ]
      },
      "original_safety_doc": "- The `buffer[initialized]` elements must all be initialized.\n- The range must be canonical, with `initialized.start <= initialized.end`.\n- The range must be in-bounds for the buffer, with `initialized.end <= N`.\n  (Like how indexing `[0][100..100]` fails despite the range being empty.)\n\nIt's sound to have more elements initialized than mentioned, though that\nwill most likely result in them being leaked."
    },
    {
      "target_api_path": "core::ascii::ascii_char::AsciiChar::backward_unchecked",
      "source_api_path": "core::ascii::ascii_char::AsciiChar::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar values preceding `start`; stepping backward by `count` scalar values must not go before `'\\0'`. Equivalently:\n\n- if `start < '\\u{E000}'`, `count <= start as usize`;\n- otherwise, `count <= (start as usize) - 0x800`.",
      "diff": "Incorrect\n1. Generated restricts `count` using Unicode scalar-value predecessors and explicit boundaries, while ORIGINAL only requires preventing overflow of `Self`'s supported value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ascii::ascii_char::AsciiChar::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts `count` using Unicode scalar-value predecessors and explicit boundaries, while ORIGINAL only requires preventing overflow of `Self`'s supported value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::ascii::ascii_char::AsciiChar::digit_unchecked",
      "source_api_path": "core::ascii::ascii_char::AsciiChar::digit_unchecked",
      "llm_generated_comment": "# Safety\n\n`d` must be less than `10`.",
      "diff": "Incorrect\n1. Generated requires `d < 10`, but ORIGINAL permits `10 <= d <= 64` without making the call immediately undefined behavior.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ascii::ascii_char::AsciiChar::digit_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires `d < 10`, but ORIGINAL permits `10 <= d <= 64` without making the call immediately undefined behavior."
        ]
      },
      "original_safety_doc": "This is immediate UB if called with `d > 64`.\n\nIf `d >= 10` and `d <= 64`, this is allowed to return any value or panic.\nNotably, it should not be expected to return hex digits, or any other\nreasonable extension of the decimal digits.\n\n(This loose safety condition is intended to simplify soundness proofs\nwhen writing code using this method, since the implementation doesn't\nneed something really specific, not to make those other arguments do\nsomething useful. It might be tightened before stabilization.)"
    },
    {
      "target_api_path": "core::ascii::ascii_char::AsciiChar::forward_unchecked",
      "source_api_path": "core::ascii::ascii_char::AsciiChar::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that advancing `start` by `count` steps does not overflow the range of valid `char` values. In particular, the advancement must produce a value no greater than `0x10FFFF`, and if it crosses the surrogate range (`0xD800..=0xDFFF`), the corresponding adjustment must also remain within the valid `char` range.",
      "diff": "Incorrect\n1. Generated adds a surrogate-range adjustment requirement, but ORIGINAL only requires avoiding overflow of Self's supported value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ascii::ascii_char::AsciiChar::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds a surrogate-range adjustment requirement, but ORIGINAL only requires avoiding overflow of Self's supported value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::ascii::ascii_char::AsciiChar::from_u8_unchecked",
      "source_api_path": "core::ascii::ascii_char::AsciiChar::from_u8_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `b` is less than or equal to `127` (`0x7F`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ascii::ascii_char::AsciiChar::from_u8_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`b` must be in `0..=127`, or else this is UB."
    },
    {
      "target_api_path": "core::bstr::ByteStr::clone_to_uninit",
      "source_api_path": "core::bstr::ByteStr::clone_to_uninit",
      "llm_generated_comment": "# Safety\n\n`dest` must be valid and properly aligned for writing `self.to_bytes_with_nul().len()` consecutive bytes. The destination range must not overlap the bytes borrowed by `self`.",
      "diff": "Incorrect\n1. Generated additionally requires the destination range not overlap bytes borrowed by `self`, but ORIGINAL imposes no non-overlap condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::bstr::ByteStr::clone_to_uninit",
        "classification": "Incorrect",
        "items": [
          "Generated additionally requires the destination range not overlap bytes borrowed by `self`, but ORIGINAL imposes no non-overlap condition."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dest` must be [valid] for writes for `size_of_val(self)` bytes.\n* `dest` must be properly aligned to `align_of_val(self)`.\n\n[valid]: crate::ptr#safety\n[pointer metadata]: crate::ptr::metadata()"
    },
    {
      "target_api_path": "core::cell::CloneFromCell",
      "source_api_path": "core::cell::CloneFromCell",
      "llm_generated_comment": "# Safety\n\nImplementing this trait has no additional safety requirements beyond satisfying the `Clone` contract.",
      "diff": "Incorrect\n1. Generated says there are no additional safety requirements, whereas ORIGINAL requires implementing the trait only when the corresponding code is sound for that type.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::cell::CloneFromCell",
        "classification": "Incorrect",
        "items": [
          "Generated says there are no additional safety requirements, whereas ORIGINAL requires implementing the trait only when the corresponding code is sound for that type."
        ]
      },
      "original_safety_doc": "Implementing this trait for a type is sound if and only if the following code is sound for T =\nthat type.\n\n```\n#![feature(cell_get_cloned)]"
    },
    {
      "target_api_path": "core::cell::RefCell::try_borrow_unguarded",
      "source_api_path": "core::cell::RefCell::try_borrow_unguarded",
      "llm_generated_comment": "# Safety\n\nIf this method returns `Ok(reference)`, the caller must ensure that the value in this `RefCell` is not mutably borrowed or otherwise mutated for as long as `reference` remains in use.",
      "diff": "Incorrect\n1. Generated adds an unsupported prohibition on being otherwise mutated, beyond ORIGINAL's condition of mutable borrowing.\n2. Generated limits the obligation to while the reference is in use, whereas ORIGINAL applies while the reference remains alive.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::cell::RefCell::try_borrow_unguarded",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported prohibition on being otherwise mutated, beyond ORIGINAL's condition of mutable borrowing.",
          "Generated limits the obligation to while the reference is in use, whereas ORIGINAL applies while the reference remains alive."
        ]
      },
      "original_safety_doc": "Unlike `RefCell::borrow`, this method is unsafe because it does not\nreturn a `Ref`, thus leaving the borrow flag untouched. Mutably\nborrowing the `RefCell` while the reference returned by this method\nis alive is undefined behavior."
    },
    {
      "target_api_path": "core::cell::UnsafeCell::as_mut_unchecked",
      "source_api_path": "core::cell::UnsafeCell::as_mut_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that the value contained in the `UnsafeCell` is valid for `T` and that the returned `&mut T` is uniquely accessible for its entire lifetime: no other references or pointers may be used to access the contained value, and no other reference to it may remain usable, while the returned mutable reference is live.",
      "diff": "Incorrect\n1. The generated requirement that the contained value be valid for T is an unsupported additional condition.\n2. The generated requirement forbids all reference or pointer access throughout the returned reference's lifetime, while the original specifically prohibits mutation through other means during that lifetime.\n3. The generated wording changes the original call-time condition about any other references being alive into a broader lifetime-wide exclusivity requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::cell::UnsafeCell::as_mut_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated requirement that the contained value be valid for T is an unsupported additional condition.",
          "The generated requirement forbids all reference or pointer access throughout the returned reference's lifetime, while the original specifically prohibits mutation through other means during that lifetime.",
          "The generated wording changes the original call-time condition about any other references being alive into a broader lifetime-wide exclusivity requirement."
        ]
      },
      "original_safety_doc": "- It is Undefined Behavior to call this while any other\n  reference(s) to the wrapped value are alive.\n- Mutating the wrapped value through other means while the\n  returned reference is alive is Undefined Behavior."
    },
    {
      "target_api_path": "core::cell::UnsafeCell::as_ref_unchecked",
      "source_api_path": "core::cell::UnsafeCell::as_ref_unchecked",
      "llm_generated_comment": "# Safety\n\nThe underlying value must not be mutably accessed for the lifetime of the returned reference.",
      "diff": "Incorrect\n1. Generated condition applies only during the returned reference's lifetime, omitting the prohibition on an already-live mutable reference at call time.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::cell::UnsafeCell::as_ref_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated condition applies only during the returned reference's lifetime, omitting the prohibition on an already-live mutable reference at call time."
        ]
      },
      "original_safety_doc": "- It is Undefined Behavior to call this while any mutable\n  reference to the wrapped value is alive.\n- Mutating the wrapped value while the returned\n  reference is alive is Undefined Behavior."
    },
    {
      "target_api_path": "core::cell::UnsafeCell::replace",
      "source_api_path": "core::cell::UnsafeCell::replace",
      "llm_generated_comment": "# Safety\n\n- The caller must ensure that no other references to the `UnsafeCell`’s contents are used for the duration of this call.\n- The value passed in `src` must not overlap the `UnsafeCell`’s contents.",
      "diff": "Incorrect\n1. The generated requirement only prohibits using other references during the call, while ORIGINAL prohibits calls while any other references remain alive.\n2. The generated adds a requirement that `src` not overlap the contents, which is not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::cell::UnsafeCell::replace",
        "classification": "Incorrect",
        "items": [
          "The generated requirement only prohibits using other references during the call, while ORIGINAL prohibits calls while any other references remain alive.",
          "The generated adds a requirement that `src` not overlap the contents, which is not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "The caller must take care to avoid aliasing and data races.\n\n- It is Undefined Behavior to allow calls to race with\n  any other access to the wrapped value.\n- It is Undefined Behavior to call this while any other\n  reference(s) to the wrapped value are alive."
    },
    {
      "target_api_path": "core::char::as_ascii_unchecked",
      "source_api_path": "core::char::as_ascii_unchecked",
      "llm_generated_comment": "# Safety\n\nThe bytes of `self` must all be valid ASCII bytes (that is, `self.is_ascii()` must be `true`).",
      "diff": "Incorrect\n1. Generated requires all bytes of `self` to be valid ASCII, whereas ORIGINAL requires the character itself to be within the ASCII range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::char::as_ascii_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires all bytes of `self` to be valid ASCII, whereas ORIGINAL requires the character itself to be within the ASCII range."
        ]
      },
      "original_safety_doc": "This char must be within the ASCII range, or else this is UB."
    },
    {
      "target_api_path": "core::char::backward_unchecked",
      "source_api_path": "core::char::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that stepping backward by `count` from `start` remains within the range of valid `char` values. In particular, the intermediate backward step and, when `start >= 0xE000` and the intermediate result is below `0xE000`, the additional step over the surrogate-code-point gap must not underflow or otherwise leave the valid `char` range; the resulting value must be a valid Unicode scalar value.",
      "diff": "Incorrect\n1. Generated adds a specific surrogate-code-point gap condition that is not stated in the original safety requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::char::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds a specific surrogate-code-point gap condition that is not stated in the original safety requirement."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::char::forward_unchecked",
      "source_api_path": "core::char::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that advancing `start` by `count` does not overflow the `u32` range and that the resulting value, after accounting for the skipped surrogate-code-point range (`0xD800..=0xDFFF`), is a valid Unicode scalar value representable by `char`.",
      "diff": "Incorrect\n1. The generated section adds an unsupported requirement about surrogate handling and scalar-value validity beyond preventing overflow in `Self`'s supported range.\n2. It changes the overflow subject from `Self`'s supported range to the `u32` range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::char::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated section adds an unsupported requirement about surrogate handling and scalar-value validity beyond preventing overflow in `Self`'s supported range.",
          "It changes the overflow subject from `Self`'s supported range to the `u32` range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::char::from_u32_unchecked",
      "source_api_path": "core::char::from_u32_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `i` is a valid Unicode scalar value: it must be in the range `0..=0x10FFFF` and must not be in the surrogate range `0xD800..=0xDFFF`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::char::from_u32_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This function is unsafe, as it may construct invalid `char` values.\n\nFor a safe version of this function, see the [`from_u32`] function.\n\n[`from_u32`]: #method.from_u32"
    },
    {
      "target_api_path": "core::clone::CloneToUninit",
      "source_api_path": "core::clone::CloneToUninit",
      "llm_generated_comment": "# Safety\n\n`dest` must be non-null, properly aligned to `align_of_val(self)`, and valid for writes of `size_of_val(self)` bytes. These requirements apply even if the destination memory is uninitialized.",
      "diff": "Incorrect\n1. Generated imposes non-null, alignment, and write-validity requirements on `dest`, whereas ORIGINAL specifies an implementor obligation about initialization after normal return.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::clone::CloneToUninit",
        "classification": "Incorrect",
        "items": [
          "Generated imposes non-null, alignment, and write-validity requirements on `dest`, whereas ORIGINAL specifies an implementor obligation about initialization after normal return."
        ]
      },
      "original_safety_doc": "Implementations must ensure that when `.clone_to_uninit(dest)` returns normally rather than\npanicking, it always leaves `*dest` initialized as a valid value of type `Self`."
    },
    {
      "target_api_path": "core::clone::CloneToUninit::clone_to_uninit",
      "source_api_path": "core::clone::CloneToUninit::clone_to_uninit",
      "llm_generated_comment": "# Safety\n\n`dest` must be properly aligned and valid for writing `size_of_val(self)` bytes. The destination range must not overlap the memory occupied by `self` and must contain no initialized value that requires being dropped.",
      "diff": "Incorrect\n1. Generated additionally requires that the destination not overlap `self`, which is not an original safety condition.\n2. Generated additionally requires the destination to contain no initialized value requiring drop, which is not an original safety condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::clone::CloneToUninit::clone_to_uninit",
        "classification": "Incorrect",
        "items": [
          "Generated additionally requires that the destination not overlap `self`, which is not an original safety condition.",
          "Generated additionally requires the destination to contain no initialized value requiring drop, which is not an original safety condition."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dest` must be [valid] for writes for `size_of_val(self)` bytes.\n* `dest` must be properly aligned to `align_of_val(self)`.\n\n[valid]: crate::ptr#safety\n[pointer metadata]: crate::ptr::metadata()"
    },
    {
      "target_api_path": "core::clone::TrivialClone",
      "source_api_path": "core::clone::TrivialClone",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that cloning any valid value of the implementing type is trivial: `Clone::clone` must be equivalent to copying the value and must not perform additional operations, access invalid data, or rely on caller-provided state. This guarantee must hold for every valid value of the type.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements against accessing invalid data and relying on caller-provided state beyond equivalence to copying.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::clone::TrivialClone",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements against accessing invalid data and relying on caller-provided state beyond equivalence to copying."
        ]
      },
      "original_safety_doc": "`Clone::clone` must be equivalent to copying the value, otherwise calling functions\nsuch as `slice::clone_from_slice` can have undefined behaviour."
    },
    {
      "target_api_path": "core::convert::num::CheckedCastFromInt::unchecked_cast_from",
      "source_api_path": "core::convert::num::CheckedCastFromInt::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\nThe `value` must be representable in the destination type `Self`; its mathematical integer value must be within `Self`'s range.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::convert::num::CheckedCastFromInt::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::core_simd::cast::SimdCast",
      "source_api_path": "core::core_simd::cast::SimdCast",
      "llm_generated_comment": "# Safety\n\nImplementors must uphold all invariants required by `SimdCast` and its `SimdElement` supertrait.",
      "diff": "Incorrect\n1. Generated requires implementors to uphold SimdCast and SimdElement invariants, adding a supertrait obligation absent from ORIGINAL and failing to state validity for the simd_cast or simd_as intrinsics.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::cast::SimdCast",
        "classification": "Incorrect",
        "items": [
          "Generated requires implementors to uphold SimdCast and SimdElement invariants, adding a supertrait obligation absent from ORIGINAL and failing to state validity for the simd_cast or simd_as intrinsics."
        ]
      },
      "original_safety_doc": "Implementing this trait asserts that the type is a valid vector element for the `simd_cast` or\n`simd_as` intrinsics."
    },
    {
      "target_api_path": "core::core_simd::masks::Mask::from_simd_unchecked",
      "source_api_path": "core::core_simd::masks::Mask::from_simd_unchecked",
      "llm_generated_comment": "# Safety\n\n`<T as PrivateMethods>::valid(value)` must be `true`; that is, `value` must satisfy the validity invariant required for the underlying SIMD representation of `Mask<T, N>`.",
      "diff": "Incorrect\n1. Generated requires `<T as PrivateMethods>::valid(value)` to be true, an unsupported additional condition, and omits the requirement that all elements be 0 or -1.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::masks::Mask::from_simd_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires `<T as PrivateMethods>::valid(value)` to be true, an unsupported additional condition, and omits the requirement that all elements be 0 or -1."
        ]
      },
      "original_safety_doc": "All elements must be either 0 or -1."
    },
    {
      "target_api_path": "core::core_simd::masks::Mask::set_unchecked",
      "source_api_path": "core::core_simd::masks::Mask::set_unchecked",
      "llm_generated_comment": "# Safety\n\nThe `index` must be less than the number of elements in `self`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::masks::Mask::set_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`index` must be less than `self.len()`."
    },
    {
      "target_api_path": "core::core_simd::masks::Mask::test_unchecked",
      "source_api_path": "core::core_simd::masks::Mask::test_unchecked",
      "llm_generated_comment": "# Safety\n\n`index` must be less than the number of elements in `self` (that is, `index < N`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::masks::Mask::test_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`index` must be less than `self.len()`."
    },
    {
      "target_api_path": "core::core_simd::masks::MaskElement",
      "source_api_path": "core::core_simd::masks::MaskElement",
      "llm_generated_comment": "# Safety\n\nImplementors must uphold all safety invariants of `SimdElement`, `SimdCast`, and `PrivateMethods`, with the `SimdElement::Mask` associated type equal to `Self`.",
      "diff": "Incorrect\n1. GENERATED adds obligations to uphold `SimdElement`, `SimdCast`, and `PrivateMethods` invariants, which are unsupported by the ORIGINAL condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::masks::MaskElement",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds obligations to uphold `SimdElement`, `SimdCast`, and `PrivateMethods` invariants, which are unsupported by the ORIGINAL condition."
        ]
      },
      "original_safety_doc": "Type must be a signed integer."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::gather_ptr",
      "source_api_path": "core::core_simd::vector::Simd::gather_ptr",
      "llm_generated_comment": "# Safety\n\nEvery pointer in `source` must be properly aligned and valid for reading an initialized value of type `T`. Each pointed-to value must remain readable for the duration of the call, including being free from concurrent mutation that would cause a data race.",
      "diff": "Incorrect\n1. Generated requires each pointer to remain readable for the call's duration, adding a temporal obligation beyond the referenced core::ptr::read conditions.\n2. Generated adds a no-concurrent-mutation data-race condition not stated by the original safety requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::gather_ptr",
        "classification": "Incorrect",
        "items": [
          "Generated requires each pointer to remain readable for the call's duration, adding a temporal obligation beyond the referenced core::ptr::read conditions.",
          "Generated adds a no-concurrent-mutation data-race condition not stated by the original safety requirement."
        ]
      },
      "original_safety_doc": "Each read must satisfy the same conditions as [`core::ptr::read`]."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::gather_select_ptr",
      "source_api_path": "core::core_simd::vector::Simd::gather_select_ptr",
      "llm_generated_comment": "# Safety\n\nFor each lane enabled by `enable`, the corresponding pointer in `source` must be properly aligned, point to an initialized and valid value of type `T`, and be valid for reading for the duration of the call. Pointers in disabled lanes need not be valid.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::gather_select_ptr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Enabled elements must satisfy the same conditions as [`core::ptr::read`]."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::gather_select_unchecked",
      "source_api_path": "core::core_simd::vector::Simd::gather_select_unchecked",
      "llm_generated_comment": "# Safety\n\nFor every lane `i` where `enable[i]` is true, `idxs[i]` must be less than `slice.len()`, so that `slice.as_ptr().wrapping_add(idxs[i])` is valid and properly aligned for reading a `T`. Indices in disabled lanes need not be in bounds.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::gather_select_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this function with an `enable`d out-of-bounds index is *[undefined behavior]*\neven if the resulting value is not used."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::load_select_ptr",
      "source_api_path": "core::core_simd::vector::Simd::load_select_ptr",
      "llm_generated_comment": "# Safety\n\nFor every lane `i` enabled by `enable`, `ptr.add(i)` must be valid for reading a properly aligned and initialized value of type `T` for the duration of the call. Disabled lanes impose no validity requirement on the corresponding pointer.",
      "diff": "Incorrect\n1. Generated requires each enabled pointer to remain valid for the duration of the call, strengthening ORIGINAL's read-safety condition at the read operation.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::load_select_ptr",
        "classification": "Incorrect",
        "items": [
          "Generated requires each enabled pointer to remain valid for the duration of the call, strengthening ORIGINAL's read-safety condition at the read operation."
        ]
      },
      "original_safety_doc": "Enabled `ptr` elements must be safe to read as if by `core::ptr::read`."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::load_select_unchecked",
      "source_api_path": "core::core_simd::vector::Simd::load_select_unchecked",
      "llm_generated_comment": "# Safety\n\nFor every enabled lane `i` in `enable`, `i` must be less than `slice.len()`. Disabled lanes need not correspond to elements within `slice`; their values are taken from `or`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::load_select_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Enabled loads must not exceed the length of `slice`."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::scatter_ptr",
      "source_api_path": "core::core_simd::vector::Simd::scatter_ptr",
      "llm_generated_comment": "# Safety\n\n- Every pointer in `dest` must be non-null, properly aligned for `T`, and valid for writing a `T` value for the duration of this call.\n- The writable memory regions referenced by the pointers in `dest` must not overlap.",
      "diff": "Incorrect\n1. The generated section adds a non-overlap requirement for writable regions, which is not required by the original condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::scatter_ptr",
        "classification": "Incorrect",
        "items": [
          "The generated section adds a non-overlap requirement for writable regions, which is not required by the original condition."
        ]
      },
      "original_safety_doc": "Each write must satisfy the same conditions as [`core::ptr::write`]."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::scatter_select_ptr",
      "source_api_path": "core::core_simd::vector::Simd::scatter_select_ptr",
      "llm_generated_comment": "# Safety\n\n- For every lane whose corresponding `enable` value is `true`, the pointer in `dest` must be valid for writing a value of type `T`: it must be non-null, properly aligned, point to live storage, and that storage must be writable for the duration of this call.\n- For every lane whose corresponding `enable` value is `false`, the pointer in `dest` need not be valid and will not be accessed.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::scatter_select_ptr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Enabled pointers must satisfy the same conditions as [`core::ptr::write`]."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::scatter_select_unchecked",
      "source_api_path": "core::core_simd::vector::Simd::scatter_select_unchecked",
      "llm_generated_comment": "# Safety\n\n- For every lane where `enable` is `true`, the corresponding value in `idxs` must be less than `slice.len()`. Indices in disabled lanes are not accessed and need not be in bounds.\n- The enabled lanes must not contain duplicate indices; each enabled lane must designate a distinct element of `slice`.",
      "diff": "Incorrect\n1. Generated additionally requires enabled lanes to have distinct indices, but ORIGINAL states no duplicate-index safety condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::scatter_select_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated additionally requires enabled lanes to have distinct indices, but ORIGINAL states no duplicate-index safety condition."
        ]
      },
      "original_safety_doc": "Calling this function with an enabled out-of-bounds index is *[undefined behavior]*,\nand may lead to memory corruption."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::store_select_ptr",
      "source_api_path": "core::core_simd::vector::Simd::store_select_ptr",
      "llm_generated_comment": "# Safety\n\nFor every lane index `i` for which `enable` is `true`, `ptr.add(i)` must be properly aligned and valid for writing a `T`, including satisfying all allocation, bounds, and aliasing requirements for the duration of the call. No validity requirement applies to disabled lanes.",
      "diff": "Incorrect\n1. The generated condition uses `ptr.add(i)`, imposing its in-bounds requirements, whereas ORIGINAL specifies addresses calculated with `pointer::wrapping_offset`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::store_select_ptr",
        "classification": "Incorrect",
        "items": [
          "The generated condition uses `ptr.add(i)`, imposing its in-bounds requirements, whereas ORIGINAL specifies addresses calculated with `pointer::wrapping_offset`."
        ]
      },
      "original_safety_doc": "Memory addresses for element are calculated [`pointer::wrapping_offset`] and\neach enabled element must satisfy the same conditions as [`core::ptr::write`]."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::store_select_unchecked",
      "source_api_path": "core::core_simd::vector::Simd::store_select_unchecked",
      "llm_generated_comment": "# Safety\n\nFor every enabled lane at index `i`, `i` must be less than `slice.len()`. Equivalently, `slice` must contain all elements selected by `enable`; disabled lanes may lie beyond the end of the slice.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::store_select_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Every enabled element must be in bounds for the `slice`."
    },
    {
      "target_api_path": "core::core_simd::vector::Simd::to_int_unchecked",
      "source_api_path": "core::core_simd::vector::Simd::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\nEvery lane of `self` must be finite, and its value after truncation toward zero must be representable by the corresponding integer lane of `Int`. In particular, no lane may be NaN, infinite, or outside the target integer type’s range after truncation.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::Simd::to_int_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The value must:\n\n* Not be NaN\n* Not be infinite\n* Be representable in the return type, after truncating off its fractional part\n\nIf these requirements are infeasible or costly, consider using the safe function [cast],\nwhich saturates on conversion.\n\n[cast]: Simd::cast"
    },
    {
      "target_api_path": "core::core_simd::vector::SimdElement",
      "source_api_path": "core::core_simd::vector::SimdElement",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that `Self` is a valid SIMD vector element type and that `Self::Mask` is the mask element type corresponding to lanes of `Self`.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements that `Self` be a valid SIMD vector element type and that `Self::Mask` correspond to `Self` lanes.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::core_simd::vector::SimdElement",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements that `Self` be a valid SIMD vector element type and that `Self::Mask` correspond to `Self` lanes."
        ]
      },
      "original_safety_doc": "This trait, when implemented, asserts the compiler can monomorphize\n`#[repr(simd)]` structs with the marked type as an element.\nStrictly, it is valid to impl if the vector will not be miscompiled.\nPractically, it is user-unfriendly to impl it if the vector won't compile,\neven when no soundness guarantees are broken by allowing the user to try."
    },
    {
      "target_api_path": "core::f128::to_int_unchecked",
      "source_api_path": "core::f128::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\n`self` must not be `NaN`, and its value after truncating any fractional part toward zero must be representable by `Int`.",
      "diff": "Missing\n1. The generated section omits the original requirement that the value not be infinite.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::f128::to_int_unchecked",
        "classification": "Missing",
        "items": [
          "The generated section omits the original requirement that the value not be infinite."
        ]
      },
      "original_safety_doc": "The value must:\n\n* Not be `NaN`\n* Not be infinite\n* Be representable in the return type `Int`, after truncating off its fractional part"
    },
    {
      "target_api_path": "core::f16::to_int_unchecked",
      "source_api_path": "core::f16::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\n`self` must be finite, must not be NaN, and its value after truncating any fractional part toward zero must be representable by `Int`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::f16::to_int_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The value must:\n\n* Not be `NaN`\n* Not be infinite\n* Be representable in the return type `Int`, after truncating off its fractional part"
    },
    {
      "target_api_path": "core::f32::to_int_unchecked",
      "source_api_path": "core::f32::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\n`self` must be finite, and its value after truncating any fractional part toward zero must be representable by `Int`. In particular, `self` must not be NaN or infinite.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::f32::to_int_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The value must:\n\n* Not be `NaN`\n* Not be infinite\n* Be representable in the return type `Int`, after truncating off its fractional part"
    },
    {
      "target_api_path": "core::f64::to_int_unchecked",
      "source_api_path": "core::f64::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\nThe value of `self` must be finite and fit within the range of the return type `Int`. In particular, `self` must not be `NaN` or infinite, and the conversion must not produce a value outside the range representable by `Int`.",
      "diff": "Incorrect\n1. GENERATED requires `self` itself to fit `Int`'s range, while ORIGINAL requires only the value after truncation to be representable.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::f64::to_int_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires `self` itself to fit `Int`'s range, while ORIGINAL requires only the value after truncation to be representable."
        ]
      },
      "original_safety_doc": "The value must:\n\n* Not be `NaN`\n* Not be infinite\n* Be representable in the return type `Int`, after truncating off its fractional part"
    },
    {
      "target_api_path": "core::ffi::VaArgSafe",
      "source_api_path": "core::ffi::VaArgSafe",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that values of the implementing type can be safely passed through and retrieved from a C variadic argument list without violating Rust’s memory-safety invariants or the platform’s variadic calling-convention requirements.",
      "diff": "Incorrect\n1. Generated adds a broad requirement about memory safety and variadic calling conventions that is not stated in ORIGINAL.\n2. Generated omits the prohibition on implementing the trait for types subject to C default argument promotions.\n3. Generated omits the platform-specific restriction that 128-bit integer implementations require a defined __int128 type.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ffi::VaArgSafe",
        "classification": "Incorrect",
        "items": [
          "Generated adds a broad requirement about memory safety and variadic calling conventions that is not stated in ORIGINAL.",
          "Generated omits the prohibition on implementing the trait for types subject to C default argument promotions.",
          "Generated omits the platform-specific restriction that 128-bit integer implementations require a defined __int128 type."
        ]
      },
      "original_safety_doc": "When C passes variable arguments, signed integers smaller than [`c_int`] are promoted\nto [`c_int`], unsigned integers smaller than [`c_uint`] are promoted to [`c_uint`],\nand [`c_float`] is promoted to [`c_double`]. Implementing this trait for types that are\nsubject to this promotion rule is invalid.\n\nThis trait is only implemented for 128-bit integers when the platform defines the `__int128`\ntype.\n\n[`c_int`]: core::ffi::c_int\n[`c_long`]: core::ffi::c_long\n[`c_longlong`]: core::ffi::c_longlong\n\n[`c_uint`]: core::ffi::c_uint\n[`c_ulong`]: core::ffi::c_ulong\n[`c_ulonglong`]: core::ffi::c_ulonglong\n\n[`c_float`]: core::ffi::c_float\n[`c_double`]: core::ffi::c_double"
    },
    {
      "target_api_path": "core::ffi::c_str::CStr::clone_to_uninit",
      "source_api_path": "core::ffi::c_str::CStr::clone_to_uninit",
      "llm_generated_comment": "# Safety\n\n`dest` must point to writable memory large enough to hold `self.to_bytes_with_nul().len()` bytes. That memory must be uninitialized and must not overlap the memory occupied by `self`.",
      "diff": "Incorrect\n1. Generated requires memory to be uninitialized, but ORIGINAL only requires destination validity and alignment.\n2. Generated requires non-overlap with self, but ORIGINAL states no such condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ffi::c_str::CStr::clone_to_uninit",
        "classification": "Incorrect",
        "items": [
          "Generated requires memory to be uninitialized, but ORIGINAL only requires destination validity and alignment.",
          "Generated requires non-overlap with self, but ORIGINAL states no such condition."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dest` must be [valid] for writes for `size_of_val(self)` bytes.\n* `dest` must be properly aligned to `align_of_val(self)`.\n\n[valid]: crate::ptr#safety\n[pointer metadata]: crate::ptr::metadata()"
    },
    {
      "target_api_path": "core::ffi::c_str::CStr::from_bytes_with_nul_unchecked",
      "source_api_path": "core::ffi::c_str::CStr::from_bytes_with_nul_unchecked",
      "llm_generated_comment": "# Safety\n\n`bytes` must be non-empty, must end with a nul byte (`0`), and must contain no other nul bytes.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ffi::c_str::CStr::from_bytes_with_nul_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The provided slice **must** be nul-terminated and not contain any interior\nnul bytes."
    },
    {
      "target_api_path": "core::ffi::c_str::CStr::from_ptr",
      "source_api_path": "core::ffi::c_str::CStr::from_ptr",
      "llm_generated_comment": "# Safety\n\n- `ptr` must be non-null and point to an initialized, NUL-terminated sequence of `c_char`.\n- The first NUL byte must occur at an offset strictly less than `isize::MAX` from `ptr`.\n- The range from `ptr` through that first NUL byte, inclusive, must be contained within a single allocated object and be valid for reads.\n- That range must remain valid and must not be mutated for the entire lifetime `'a` of the returned `CStr`.",
      "diff": "Incorrect\n1. The generated bound requires the first NUL byte at an offset strictly less than isize::MAX, while ORIGINAL requires the terminator within isize::MAX from ptr.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ffi::c_str::CStr::from_ptr",
        "classification": "Incorrect",
        "items": [
          "The generated bound requires the first NUL byte at an offset strictly less than isize::MAX, while ORIGINAL requires the terminator within isize::MAX from ptr."
        ]
      },
      "original_safety_doc": "* The memory pointed to by `ptr` must contain a valid nul terminator at the\n  end of the string.\n\n* `ptr` must be [valid] for reads of bytes up to and including the nul terminator.\n  This means in particular:\n\n    * The entire memory range of this `CStr` must be contained within a single allocation!\n    * `ptr` must be non-null even for a zero-length cstr.\n\n* The memory referenced by the returned `CStr` must not be mutated for\n  the duration of lifetime `'a`.\n\n* The nul terminator must be within `isize::MAX` from `ptr`\n\n> **Note**: This operation is intended to be a 0-cost cast but it is\n> currently implemented with an up-front calculation of the length of\n> the string. This is not guaranteed to always be the case."
    },
    {
      "target_api_path": "core::ffi::va_list::VaList::next_arg",
      "source_api_path": "core::ffi::va_list::VaList::next_arg",
      "llm_generated_comment": "# Safety\n\n- `self` must be a valid, properly initialized `VaList` whose next argument is available to be consumed.\n- The next argument in `self` must have a type compatible with `T`; otherwise, calling this function is undefined behavior.",
      "diff": "Incorrect\n1. GENERATED requires `self` to be valid and properly initialized, an additional condition not stated in ORIGINAL.\n2. GENERATED omits the ORIGINAL integer representability requirement when `U` and `T` are integer types.\n3. GENERATED omits the ORIGINAL restriction on rereading non-`Copy` values through a cloned `VaList`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ffi::va_list::VaList::next_arg",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires `self` to be valid and properly initialized, an additional condition not stated in ORIGINAL.",
          "GENERATED omits the ORIGINAL integer representability requirement when `U` and `T` are integer types.",
          "GENERATED omits the ORIGINAL restriction on rereading non-`Copy` values through a cloned `VaList`."
        ]
      },
      "original_safety_doc": "This function is safe to call only if all of the following conditions are satisfied:\n\n- There is another c-variadic argument to read.\n- The actual type of the argument `U` is compatible with `T` (as defined below).\n- If `U` and `T` are both integer types, then the value passed by the caller must be\nrepresentable in both types.\n- If `T` is not [`Copy`], then it must not have already been read using `next_arg`\non a [`clone`][VaList::clone]d copy of this `VaList`.\n(Currently, all types implementing [`VaArgSafe`] also implement [`Copy`],\nbut this may change in the future.)\n\nTypes `T` and `U` are compatible when:\n\n- `T` and `U` are the same type.\n- `T` and `U` are integer types of the same size.\n- `T` and `U` are both pointers, and their target types are compatible.\n- `T` is a pointer to [`c_void`] and `U` is a pointer to [`i8`] or [`u8`], or vice versa.\n\n[`c_void`]: core::ffi::c_void"
    },
    {
      "target_api_path": "core::field::Field",
      "source_api_path": "core::field::Field",
      "llm_generated_comment": "# Safety\n\nImplementations must describe an actual field of `Base`: `Type` must be the field’s type, and `OFFSET` must be its exact byte offset within `Base`. Consequently, for any valid, properly aligned `Base`, the field must lie in bounds at `OFFSET` and be properly aligned for `Type`.",
      "diff": "Incorrect\n1. Generated requires `Type` to be the field's type and `OFFSET` to be its exact offset, strengthening the original existential condition.\n2. Generated requires the field to be in bounds and properly aligned for every valid, properly aligned `Base`, which is not required by the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::field::Field",
        "classification": "Incorrect",
        "items": [
          "Generated requires `Type` to be the field's type and `OFFSET` to be its exact offset, strengthening the original existential condition.",
          "Generated requires the field to be in bounds and properly aligned for every valid, properly aligned `Base`, which is not required by the original."
        ]
      },
      "original_safety_doc": "Given a valid value of type `Self::Base`, there exists a valid value of type `Self::Type` at\nbyte offset `OFFSET`"
    },
    {
      "target_api_path": "core::future::async_drop_in_place",
      "source_api_path": "core::future::async_drop_in_place",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that, once the returned future has completed, `_to_drop` is treated as uninitialized: it must not be read, accessed as a `T`, or dropped again unless it is first reinitialized. Its storage must remain valid for the duration of the drop operation.",
      "diff": "Incorrect\n1. Generated requires treating `_to_drop` as uninitialized only after completion, whereas ORIGINAL requires pointer validity for reads and writes through completion.\n2. Generated adds a post-completion no-read, no-access, and no-double-drop obligation absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::future::async_drop_in_place",
        "classification": "Incorrect",
        "items": [
          "Generated requires treating `_to_drop` as uninitialized only after completion, whereas ORIGINAL requires pointer validity for reads and writes through completion.",
          "Generated adds a post-completion no-read, no-access, and no-double-drop obligation absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "The pointer `_to_drop` must be valid for both reads and writes,\nnot only for the duration of this function call,\nbut also until the returned future has completed.\nSee [ptr::drop_in_place] for additional safety concerns.\n\n[ptr::drop_in_place]: crate::ptr::drop_in_place()"
    },
    {
      "target_api_path": "core::hint::assert_unchecked",
      "source_api_path": "core::hint::assert_unchecked",
      "llm_generated_comment": "# Safety\n\n`cond` must be `true`. Calling this function with `cond == false` results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::hint::assert_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`cond` must be `true`. It is immediate UB to call this with `false`."
    },
    {
      "target_api_path": "core::hint::unreachable_unchecked",
      "source_api_path": "core::hint::unreachable_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that execution can never reach the call to this function. Reaching this function results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::hint::unreachable_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Reaching this function is *Undefined Behavior*.\n\nAs the compiler assumes that all forms of Undefined Behavior can never\nhappen, it will eliminate all branches in the surrounding code that it can\ndetermine will invariably lead to a call to `unreachable_unchecked()`.\n\nIf the assumptions embedded in using this function turn out to be wrong -\nthat is, if the site which is calling `unreachable_unchecked()` is actually\nreachable at runtime - the compiler may have generated nonsensical machine\ninstructions for this situation, including in seemingly unrelated code,\ncausing difficult-to-debug problems.\n\nUse this function sparingly. Consider using the [`unreachable!`] macro,\nwhich may prevent some optimizations but will safely panic in case it is\nactually reached at runtime. Benchmark your code to find out if using\n`unreachable_unchecked()` comes with a performance benefit."
    },
    {
      "target_api_path": "core::i128::backward_unchecked",
      "source_api_path": "core::i128::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that stepping backward `count` positions from `start` stays within the range of valid `char` values. In particular, the backward step and any adjustment across the surrogate-code-point gap must not underflow or produce a value outside the valid Unicode scalar-value range.",
      "diff": "Incorrect\n1. Generated changes the subject from `Self`'s supported range to valid `char` values and adds an unsupported surrogate-gap adjustment condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i128::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated changes the subject from `Self`'s supported range to valid `char` values and adds an unsupported surrogate-gap adjustment condition."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::i128::forward_unchecked",
      "source_api_path": "core::i128::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` forward by `count` Unicode scalar values must not exceed `char::MAX`. Equivalently, `count` must be at most:\n\n- `0x10_FFFF - (start as u32) - 0x800` when `start < '\\u{D800}'`, or\n- `0x10_FFFF - (start as u32)` when `start >= '\\u{E000}'`.",
      "diff": "Incorrect\n1. GENERATED restricts advancement to Unicode scalar values and char::MAX, adding unsupported type-specific bounds absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i128::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED restricts advancement to Unicode scalar values and char::MAX, adding unsupported type-specific bounds absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::i128::unchecked_add",
      "source_api_path": "core::i128::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe mathematical sum of `self` and `rhs` must be representable as an `i128`; that is, it must be within `i128::MIN..=i128::MAX`. Otherwise, signed integer overflow results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > i128::MAX` or `self + rhs < i128::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: i128::checked_add\n[`wrapping_add`]: i128::wrapping_add"
    },
    {
      "target_api_path": "core::i128::unchecked_cast",
      "source_api_path": "core::i128::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in the destination type `T`; equivalently, a checked cast from `i128` to `T` must succeed.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::i128::unchecked_cast_from",
      "source_api_path": "core::i128::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `i128`; that is, it must be within `i128::MIN..=i128::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::i128::unchecked_div_exact",
      "source_api_path": "core::i128::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is nonzero, that `self` is exactly divisible by `rhs` (that is, `self % rhs == 0`), and that the division does not overflow (in particular, `self` must not be `i128::MIN` when `rhs` is `-1`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == i128::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::i128::unchecked_mul",
      "source_api_path": "core::i128::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe mathematical product of `self` and `rhs` must be representable as an `i128`; it must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > i128::MAX` or `self * rhs < i128::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: i128::checked_mul\n[`wrapping_mul`]: i128::wrapping_mul"
    },
    {
      "target_api_path": "core::i128::unchecked_neg",
      "source_api_path": "core::i128::unchecked_neg",
      "llm_generated_comment": "# Safety\n\n`self` must not be `i128::MIN`, as negating the minimum value would overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == i128::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: i128::checked_neg"
    },
    {
      "target_api_path": "core::i128::unchecked_shl",
      "source_api_path": "core::i128::unchecked_shl",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than `128` (the number of bits in an `i128`). Otherwise, the shift has undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: i128::checked_shl"
    },
    {
      "target_api_path": "core::i128::unchecked_shl_exact",
      "source_api_path": "core::i128::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than 128, and `self * 2^rhs` must be representable as an `i128` without overflow.",
      "diff": "Incorrect\n1. Generated adds an unsupported `rhs < 128` precondition beyond the original undefined-behavior condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_shl_exact",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported `rhs < 128` precondition beyond the original undefined-behavior condition."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`i128::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i128::unchecked_shr",
      "source_api_path": "core::i128::unchecked_shr",
      "llm_generated_comment": "# Safety\n\nThe `rhs` shift amount must be less than 128.",
      "diff": "Incorrect\n1. Generated requires `rhs` to be less than 128, while ORIGINAL requires it to be less than the number of bits in `self`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_shr",
        "classification": "Incorrect",
        "items": [
          "Generated requires `rhs` to be less than 128, while ORIGINAL requires it to be less than the number of bits in `self`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: i128::checked_shr"
    },
    {
      "target_api_path": "core::i128::unchecked_shr_exact",
      "source_api_path": "core::i128::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than 128, and `self` must be exactly divisible by `2^rhs`; equivalently, the least significant `rhs` bits of `self` must all be zero.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\ni128::BITS`\ni.e. when\n[`i128::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i128::unchecked_sub",
      "source_api_path": "core::i128::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe mathematical result of `self - rhs` must be representable as an `i128`; that is, it must be within the range `i128::MIN..=i128::MAX`. Otherwise, the subtraction may overflow and cause undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i128::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > i128::MAX` or `self - rhs < i128::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: i128::checked_sub\n[`wrapping_sub`]: i128::wrapping_sub"
    },
    {
      "target_api_path": "core::i16::backward_unchecked",
      "source_api_path": "core::i16::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar values preceding `start`; stepping backward by `count` must not pass before `char::MIN`. Equivalently, `count` must be at most `start as usize` when `start < '\\u{E000}'`, and at most `(start as usize) - 0x800` otherwise.",
      "diff": "Incorrect\n1. Generated imposes Unicode scalar-value and `char::MIN` bounds on `count`, but ORIGINAL requires preventing overflow of the `i16` value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i16::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated imposes Unicode scalar-value and `char::MIN` bounds on `count`, but ORIGINAL requires preventing overflow of the `i16` value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::i16::forward_unchecked",
      "source_api_path": "core::i16::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n- The caller must ensure that advancing `start as u32` by `count` does not overflow, and that the subsequent adjustment for skipping the surrogate range, when `start < 0xD800` and the intermediate result is at least `0xD800`, also does not overflow past `0x10FFFF`. Equivalently, the resulting value must be a valid Unicode scalar value (and therefore a valid `char`).",
      "diff": "Incorrect\n1. Generated imposes Unicode-scalar and u32 overflow conditions, whereas ORIGINAL requires only that the operation not overflow Self's supported i16 range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i16::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated imposes Unicode-scalar and u32 overflow conditions, whereas ORIGINAL requires only that the operation not overflow Self's supported i16 range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::i16::unchecked_add",
      "source_api_path": "core::i16::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum `self + rhs` must be representable as an `i16`; it must not overflow or underflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > i16::MAX` or `self + rhs < i16::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: i16::checked_add\n[`wrapping_add`]: i16::wrapping_add"
    },
    {
      "target_api_path": "core::i16::unchecked_cast",
      "source_api_path": "core::i16::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in `T`; that is, its value must lie within the range of values accepted by `T`’s `CheckedCastFromInt<i16>` implementation.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::i16::unchecked_cast_from",
      "source_api_path": "core::i16::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `i16`; that is, it must lie in the inclusive range `i16::MIN..=i16::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::i16::unchecked_div_exact",
      "source_api_path": "core::i16::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be exactly divisible by `rhs` with no remainder, and the quotient must be representable as an `i16` (in particular, `i16::MIN` must not be divided by `-1`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == i16::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::i16::unchecked_mul",
      "source_api_path": "core::i16::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe product of `self` and `rhs` must be representable as an `i16`; otherwise, the multiplication causes undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > i16::MAX` or `self * rhs < i16::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: i16::checked_mul\n[`wrapping_mul`]: i16::wrapping_mul"
    },
    {
      "target_api_path": "core::i16::unchecked_neg",
      "source_api_path": "core::i16::unchecked_neg",
      "llm_generated_comment": "# Safety\n\n`self` must not be equal to `i16::MIN`, because negating that value cannot be represented as an `i16` and would cause undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == i16::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: i16::checked_neg"
    },
    {
      "target_api_path": "core::i16::unchecked_shl",
      "source_api_path": "core::i16::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i16::BITS` (that is, less than 16).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: i16::checked_shl"
    },
    {
      "target_api_path": "core::i16::unchecked_shl_exact",
      "source_api_path": "core::i16::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be less than 16, the number of bits in an `i16`.\n- Shifting `self` left by `rhs` bits must not overflow the `i16` range; the mathematical result of `self * 2^rhs` must be representable as an `i16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`i16::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i16::unchecked_shr",
      "source_api_path": "core::i16::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than the number of bits in `i16`; that is, `rhs < 16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: i16::checked_shr"
    },
    {
      "target_api_path": "core::i16::unchecked_shr_exact",
      "source_api_path": "core::i16::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i16::BITS`, and `self` must be evenly divisible by `2^rhs` (equivalently, the lowest `rhs` bits of `self` must all be zero).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\ni16::BITS`\ni.e. when\n[`i16::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i16::unchecked_sub",
      "source_api_path": "core::i16::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction `self - rhs` must not overflow; its mathematical result must be representable as an `i16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i16::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > i16::MAX` or `self - rhs < i16::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: i16::checked_sub\n[`wrapping_sub`]: i16::wrapping_sub"
    },
    {
      "target_api_path": "core::i32::backward_unchecked",
      "source_api_path": "core::i32::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of valid Unicode scalar values preceding `start`. Equivalently, it must satisfy:\n\n- `count <= start as usize` when `start <= '\\u{D7FF}'`;\n- `count <= (start as usize) - 0x800` when `start >= '\\u{E000}'`.",
      "diff": "Incorrect\n1. Generated imposes Unicode scalar-value bounds on `count` and `start`, but ORIGINAL requires only that the operation not overflow `Self`'s supported value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i32::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated imposes Unicode scalar-value bounds on `count` and `start`, but ORIGINAL requires only that the operation not overflow `Self`'s supported value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::i32::forward_unchecked",
      "source_api_path": "core::i32::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n- `count` must be such that advancing `start` by `count` does not overflow the range of valid `char` values.\n- If that advancement crosses the surrogate range beginning at `0xD800`, the additional unchecked advancement by `0x800` must also not overflow the range of valid `char` values; the resulting value must be a valid Unicode scalar value.",
      "diff": "Incorrect\n1. Generated restricts the operation to valid `char` values and adds surrogate-range behavior, while ORIGINAL only requires that advancing within `Self` does not overflow.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i32::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts the operation to valid `char` values and adds surrogate-range behavior, while ORIGINAL only requires that advancing within `Self` does not overflow."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::i32::unchecked_add",
      "source_api_path": "core::i32::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must be representable as an `i32`; in other words, the addition must not overflow or underflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > i32::MAX` or `self + rhs < i32::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: i32::checked_add\n[`wrapping_add`]: i32::wrapping_add"
    },
    {
      "target_api_path": "core::i32::unchecked_cast",
      "source_api_path": "core::i32::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in the target type `T`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::i32::unchecked_cast_from",
      "source_api_path": "core::i32::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\nThe `value` must be representable in the destination integer type `Self`; otherwise, the unchecked conversion may cause undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::i32::unchecked_div_exact",
      "source_api_path": "core::i32::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must not be zero.\n- `self` must be evenly divisible by `rhs`.\n- The quotient must be representable as an `i32`; in particular, `self` must not be `i32::MIN` when `rhs` is `-1`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == i32::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::i32::unchecked_mul",
      "source_api_path": "core::i32::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; the mathematical product must be representable as an `i32`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > i32::MAX` or `self * rhs < i32::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: i32::checked_mul\n[`wrapping_mul`]: i32::wrapping_mul"
    },
    {
      "target_api_path": "core::i32::unchecked_neg",
      "source_api_path": "core::i32::unchecked_neg",
      "llm_generated_comment": "# Safety\n\n`self` must not be equal to `i32::MIN`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == i32::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: i32::checked_neg"
    },
    {
      "target_api_path": "core::i32::unchecked_shl",
      "source_api_path": "core::i32::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than the number of bits in `i32`; that is, `rhs < 32`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: i32::checked_shl"
    },
    {
      "target_api_path": "core::i32::unchecked_shl_exact",
      "source_api_path": "core::i32::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i32::BITS` (32), and shifting `self` left by `rhs` bits must be exact: the mathematical result `self * 2^rhs` must be representable as an `i32` without discarding any significant bits.",
      "diff": "Incorrect\n1. Generated additionally requires `rhs < i32::BITS`, a condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_shl_exact",
        "classification": "Incorrect",
        "items": [
          "Generated additionally requires `rhs < i32::BITS`, a condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`i32::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i32::unchecked_shr",
      "source_api_path": "core::i32::unchecked_shr",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than 32.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: i32::checked_shr"
    },
    {
      "target_api_path": "core::i32::unchecked_shr_exact",
      "source_api_path": "core::i32::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i32::BITS` (32), and `self` must be exactly divisible by `2^rhs`; equivalently, the lowest `rhs` bits of `self` must all be zero.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\ni32::BITS`\ni.e. when\n[`i32::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i32::unchecked_sub",
      "source_api_path": "core::i32::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction `self - rhs` must not overflow; its mathematical result must be representable as an `i32`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i32::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > i32::MAX` or `self - rhs < i32::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: i32::checked_sub\n[`wrapping_sub`]: i32::wrapping_sub"
    },
    {
      "target_api_path": "core::i64::backward_unchecked",
      "source_api_path": "core::i64::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of valid Unicode scalar values preceding `start`. Equivalently, after converting `start` to `u32`, `count` must be at most `start as u32` when `start <= '\\u{D7FF}'`, and at most `(start as u32) - 0x800` when `start >= '\\u{E000}'`.",
      "diff": "Incorrect\n1. The generated condition imposes a Unicode-scalar predecessor bound on `count`, whereas ORIGINAL requires only preventing overflow of `Self`'s supported value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i64::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition imposes a Unicode-scalar predecessor bound on `count`, whereas ORIGINAL requires only preventing overflow of `Self`'s supported value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::i64::forward_unchecked",
      "source_api_path": "core::i64::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not advance `start` past `char::MAX`, treating the surrogate range `0xD800..=0xDFFF` as skipped. Equivalently, writing `s = start as u32`:\n\n- if `s < 0xD800`, `count` must be at most `0x10F7FF - s`;\n- otherwise, `count` must be at most `0x10FFFF - s`.",
      "diff": "Incorrect\n1. Generated imposes unsupported `char::MAX` and surrogate-range conditions instead of requiring only that the operation not overflow `Self`'s supported range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i64::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated imposes unsupported `char::MAX` and surrogate-range conditions instead of requiring only that the operation not overflow `Self`'s supported range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::i64::unchecked_add",
      "source_api_path": "core::i64::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe mathematical sum of `self` and `rhs` must be representable as an `i64`; that is, the addition must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > i64::MAX` or `self + rhs < i64::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: i64::checked_add\n[`wrapping_add`]: i64::wrapping_add"
    },
    {
      "target_api_path": "core::i64::unchecked_cast",
      "source_api_path": "core::i64::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in the destination type `T`. Passing a value outside `T`'s representable range results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::i64::unchecked_cast_from",
      "source_api_path": "core::i64::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `i64` without truncation or sign change. In other words, it must lie in the range `i64::MIN..=i64::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::i64::unchecked_div_exact",
      "source_api_path": "core::i64::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that:\n\n- `rhs` is not zero.\n- `self` is evenly divisible by `rhs`.\n- The division does not overflow; in particular, `self` must not be `i64::MIN` when `rhs` is `-1`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == i64::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::i64::unchecked_mul",
      "source_api_path": "core::i64::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe product of `self` and `rhs` must be representable as an `i64`; that is, the multiplication must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > i64::MAX` or `self * rhs < i64::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: i64::checked_mul\n[`wrapping_mul`]: i64::wrapping_mul"
    },
    {
      "target_api_path": "core::i64::unchecked_neg",
      "source_api_path": "core::i64::unchecked_neg",
      "llm_generated_comment": "# Safety\n\n`self` must not be `i64::MIN`, as negating that value would overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == i64::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: i64::checked_neg"
    },
    {
      "target_api_path": "core::i64::unchecked_shl",
      "source_api_path": "core::i64::unchecked_shl",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than 64. Otherwise, the shift amount is invalid for an `i64` and calling this function results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: i64::checked_shl"
    },
    {
      "target_api_path": "core::i64::unchecked_shl_exact",
      "source_api_path": "core::i64::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i64::BITS` (64), and the mathematical value `self * 2^rhs` must be representable as an `i64`; equivalently, the shift must not discard significant bits.",
      "diff": "Incorrect\n1. Generated adds `rhs < 64`, but ORIGINAL permits no separate bit-width bound and defines undefined behavior using the combined leading-zero and leading-one predicate.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_shl_exact",
        "classification": "Incorrect",
        "items": [
          "Generated adds `rhs < 64`, but ORIGINAL permits no separate bit-width bound and defines undefined behavior using the combined leading-zero and leading-one predicate."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`i64::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i64::unchecked_shr",
      "source_api_path": "core::i64::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than 64.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: i64::checked_shr"
    },
    {
      "target_api_path": "core::i64::unchecked_shr_exact",
      "source_api_path": "core::i64::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i64::BITS` (64), and `self` must be exactly divisible by `2^rhs`; equivalently, the lowest `rhs` bits of `self` must all be zero.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\ni64::BITS`\ni.e. when\n[`i64::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i64::unchecked_sub",
      "source_api_path": "core::i64::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction `self - rhs` must not overflow; its mathematical result must be representable as an `i64`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i64::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > i64::MAX` or `self - rhs < i64::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: i64::checked_sub\n[`wrapping_sub`]: i64::wrapping_sub"
    },
    {
      "target_api_path": "core::i8::backward_unchecked",
      "source_api_path": "core::i8::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar values preceding `start`. Equivalently, if `start as u32` is less than `0xE000`, then `count <= start as u32`; otherwise, `count <= (start as u32) - 0x800`.",
      "diff": "Incorrect\n1. The generated condition imposes unsupported Unicode scalar-value and `start` boundary requirements instead of requiring only that the operation not overflow `Self`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i8::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition imposes unsupported Unicode scalar-value and `start` boundary requirements instead of requiring only that the operation not overflow `Self`."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::i8::forward_unchecked",
      "source_api_path": "core::i8::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` by `count` Unicode scalar values must not go past `char::MAX`. Equivalently, `count` must be at most `0x10FFFF - start as u32`, minus the `0x800` surrogate-code-point gap when `start < '\\u{D800}'`.",
      "diff": "Incorrect\n1. Generated changes the subject to Unicode scalar values and adds a surrogate-gap boundary absent from ORIGINAL, rather than requiring only that the operation not overflow Self's supported range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i8::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated changes the subject to Unicode scalar values and adds a surrogate-gap boundary absent from ORIGINAL, rather than requiring only that the operation not overflow Self's supported range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::i8::unchecked_add",
      "source_api_path": "core::i8::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must be representable as an `i8`; that is, it must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > i8::MAX` or `self + rhs < i8::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: i8::checked_add\n[`wrapping_add`]: i8::wrapping_add"
    },
    {
      "target_api_path": "core::i8::unchecked_cast",
      "source_api_path": "core::i8::unchecked_cast",
      "llm_generated_comment": "# Safety\n\nThe value of `self` must be representable in the target type `T`. In particular, the conversion must not overflow or otherwise produce a value outside the range of `T`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::i8::unchecked_cast_from",
      "source_api_path": "core::i8::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\nThe value passed as `value` must be representable by the target integer type `Self`; that is, converting it to `Self` must not require truncation or produce a value outside `Self`'s range.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::i8::unchecked_div_exact",
      "source_api_path": "core::i8::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must not be zero.\n- `self` must be evenly divisible by `rhs`.\n- The quotient must be representable as an `i8`; in particular, `self` must not be `i8::MIN` when `rhs == -1`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == i8::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::i8::unchecked_mul",
      "source_api_path": "core::i8::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe product of `self` and `rhs` must be representable as an `i8` without overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > i8::MAX` or `self * rhs < i8::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: i8::checked_mul\n[`wrapping_mul`]: i8::wrapping_mul"
    },
    {
      "target_api_path": "core::i8::unchecked_neg",
      "source_api_path": "core::i8::unchecked_neg",
      "llm_generated_comment": "# Safety\n\n`self` must not be equal to [`i8::MIN`], as its negation is not representable in `i8`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == i8::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: i8::checked_neg"
    },
    {
      "target_api_path": "core::i8::unchecked_shl",
      "source_api_path": "core::i8::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than the number of bits in `i8` (that is, `rhs < 8`).",
      "diff": "Incorrect\n1. Generated requires rhs < 8, while ORIGINAL requires rhs be less than the number of bits in self.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_shl",
        "classification": "Incorrect",
        "items": [
          "Generated requires rhs < 8, while ORIGINAL requires rhs be less than the number of bits in self."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: i8::checked_shl"
    },
    {
      "target_api_path": "core::i8::unchecked_shl_exact",
      "source_api_path": "core::i8::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i8::BITS` (8), and the shift must not discard any nonzero bits. Equivalently, the `rhs` most significant bits of `self` must all be zero.",
      "diff": "Incorrect\n1. Generated requires the most significant bits of `self` to be zero, omitting the required leading-ones condition for negative values.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_shl_exact",
        "classification": "Incorrect",
        "items": [
          "Generated requires the most significant bits of `self` to be zero, omitting the required leading-ones condition for negative values."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`i8::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i8::unchecked_shr",
      "source_api_path": "core::i8::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `i8::BITS` (that is, less than 8).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: i8::checked_shr"
    },
    {
      "target_api_path": "core::i8::unchecked_shr_exact",
      "source_api_path": "core::i8::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be less than the number of bits in `i8` (that is, `rhs < 8`).",
      "diff": "Incorrect\n1. Generated requires only `rhs < 8`, omitting the required `rhs <= self.trailing_zeros()` condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_shr_exact",
        "classification": "Incorrect",
        "items": [
          "Generated requires only `rhs < 8`, omitting the required `rhs <= self.trailing_zeros()` condition."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\ni8::BITS`\ni.e. when\n[`i8::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::i8::unchecked_sub",
      "source_api_path": "core::i8::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction `self - rhs` must be representable as an `i8`; it must not overflow the `i8` range (`-128..=127`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::i8::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > i8::MAX` or `self - rhs < i8::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: i8::checked_sub\n[`wrapping_sub`]: i8::wrapping_sub"
    },
    {
      "target_api_path": "core::index::Clamp::get_unchecked",
      "source_api_path": "core::index::Clamp::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str` with correct length metadata and provenance for its entire byte range.\n- The range must be in bounds: `self.start <= self.end <= slice.len()`.\n- Both `self.start` and `self.end` must lie on UTF-8 character boundaries of the string.",
      "diff": "Incorrect\n1. `slice` must be a valid `str` with correct metadata and provenance, but ORIGINAL only requires that the slice pointer not be dangling.\n2. Both endpoints must be UTF-8 character boundaries, but ORIGINAL imposes no character-boundary requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::index::Clamp::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "`slice` must be a valid `str` with correct metadata and provenance, but ORIGINAL only requires that the slice pointer not be dangling.",
          "Both endpoints must be UTF-8 character boundaries, but ORIGINAL imposes no character-boundary requirement."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::index::Clamp::get_unchecked_mut",
      "source_api_path": "core::index::Clamp::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid, properly aligned, non-null pointer to a valid `str` slice, and the slice must remain valid for mutable access for as long as the returned pointer is used.\n- `self.start` and `self.end` must satisfy `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` must both lie on UTF-8 character boundaries of `slice`, so that the returned pointer refers to a valid `str`.\n- While the returned pointer is used, the caller must uphold Rust's aliasing requirements for mutable references, including ensuring that no conflicting accesses to the referenced range occur.",
      "diff": "Incorrect\n1. Generated adds unsupported alignment, non-null, initialization, lifetime, and aliasing obligations beyond the original preconditions.\n2. Generated requires UTF-8 character boundaries and range bounds, which are additional conditions not stated in the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::index::Clamp::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported alignment, non-null, initialization, lifetime, and aliasing obligations beyond the original preconditions.",
          "Generated requires UTF-8 character boundaries and range bounds, which are additional conditions not stated in the original."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::index::Last::get_unchecked",
      "source_api_path": "core::index::Last::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range `self.start..self.end` must be contained within that string (`self.start <= self.end <= slice.len()`), and both `self.start` and `self.end` must lie on UTF-8 character boundaries. The pointed-to allocation must remain valid for any later use of the returned pointer.",
      "diff": "Incorrect\n1. The generated UTF-8 character-boundary requirement is an additional condition absent from the original safety contract.\n2. The generated allocation-lifetime requirement limits invalidity to later use, whereas the original makes a dangling slice pointer undefined behavior even when the returned pointer is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::index::Last::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated UTF-8 character-boundary requirement is an additional condition absent from the original safety contract.",
          "The generated allocation-lifetime requirement limits invalidity to later use, whereas the original makes a dangling slice pointer undefined behavior even when the returned pointer is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::index::Last::get_unchecked_mut",
      "source_api_path": "core::index::Last::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range `self.start..self.end` must be within that string (`self.start <= self.end <= slice.len()`), and both bounds must lie on UTF-8 code point boundaries. The allocation backing `slice` must remain valid for any use of the returned pointer.",
      "diff": "Incorrect\n1. Generated requires `slice` to point to a valid `str`, adding an unsupported precondition beyond ORIGINAL's dangling-pointer condition.\n2. Generated requires UTF-8 code point boundaries, an unsupported condition absent from ORIGINAL.\n3. Generated requires the backing allocation to remain valid for any use of the returned pointer, weakening ORIGINAL's unconditional dangling-pointer prohibition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::index::Last::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires `slice` to point to a valid `str`, adding an unsupported precondition beyond ORIGINAL's dangling-pointer condition.",
          "Generated requires UTF-8 code point boundaries, an unsupported condition absent from ORIGINAL.",
          "Generated requires the backing allocation to remain valid for any use of the returned pointer, weakening ORIGINAL's unconditional dangling-pointer prohibition."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::intrinsics::align_of_val",
      "source_api_path": "core::intrinsics::align_of_val",
      "llm_generated_comment": "# Safety\n\nThe `ptr` pointer must be valid for reads for the value of type `T` it points to, including valid metadata when `T` is dynamically sized.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::align_of_val",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "See [`crate::mem::align_of_val_raw`] for safety conditions."
    },
    {
      "target_api_path": "core::intrinsics::arith_offset",
      "source_api_path": "core::intrinsics::arith_offset",
      "llm_generated_comment": "# Safety\n\nIf `offset` is nonzero and `T` is not zero-sized:\n\n- `dst` must be derived from an allocated object.\n- Both `dst` and the resulting pointer must lie within that same allocation or be one-past-the-end of it, and the entire pointer range between them must remain within those bounds.\n- `offset * size_of::<T>()` must fit in an `isize`, and computing the resulting address must not rely on address-space wraparound.",
      "diff": "Incorrect\n1. GENERATED adds allocation, bounds, provenance, representability, and no-address-wraparound requirements absent from ORIGINAL, which states wrapping arithmetic and permits results unsuitable for memory access.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::arith_offset",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds allocation, bounds, provenance, representability, and no-address-wraparound requirements absent from ORIGINAL, which states wrapping arithmetic and permits results unsuitable for memory access."
        ]
      },
      "original_safety_doc": "Unlike the `offset` intrinsic, this intrinsic does not restrict the\nresulting pointer to point into or at the end of an allocated\nobject, and it wraps with two's complement arithmetic. The resulting\nvalue is not necessarily valid to be used to actually access memory.\n\nThe stabilized version of this intrinsic is [`pointer::wrapping_offset`]."
    },
    {
      "target_api_path": "core::intrinsics::compare_bytes",
      "source_api_path": "core::intrinsics::compare_bytes",
      "llm_generated_comment": "# Safety\n\n`left` and `right` must each be valid for reading `bytes` consecutive bytes for the duration of the call.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::compare_bytes",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`left` and `right` must each be [valid] for reads of `bytes` bytes.\n\nNote that this applies to the whole range, not just until the first byte\nthat differs.  That allows optimizations that can read in large chunks.\n\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::intrinsics::const_allocate",
      "source_api_path": "core::intrinsics::const_allocate",
      "llm_generated_comment": "# Safety\n\nThe returned pointer must not be dereferenced or used for memory access unless it points to a live allocation that is valid for the access, with sufficient size and alignment.",
      "diff": "Missing\n1. The generated section omits the requirement that `align` must be a power of two.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::intrinsics::const_allocate",
        "classification": "Missing",
        "items": [
          "The generated section omits the requirement that `align` must be a power of two."
        ]
      },
      "original_safety_doc": "- The `align` argument must be a power of two.\n   - At compile time, a compile error occurs if this constraint is violated.\n   - At runtime, it is not checked."
    },
    {
      "target_api_path": "core::intrinsics::const_deallocate",
      "source_api_path": "core::intrinsics::const_deallocate",
      "llm_generated_comment": "# Safety\n\nThis function has no safety preconditions.",
      "diff": "Incorrect\n1. The generated section states there are no safety preconditions, contradicting the required power-of-two alignment and pointer provenance restrictions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::const_deallocate",
        "classification": "Incorrect",
        "items": [
          "The generated section states there are no safety preconditions, contradicting the required power-of-two alignment and pointer provenance restrictions."
        ]
      },
      "original_safety_doc": "- The `align` argument must be a power of two.\n   - At compile time, a compile error occurs if this constraint is violated.\n   - At runtime, it is not checked.\n- If the `ptr` is created in an another const, this intrinsic doesn't deallocate it.\n- If the `ptr` is pointing to a local variable, this intrinsic doesn't deallocate it."
    },
    {
      "target_api_path": "core::intrinsics::disjoint_bitor",
      "source_api_path": "core::intrinsics::disjoint_bitor",
      "llm_generated_comment": "# Safety\n\nThe bit representations of `a` and `b` must be disjoint: no bit may be set in both operands.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(a & b) == 0`, or equivalently that `(a | b) == (a + b)`.\n\nOtherwise it's immediate UB."
    },
    {
      "target_api_path": "core::intrinsics::offset",
      "source_api_path": "core::intrinsics::offset",
      "llm_generated_comment": "# Safety\n\nThe byte offset `count * size_of::<T>()` must be representable as an `isize`, and adding that offset to `self` must not overflow or underflow the address space.\n\nIf `T` is non-zero-sized, both `self` and the resulting pointer must be within, or one byte past the end of, the same allocated object.",
      "diff": "Incorrect\n1. Generated restricts non-zero-sized `T`, while ORIGINAL applies whenever the computed offset is non-zero.\n2. Generated requires both pointers belong to the same allocated object, an additional condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::offset",
        "classification": "Incorrect",
        "items": [
          "Generated restricts non-zero-sized `T`, while ORIGINAL applies whenever the computed offset is non-zero.",
          "Generated requires both pointers belong to the same allocated object, an additional condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "If the computed offset is non-zero, then both the starting and resulting pointer must be\neither in bounds or at the end of an allocation. If either pointer is out\nof bounds or arithmetic overflow occurs then this operation is undefined behavior.\n\nThe stabilized version of this intrinsic is [`pointer::offset`]."
    },
    {
      "target_api_path": "core::intrinsics::raw_eq",
      "source_api_path": "core::intrinsics::raw_eq",
      "llm_generated_comment": "# Safety\n\n- `T` must have no padding or other bytes that may be uninitialized; all bytes of the values referenced by `a` and `b` must be valid to read for the duration of the call.",
      "diff": "Incorrect\n1. The generated condition requires all referenced bytes to be valid to read, adding a requirement beyond ORIGINAL's initialization condition.\n2. The generated section omits ORIGINAL's compile-time prohibition on bytes having provenance.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::raw_eq",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires all referenced bytes to be valid to read, adding a requirement beyond ORIGINAL's initialization condition.",
          "The generated section omits ORIGINAL's compile-time prohibition on bytes having provenance."
        ]
      },
      "original_safety_doc": "It's UB to call this if any of the *bytes* in `*a` or `*b` are uninitialized.\nNote that this is a stricter criterion than just the *values* being\nfully-initialized: if `T` has padding, it's UB to call this intrinsic.\n\nAt compile-time, it is furthermore UB to call this if any of the bytes\nin `*a` or `*b` have provenance.\n\n(The implementation is allowed to branch on the results of comparisons,\nwhich is UB if any of their inputs are `undef`.)"
    },
    {
      "target_api_path": "core::intrinsics::simd::scalable::sve_tuple_get",
      "source_api_path": "core::intrinsics::simd::scalable::sve_tuple_get",
      "llm_generated_comment": "# Safety\n\n- `SVecTup` must be a supported SVE tuple type whose elements are vectors of type `SVec`.\n- `IDX` must identify an element within the tuple represented by `SVecTup`.",
      "diff": "Incorrect\n1. Generated requires `SVecTup` to be a supported SVE tuple type with vector elements, an additional condition absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::scalable::sve_tuple_get",
        "classification": "Incorrect",
        "items": [
          "Generated requires `SVecTup` to be a supported SVE tuple type with vector elements, an additional condition absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "`IDX` must be in-bounds of the tuple."
    },
    {
      "target_api_path": "core::intrinsics::simd::scalable::sve_tuple_set",
      "source_api_path": "core::intrinsics::simd::scalable::sve_tuple_set",
      "llm_generated_comment": "# Safety\n\n- `SVecTup` must be a valid SVE tuple type, and `SVec` must be the tuple’s corresponding SVE vector element type.\n- `IDX` must designate a valid element of `SVecTup`; it must be nonnegative and less than the number of vectors in the tuple.",
      "diff": "Incorrect\n1. GENERATED adds unsupported validity requirements for `SVecTup` and `SVec`; ORIGINAL only requires `IDX` to be in-bounds of the tuple.\n2. GENERATED requires `IDX` to be nonnegative, an unsupported condition because the original only specifies that it is in-bounds.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::scalable::sve_tuple_set",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds unsupported validity requirements for `SVecTup` and `SVec`; ORIGINAL only requires `IDX` to be in-bounds of the tuple.",
          "GENERATED requires `IDX` to be nonnegative, an unsupported condition because the original only specifies that it is in-bounds."
        ]
      },
      "original_safety_doc": "`IDX` must be in-bounds of the tuple."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_bitmask",
      "source_api_path": "core::intrinsics::simd::simd_bitmask",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `T` is a SIMD vector type with an integer element type, and that `U` is an integer type with at least as many bits as `T` has lanes.",
      "diff": "Incorrect\n1. The generated type and lane-count requirements are unsupported by the original, which instead requires every element of `x` to be `0` or `!0`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_bitmask",
        "classification": "Incorrect",
        "items": [
          "The generated type and lane-count requirements are unsupported by the original, which instead requires every element of `x` to be `0` or `!0`."
        ]
      },
      "original_safety_doc": "`x` must contain only `0` and `!0`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_cast",
      "source_api_path": "core::intrinsics::simd::simd_cast",
      "llm_generated_comment": "# Safety\n\n`T` and `U` must both be SIMD vector types with the same number of lanes. Their lane element types must each be a primitive integer or floating-point type.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `T` and `U` have the same lane count and primitive lane element types.\n2. Generated omits the requirements for float-to-integer casts: elements must be non-NaN, finite, and representable after truncation.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_cast",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `T` and `U` have the same lane count and primitive lane element types.",
          "Generated omits the requirements for float-to-integer casts: elements must be non-NaN, finite, and representable after truncation."
        ]
      },
      "original_safety_doc": "Casting from integer types is always safe.\nCasting between two float types is also always safe.\n\nCasting floats to integers truncates, following the same rules as `to_int_unchecked`.\nSpecifically, each element must:\n* Not be `NaN`\n* Not be infinite\n* Be representable in the return type, after truncating off its fractional part"
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_div",
      "source_api_path": "core::intrinsics::simd::simd_div",
      "llm_generated_comment": "# Safety\n\nThe arguments must be SIMD vectors with the same element type and number of lanes, and the element-wise division must be valid for every lane of `rhs` (in particular, integer lanes must not be divided by zero or overflow).",
      "diff": "Incorrect\n1. The generated section requires matching SIMD element types and lane counts, an additional condition not present in the original safety requirements.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_div",
        "classification": "Incorrect",
        "items": [
          "The generated section requires matching SIMD element types and lane counts, an additional condition not present in the original safety requirements."
        ]
      },
      "original_safety_doc": "For integers, `rhs` must not contain any zero elements.\nAdditionally for signed integers, `<int>::MIN / -1` is undefined behavior."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_extract",
      "source_api_path": "core::intrinsics::simd::simd_extract",
      "llm_generated_comment": "# Safety\n\n- `x` must be a SIMD vector whose element type is `U`.\n- `idx` must be less than the number of elements in the SIMD vector `x`.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `x` have element type `U`; ORIGINAL only requires `idx` to be const and in-bounds.\n2. Generated omits ORIGINAL's requirement that `idx` be const.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_extract",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `x` have element type `U`; ORIGINAL only requires `idx` to be const and in-bounds.",
          "Generated omits ORIGINAL's requirement that `idx` be const."
        ]
      },
      "original_safety_doc": "`idx` must be const and in-bounds of the vector."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_extract_dyn",
      "source_api_path": "core::intrinsics::simd::simd_extract_dyn",
      "llm_generated_comment": "# Safety\n\n`idx` must be less than the number of lanes in the SIMD vector `x`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_extract_dyn",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`idx` must be in-bounds of the vector."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_funnel_shl",
      "source_api_path": "core::intrinsics::simd::simd_funnel_shl",
      "llm_generated_comment": "# Safety\n\n- `T` must be a supported SIMD integer-vector type for this intrinsic.\n- The value of each lane of `shift` must be less than the number of bits in the corresponding lane type.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `T` be a supported SIMD integer-vector type.\n2. Generated changes the bound from `int::BITS` to the number of bits in the corresponding lane type.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_funnel_shl",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `T` be a supported SIMD integer-vector type.",
          "Generated changes the bound from `int::BITS` to the number of bits in the corresponding lane type."
        ]
      },
      "original_safety_doc": "Each element of `shift` must be in `0..<int>::BITS`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_funnel_shr",
      "source_api_path": "core::intrinsics::simd::simd_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`T` must be a SIMD vector type with integer element types.",
      "diff": "Incorrect\n1. The generated `T` restriction is unsupported and omits the original requirement that every `shift` element be less than `int::BITS.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_funnel_shr",
        "classification": "Incorrect",
        "items": [
          "The generated `T` restriction is unsupported and omits the original requirement that every `shift` element be less than `int::BITS."
        ]
      },
      "original_safety_doc": "Each element of `shift` must be in `0..<int>::BITS`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_gather",
      "source_api_path": "core::intrinsics::simd::simd_gather",
      "llm_generated_comment": "# Safety\n\n- `T` must be a SIMD vector, `U` must be a SIMD vector with the same number of lanes whose elements are pointers to `T`'s element type, and `V` must be a mask vector with the same number of lanes.\n- Every lane of `mask` must contain a valid mask value: all zeroes for a disabled lane or all ones for an enabled lane.\n- For each enabled lane, the corresponding pointer in `ptr` must be properly aligned and valid for reading one initialized value of `T`'s element type. The read must not violate aliasing or data-race rules. Pointers in disabled lanes need not be valid.",
      "diff": "Incorrect\n1. Generated requires specific SIMD types, lane counts, and pointer element relationships that are not safety conditions stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_gather",
        "classification": "Incorrect",
        "items": [
          "Generated requires specific SIMD types, lane counts, and pointer element relationships that are not safety conditions stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Each pointer in `ptr` whose corresponding value in `mask` is `!0` must be readable as if by\n[`ptr::read`][crate::ptr::read] (e.g. aligned to the element type).\n\n`mask` must only contain `0` or `!0` values."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_insert",
      "source_api_path": "core::intrinsics::simd::simd_insert",
      "llm_generated_comment": "# Safety\n\n- `x` must be a SIMD vector, and `U` must be its element type.\n- `idx` must be less than the number of elements (lanes) in `x`.",
      "diff": "Incorrect\n1. The generated condition requires `x` to be a SIMD vector and `U` to be its element type, adding safety obligations absent from the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_insert",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires `x` to be a SIMD vector and `U` to be its element type, adding safety obligations absent from the original."
        ]
      },
      "original_safety_doc": "`idx` must be in-bounds of the vector."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_insert_dyn",
      "source_api_path": "core::intrinsics::simd::simd_insert_dyn",
      "llm_generated_comment": "# Safety\n\n`idx` must be less than the number of lanes in the SIMD vector `x`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_insert_dyn",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "`idx` must be in-bounds of the vector."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_masked_load",
      "source_api_path": "core::intrinsics::simd::simd_masked_load",
      "llm_generated_comment": "# Safety\n\n- `T` must be a SIMD vector, `V` must be a compatible mask vector with the same number of lanes, and every mask lane must have a valid mask value (all zeros or all ones).\n- `U` must be a pointer to the element type of `T`.\n- For every enabled mask lane `i`, `ptr` advanced to lane `i` must be properly aligned as specified by `ALIGN` and valid for reading a properly initialized element for the duration of the call. No validity or alignment requirement applies solely because of a disabled lane.",
      "diff": "Incorrect\n1. Generated requires `T`, `V`, and `U` type relationships absent from ORIGINAL.\n2. Generated makes alignment lane-specific and only for enabled lanes, differing from ORIGINAL's `ptr` alignment requirement according to `ALIGN`.\n3. Generated requires readable values to be properly initialized and valid for the call duration, strengthening ORIGINAL's `ptr::read` readability condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_masked_load",
        "classification": "Incorrect",
        "items": [
          "Generated requires `T`, `V`, and `U` type relationships absent from ORIGINAL.",
          "Generated makes alignment lane-specific and only for enabled lanes, differing from ORIGINAL's `ptr` alignment requirement according to `ALIGN`.",
          "Generated requires readable values to be properly initialized and valid for the call duration, strengthening ORIGINAL's `ptr::read` readability condition."
        ]
      },
      "original_safety_doc": "`ptr` must be aligned according to the `ALIGN` parameter, see [`SimdAlign`] for details.\n\nEach pointer offset from `ptr` whose corresponding value in `mask` is `!0` must be readable as if\nby [`ptr::read`][crate::ptr::read].\n\n`mask` must only contain `0` or `!0` values."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_masked_store",
      "source_api_path": "core::intrinsics::simd::simd_masked_store",
      "llm_generated_comment": "# Safety\n\nFor every lane enabled by `mask`, the corresponding memory location derived from `ptr` must:\n\n- be properly aligned according to `ALIGN`,\n- be valid for writing the corresponding lane of `val`,\n- have valid provenance and remain writable for the duration of the call, and\n- not be accessed in a manner that conflicts with that write.\n\nNo validity or writability requirement applies to locations corresponding only to disabled lanes.",
      "diff": "Incorrect\n1. The generated text omits the requirement that mask contain only 0 or !0 values.\n2. The generated text limits alignment to enabled lanes instead of requiring ptr itself to satisfy ALIGN unconditionally.\n3. The generated text adds valid provenance and duration requirements unsupported by the original.\n4. The generated text adds a non-conflicting-access requirement unsupported by the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_masked_store",
        "classification": "Incorrect",
        "items": [
          "The generated text omits the requirement that mask contain only 0 or !0 values.",
          "The generated text limits alignment to enabled lanes instead of requiring ptr itself to satisfy ALIGN unconditionally.",
          "The generated text adds valid provenance and duration requirements unsupported by the original.",
          "The generated text adds a non-conflicting-access requirement unsupported by the original."
        ]
      },
      "original_safety_doc": "`ptr` must be aligned according to the `ALIGN` parameter, see [`SimdAlign`] for details.\n\nEach pointer offset from `ptr` whose corresponding value in `mask` is `!0` must be writable as if\nby [`ptr::write`][crate::ptr::write].\n\n`mask` must only contain `0` or `!0` values."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_reduce_all",
      "source_api_path": "core::intrinsics::simd::simd_reduce_all",
      "llm_generated_comment": "# Safety\n\n`T` must be a SIMD vector type whose elements are integers.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `T` be a SIMD vector type with integer elements and omits the required element-value condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_reduce_all",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `T` be a SIMD vector type with integer elements and omits the required element-value condition."
        ]
      },
      "original_safety_doc": "`x` must contain only `0` or `!0`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_reduce_any",
      "source_api_path": "core::intrinsics::simd::simd_reduce_any",
      "llm_generated_comment": "# Safety\n\n`T` must be a SIMD vector whose element type is an integer type.",
      "diff": "Incorrect\n1. The generated condition requires an integer-element SIMD vector for `T`, which is unsupported by the original safety requirement.\n2. The generated section omits the requirement that `x` contain only `0` or `!0`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_reduce_any",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires an integer-element SIMD vector for `T`, which is unsupported by the original safety requirement.",
          "The generated section omits the requirement that `x` contain only `0` or `!0`."
        ]
      },
      "original_safety_doc": "`x` must contain only `0` or `!0`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_rem",
      "source_api_path": "core::intrinsics::simd::simd_rem",
      "llm_generated_comment": "# Safety\n\n`T` must be a SIMD vector type whose elements are integers or floating-point numbers. For integer vectors, every lane of `rhs` must be nonzero. For signed integer vectors, no lane may have `lhs` equal to the minimum representable value while the corresponding `rhs` lane is `-1`.",
      "diff": "Incorrect\n1. Generated adds a condition that `T` must be a SIMD vector type with integer or floating-point elements, absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_rem",
        "classification": "Incorrect",
        "items": [
          "Generated adds a condition that `T` must be a SIMD vector type with integer or floating-point elements, absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "For integers, `rhs` must not contain any zero elements.\nAdditionally for signed integers, `<int>::MIN / -1` is undefined behavior."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_scatter",
      "source_api_path": "core::intrinsics::simd::simd_scatter",
      "llm_generated_comment": "# Safety\n\nFor every lane enabled by `mask`, the corresponding pointer in `ptr` must be valid and properly aligned for writing the associated element of `val`, and it must remain so for the duration of the call.",
      "diff": "Missing\n1. It omits the requirement that `mask` contain only `0` or `!0` values.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_scatter",
        "classification": "Missing",
        "items": [
          "It omits the requirement that `mask` contain only `0` or `!0` values."
        ]
      },
      "original_safety_doc": "Each pointer in `ptr` whose corresponding value in `mask` is `!0` must be writable as if by\n[`ptr::write`][crate::ptr::write] (e.g. aligned to the element type).\n\n`mask` must only contain `0` or `!0` values."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_select",
      "source_api_path": "core::intrinsics::simd::simd_select",
      "llm_generated_comment": "# Safety\n\n- `M` must be a SIMD vector whose lane elements are integers, and `T` must be a SIMD vector with the same number of lanes.\n- Every lane of `mask` must be either `0` or all bits set (`!0`), representing false and true respectively.",
      "diff": "Incorrect\n1. GENERATED additionally requires specific SIMD element types and matching lane counts, which ORIGINAL does not state as safety obligations.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_select",
        "classification": "Incorrect",
        "items": [
          "GENERATED additionally requires specific SIMD element types and matching lane counts, which ORIGINAL does not state as safety obligations."
        ]
      },
      "original_safety_doc": "`mask` must only contain `0` and `!0`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_shl",
      "source_api_path": "core::intrinsics::simd::simd_shl",
      "llm_generated_comment": "# Safety\n\nEvery lane of `rhs` must be non-negative and strictly less than the bit width of the corresponding integer lane of `lhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Each element of `rhs` must be in `0..<int>::BITS`."
    },
    {
      "target_api_path": "core::intrinsics::simd::simd_shr",
      "source_api_path": "core::intrinsics::simd::simd_shr",
      "llm_generated_comment": "# Safety\n\n- `T` must be a SIMD vector type with an integer element type.\n- For every lane, the corresponding shift amount in `rhs` must be non-negative and less than the number of bits in the corresponding element of `lhs`.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `T` be a SIMD vector with an integer element type.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::simd::simd_shr",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `T` be a SIMD vector with an integer element type."
        ]
      },
      "original_safety_doc": "Each element of `rhs` must be in `0..<int>::BITS`."
    },
    {
      "target_api_path": "core::intrinsics::size_of_val",
      "source_api_path": "core::intrinsics::size_of_val",
      "llm_generated_comment": "# Safety\n\n- `ptr` must be valid for reads of a value of type `T`. In particular, it must be non-null and properly aligned, and if `T` is unsized, its metadata must be valid for the corresponding dynamically sized value.",
      "diff": "Incorrect\n1. Generated adds pointer validity, non-nullness, alignment, and metadata requirements not stated in the original Safety section.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::size_of_val",
        "classification": "Incorrect",
        "items": [
          "Generated adds pointer validity, non-nullness, alignment, and metadata requirements not stated in the original Safety section."
        ]
      },
      "original_safety_doc": "See [`crate::mem::size_of_val_raw`] for safety conditions."
    },
    {
      "target_api_path": "core::intrinsics::slice_get_unchecked",
      "source_api_path": "core::intrinsics::slice_get_unchecked",
      "llm_generated_comment": "# Safety\n\n`index` must be strictly less than the length encoded in `slice_ptr`; that is, it must identify an in-bounds element of the slice.",
      "diff": "Missing\n1. It omits the requirement that the resulting pointer offset remain in-bounds of the allocation, which must be upheld manually for pointers.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::intrinsics::slice_get_unchecked",
        "classification": "Missing",
        "items": [
          "It omits the requirement that the resulting pointer offset remain in-bounds of the allocation, which must be upheld manually for pointers."
        ]
      },
      "original_safety_doc": "- `index < PtrMetadata(slice_ptr)`, so the indexing is in-bounds for the slice\n- the resulting offsetting is in-bounds of the allocation, which is\n  always the case for references, but needs to be upheld manually for pointers"
    },
    {
      "target_api_path": "core::intrinsics::transmute",
      "source_api_path": "core::intrinsics::transmute",
      "llm_generated_comment": "# Safety\n\nThe bitwise representation of `src` must be a valid value of `Self`, satisfying all language-level and library invariants of `Self`. In particular, every bit required to be initialized by `Self` must originate from initialized data in `src`, and any references, pointers, ownership state, discriminants, or other validity requirements imposed by `Self` must hold for their required lifetimes.",
      "diff": "Incorrect\n1. Generated makes validity of the source representation an unconditional caller obligation, whereas ORIGINAL requires it only when `ASSUME.validity` applies.\n2. Generated adds obligations for initialized bits, pointers, ownership state, and discriminants not specified by ORIGINAL.\n3. Generated omits the conditional alignment obligation for references in the returned value.\n4. Generated omits the conditional lifetime obligation that references in the returned value not outlive their referents.\n5. Generated omits the conditional requirement that uses of a returned value not cause undefined behavior when it violates `Self` library safety invariants.\n6. Generated omits the warning that the trait provides no inherent layout portability or stability guarantee.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::transmute",
        "classification": "Incorrect",
        "items": [
          "Generated makes validity of the source representation an unconditional caller obligation, whereas ORIGINAL requires it only when `ASSUME.validity` applies.",
          "Generated adds obligations for initialized bits, pointers, ownership state, and discriminants not specified by ORIGINAL.",
          "Generated omits the conditional alignment obligation for references in the returned value.",
          "Generated omits the conditional lifetime obligation that references in the returned value not outlive their referents.",
          "Generated omits the conditional requirement that uses of a returned value not cause undefined behavior when it violates `Self` library safety invariants.",
          "Generated omits the warning that the trait provides no inherent layout portability or stability guarantee."
        ]
      },
      "original_safety_doc": "The safety obligations of the caller depend on the value of `ASSUME`:\n- If [`ASSUME.alignment`](Assume::alignment), the caller must guarantee\n  that the addresses of references in the returned `Self` satisfy the\n  alignment requirements of their referent types.\n- If [`ASSUME.lifetimes`](Assume::lifetimes), the caller must guarantee\n  that references in the returned `Self` will not outlive their\n  referents.\n- If [`ASSUME.safety`](Assume::safety), the returned value might not\n  satisfy the library safety invariants of `Self`, and the caller must\n  guarantee that undefined behavior does not arise from uses of the\n  returned value.\n- If [`ASSUME.validity`](Assume::validity), the caller must guarantee\n  that `src` is a bit-valid instance of `Self`.\n\nWhen satisfying the above obligations (if any), the caller must *not*\nassume that this trait provides any inherent guarantee of layout\n[portability](#portability) or [stability](#stability)."
    },
    {
      "target_api_path": "core::intrinsics::typed_swap_nonoverlapping",
      "source_api_path": "core::intrinsics::typed_swap_nonoverlapping",
      "llm_generated_comment": "# Safety\n\nFor non-zero-sized `T`, `x` and `y` must each be non-null, properly aligned, and valid for both reading and writing one `T` for the duration of the call. The `size_of::<T>()`-byte memory ranges beginning at `x` and `y` must not overlap.",
      "diff": "Incorrect\n1. GENERATED adds a non-null requirement only for non-zero-sized `T`, which is unsupported by ORIGINAL.\n2. GENERATED omits that the memory pointed to by `x` and `y` must contain values of type `T`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::typed_swap_nonoverlapping",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds a non-null requirement only for non-zero-sized `T`, which is unsupported by ORIGINAL.",
          "GENERATED omits that the memory pointed to by `x` and `y` must contain values of type `T`."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* Both `x` and `y` must be [valid] for both reads and writes.\n\n* Both `x` and `y` must be properly aligned.\n\n* The region of memory beginning at `x` must *not* overlap with the region of memory\n  beginning at `y`.\n\n* The memory pointed by `x` and `y` must both contain values of type `T`.\n\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::intrinsics::va_arg",
      "source_api_path": "core::intrinsics::va_arg",
      "llm_generated_comment": "# Safety\n\n`ap` must have an unread argument remaining, and its next argument must be compatible with `T` under the applicable C variadic calling convention, including any default argument promotions.",
      "diff": "Incorrect\n1. The generated condition omits the requirement that the next argument contain a properly initialized value of type T.\n2. The generated condition adds compatibility requirements tied to C variadic calling conventions and default promotions, beyond ORIGINAL's ABI-compatibility condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::va_arg",
        "classification": "Incorrect",
        "items": [
          "The generated condition omits the requirement that the next argument contain a properly initialized value of type T.",
          "The generated condition adds compatibility requirements tied to C variadic calling conventions and default promotions, beyond ORIGINAL's ABI-compatibility condition."
        ]
      },
      "original_safety_doc": "This function is only sound to call when:\n\n- there is a next variable argument available.\n- the next argument's type must be ABI-compatible with the type `T`.\n- the next argument must have a properly initialized value of type `T`.\n\nCalling this function with an incompatible type, an invalid value, or when there\nare no more variable arguments, is unsound."
    },
    {
      "target_api_path": "core::intrinsics::va_end",
      "source_api_path": "core::intrinsics::va_end",
      "llm_generated_comment": "# Safety\n\nThis function has no additional safety requirements.",
      "diff": "Incorrect\n1. Generated states that no safety requirements apply, contradicting ORIGINAL's restriction on accessing variable arguments after the call.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::va_end",
        "classification": "Incorrect",
        "items": [
          "Generated states that no safety requirements apply, contradicting ORIGINAL's restriction on accessing variable arguments after the call."
        ]
      },
      "original_safety_doc": "`ap` must not be used to access variable arguments after this call."
    },
    {
      "target_api_path": "core::intrinsics::volatile_copy_nonoverlapping_memory",
      "source_api_path": "core::intrinsics::volatile_copy_nonoverlapping_memory",
      "llm_generated_comment": "# Safety\n\n- `src` must be valid for reads of `count` values of type `T`, and `dst` must be valid for writes of `count` values of type `T`.\n- `src` and `dst` must be properly aligned for `T`.\n- The memory regions occupied by the `count` values starting at `src` and `dst` must not overlap.\n- Even when `count` is zero, `src` and `dst` must be non-null and properly aligned for `T`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::intrinsics::volatile_copy_nonoverlapping_memory",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The safety requirements are consistent with [`copy_nonoverlapping`]\nwhile the read and write behaviors are volatile,\nwhich means it will not be optimized out unless `_count` or `size_of::<T>()` is equal to zero.\n\n[`copy_nonoverlapping`]: ptr::copy_nonoverlapping"
    },
    {
      "target_api_path": "core::intrinsics::volatile_set_memory",
      "source_api_path": "core::intrinsics::volatile_set_memory",
      "llm_generated_comment": "# Safety\n\n- `dst` must be valid for writes of `count * size_of::<T>()` bytes, and must be properly aligned for `T`, for the duration of the operation.",
      "diff": "Incorrect\n1. Generated requires alignment for `T`, an unsupported additional condition relative to the `write_bytes` safety requirements.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::volatile_set_memory",
        "classification": "Incorrect",
        "items": [
          "Generated requires alignment for `T`, an unsupported additional condition relative to the `write_bytes` safety requirements."
        ]
      },
      "original_safety_doc": "The safety requirements are consistent with [`write_bytes`] while the write behavior is volatile,\nwhich means it will not be optimized out unless `_count` or `size_of::<T>()` is equal to zero.\n\n[`write_bytes`]: ptr::write_bytes"
    },
    {
      "target_api_path": "core::intrinsics::vtable_align",
      "source_api_path": "core::intrinsics::vtable_align",
      "llm_generated_comment": "# Safety\n\nThe `ptr` argument must be a valid pointer to a vtable for a trait object. It must remain valid for the duration of the call.",
      "diff": "Incorrect\n1. The generated text additionally requires the pointer to remain valid for the duration of the call, a lifetime condition absent from the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::vtable_align",
        "classification": "Incorrect",
        "items": [
          "The generated text additionally requires the pointer to remain valid for the duration of the call, a lifetime condition absent from the original."
        ]
      },
      "original_safety_doc": "`ptr` must point to a vtable."
    },
    {
      "target_api_path": "core::intrinsics::vtable_size",
      "source_api_path": "core::intrinsics::vtable_size",
      "llm_generated_comment": "# Safety\n\n`ptr` must point to a valid vtable and remain valid and readable for the duration of the call. In particular, it must not be null, dangling, misaligned, or point to arbitrary non-vtable data.",
      "diff": "Incorrect\n1. Generated adds validity, readability for the call duration, and non-null, non-dangling, and alignment requirements beyond the original vtable-pointing condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::intrinsics::vtable_size",
        "classification": "Incorrect",
        "items": [
          "Generated adds validity, readability for the call duration, and non-null, non-dangling, and alignment requirements beyond the original vtable-pointing condition."
        ]
      },
      "original_safety_doc": "`ptr` must point to a vtable."
    },
    {
      "target_api_path": "core::io::borrowed_buf::BorrowedBuf::set_init",
      "source_api_path": "core::io::borrowed_buf::BorrowedBuf::set_init",
      "llm_generated_comment": "# Safety\n\nAll elements in the buffer’s cursor must be initialized.",
      "diff": "Incorrect\n1. Generated limits initialization to the cursor, whereas ORIGINAL requires all buffer elements to be initialized.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::io::borrowed_buf::BorrowedBuf::set_init",
        "classification": "Incorrect",
        "items": [
          "Generated limits initialization to the cursor, whereas ORIGINAL requires all buffer elements to be initialized."
        ]
      },
      "original_safety_doc": "All the elements of the buffer must be initialized."
    },
    {
      "target_api_path": "core::io::borrowed_buf::BorrowedCursor::advance",
      "source_api_path": "core::io::borrowed_buf::BorrowedCursor::advance",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that the first `n` elements of the cursor are initialized before calling this function.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::io::borrowed_buf::BorrowedCursor::advance",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The caller must ensure that the first `n` elements of the cursor have been initialized."
    },
    {
      "target_api_path": "core::io::borrowed_buf::BorrowedCursor::as_mut",
      "source_api_path": "core::io::borrowed_buf::BorrowedCursor::as_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned, non-dangling, and point to an initialized, valid `T` within memory that is writable and remains allocated for the entire lifetime `'a`. The pointed-to value must be exclusively accessible for `'a`, so no other reference or pointer may be used to access it during that lifetime.",
      "diff": "Incorrect\n1. Generated adds unsupported non-null, alignment, validity, writability, allocation, and exclusive-access requirements.\n2. Generated does not state the original prohibition against uninitializing cursor elements when initialized.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::io::borrowed_buf::BorrowedCursor::as_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported non-null, alignment, validity, writability, allocation, and exclusive-access requirements.",
          "Generated does not state the original prohibition against uninitializing cursor elements when initialized."
        ]
      },
      "original_safety_doc": "The caller must not uninitialize any elements of the cursor if it is initialized."
    },
    {
      "target_api_path": "core::io::borrowed_buf::BorrowedCursor::set_init",
      "source_api_path": "core::io::borrowed_buf::BorrowedCursor::set_init",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that all elements of the cursor are initialized.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::io::borrowed_buf::BorrowedCursor::set_init",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "All the elements of the cursor must be initialized."
    },
    {
      "target_api_path": "core::isize::backward_unchecked",
      "source_api_path": "core::isize::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that moving backward by `count` from `start` does not underflow the `u32` range. If the intermediate result is below `0xE000` while `start` is at least `0xE000`, it must also be at least `0x800` so that the additional adjustment cannot underflow. The final adjusted value must be a valid Unicode scalar value representable by `char`.",
      "diff": "Incorrect\n1. The generated section replaces overflow of the `Self` range with underflow of the `u32` range and adds unsupported intermediate-adjustment and Unicode-scalar conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::isize::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated section replaces overflow of the `Self` range with underflow of the `u32` range and adds unsupported intermediate-adjustment and Unicode-scalar conditions."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::isize::forward_unchecked",
      "source_api_path": "core::isize::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` by `count` Unicode scalar values must not exceed `char::MAX`; equivalently, the resulting value after skipping the surrogate range must be a valid `char`.",
      "diff": "Incorrect\n1. Generated replaces the ORIGINAL requirement of no overflow within Self's supported range with an unsupported Unicode scalar value and char::MAX condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::isize::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated replaces the ORIGINAL requirement of no overflow within Self's supported range with an unsupported Unicode scalar value and char::MAX condition."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::isize::unchecked_add",
      "source_api_path": "core::isize::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe mathematical sum of `self` and `rhs` must be representable as an `isize`; it must not overflow or underflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > isize::MAX` or `self + rhs < isize::MIN`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: isize::checked_add\n[`wrapping_add`]: isize::wrapping_add"
    },
    {
      "target_api_path": "core::isize::unchecked_cast",
      "source_api_path": "core::isize::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in `T`; equivalently, `T::checked_cast_from(self)` must return `Some`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::isize::unchecked_cast_from",
      "source_api_path": "core::isize::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable in `isize`; that is, it must lie in the range `isize::MIN..=isize::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::isize::unchecked_div_exact",
      "source_api_path": "core::isize::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be exactly divisible by `rhs` (that is, `self % rhs == 0`).\n- The signed quotient `self / rhs` must be representable as an `isize` (in particular, `self` must not be `isize::MIN` with `rhs == -1`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0`, `self % rhs != 0`, or\n`self == isize::MIN && rhs == -1`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::isize::unchecked_mul",
      "source_api_path": "core::isize::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; their mathematical product must be representable as an `isize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > isize::MAX` or `self * rhs < isize::MIN`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: isize::checked_mul\n[`wrapping_mul`]: isize::wrapping_mul"
    },
    {
      "target_api_path": "core::isize::unchecked_neg",
      "source_api_path": "core::isize::unchecked_neg",
      "llm_generated_comment": "# Safety\n\nThe value of `self` must not be `isize::MIN`, because its negation cannot be represented as an `isize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_neg",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self == isize::MIN`,\ni.e. when [`checked_neg`] would return `None`.\n\n[`checked_neg`]: isize::checked_neg"
    },
    {
      "target_api_path": "core::isize::unchecked_shl",
      "source_api_path": "core::isize::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be less than the number of bits in `isize` (that is, `rhs < isize::BITS`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: isize::checked_shl"
    },
    {
      "target_api_path": "core::isize::unchecked_shl_exact",
      "source_api_path": "core::isize::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n- The shift amount `rhs` must be less than the number of bits in `isize` (`rhs < isize::BITS`).\n- Shifting `self` left by `rhs` bits must not overflow the `isize` range; the resulting value must be representable as an `isize`.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `rhs < isize::BITS`; ORIGINAL specifies only the conjunction involving `leading_zeros()` and `leading_ones()`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_shl_exact",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `rhs < isize::BITS`; ORIGINAL specifies only the conjunction involving `leading_zeros()` and `leading_ones()`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs >= self.leading_zeros() && rhs >=\nself.leading_ones()` i.e. when\n[`isize::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::isize::unchecked_shr",
      "source_api_path": "core::isize::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than [`isize::BITS`].",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: isize::checked_shr"
    },
    {
      "target_api_path": "core::isize::unchecked_shr_exact",
      "source_api_path": "core::isize::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `isize::BITS`, and `self` must have at least `rhs` trailing zero bits; that is, `self.trailing_zeros() >= rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nisize::BITS`\ni.e. when\n[`isize::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::isize::unchecked_sub",
      "source_api_path": "core::isize::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction `self - rhs` must not overflow; its mathematical result must be representable as an `isize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::isize::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs > isize::MAX` or `self - rhs < isize::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: isize::checked_sub\n[`wrapping_sub`]: isize::wrapping_sub"
    },
    {
      "target_api_path": "core::iter::TrustedLen",
      "source_api_path": "core::iter::TrustedLen",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that, for every iterator state, `Iterator::size_hint()` satisfies one of the following:\n\n- If the upper bound is `Some(n)`, the lower bound must also be `n`, and—barring a panic—the iterator must subsequently yield exactly `n` items.\n- If the upper bound is `None`, the iterator must be infinite or must have more than `usize::MAX` items remaining, and the lower bound must be `usize::MAX`.",
      "diff": "Incorrect\n1. Generated omits the ORIGINAL requirement that consumers inspect size_hint()'s upper bound.\n2. Generated adds specific lower-bound, exact-yield, infinity, and item-count requirements not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::iter::TrustedLen",
        "classification": "Incorrect",
        "items": [
          "Generated omits the ORIGINAL requirement that consumers inspect size_hint()'s upper bound.",
          "Generated adds specific lower-bound, exact-yield, infinity, and item-count requirements not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "This trait must only be implemented when the contract is upheld. Consumers\nof this trait must inspect [`Iterator::size_hint()`]’s upper bound."
    },
    {
      "target_api_path": "core::iter::TrustedStep",
      "source_api_path": "core::iter::TrustedStep",
      "llm_generated_comment": "# Safety\n\nFor every pair of values, `Step::steps_between` must report the exact number of forward steps from the start to the end. When that number fits in `usize`, it must return `(n, Some(n))` with the exact value `n`; otherwise, the upper bound must be `None`. This result must remain consistent with the implementation’s forward and backward stepping operations.",
      "diff": "Incorrect\n1. Generated imposes specific `steps_between` exactness and consistency requirements not stated in ORIGINAL, while omitting the obligation to uphold all `Step` method invariants.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::iter::TrustedStep",
        "classification": "Incorrect",
        "items": [
          "Generated imposes specific `steps_between` exactness and consistency requirements not stated in ORIGINAL, while omitting the obligation to uphold all `Step` method invariants."
        ]
      },
      "original_safety_doc": "The implementation of [`Step`] for the given type must guarantee all\ninvariants of all methods are upheld. See the [`Step`] trait's documentation\nfor details. Consumers are free to rely on the invariants in unsafe code."
    },
    {
      "target_api_path": "core::iter::range::Step::backward_unchecked",
      "source_api_path": "core::iter::range::Step::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that moving backward by `count` valid Unicode scalar values from `start` remains within the range of valid `char` values. In particular, the underlying arithmetic must not underflow, and if the movement crosses the surrogate-code-point gap, the additional adjustment must also remain within range so that the returned value is a valid `char`.",
      "diff": "Incorrect\n1. Generated limits movement to valid Unicode scalar values and adds surrogate-gap adjustment requirements absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::iter::range::Step::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated limits movement to valid Unicode scalar values and adds surrogate-gap adjustment requirements absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::iter::range::Step::forward_unchecked",
      "source_api_path": "core::iter::range::Step::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not advance `start` past `char::MAX` when counting only valid Unicode scalar values. Equivalently, the mathematical result of `start as u32 + count`, plus `0x800` when advancing from below `0xD800` across the surrogate range, must be at most `0x10FFFF`.",
      "diff": "Incorrect\n1. Generated restricts the condition to valid Unicode scalar values and adds surrogate-range arithmetic absent from the generic Self overflow requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::iter::range::Step::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts the condition to valid Unicode scalar values and adds surrogate-range arithmetic absent from the generic Self overflow requirement."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::marker::Freeze",
      "source_api_path": "core::marker::Freeze",
      "llm_generated_comment": "# Safety\n\nNo actionable safety preconditions can be inferred from this declaration alone.",
      "diff": "Missing\n1. It omits the obligation that users must not implement `Freeze` for other types.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::marker::Freeze",
        "classification": "Missing",
        "items": [
          "It omits the obligation that users must not implement `Freeze` for other types."
        ]
      },
      "original_safety_doc": "This trait is a core part of the language, it is just expressed as a trait in libcore for\nconvenience. Do *not* implement it for other types."
    },
    {
      "target_api_path": "core::mem::TransmuteFrom",
      "source_api_path": "core::mem::TransmuteFrom",
      "llm_generated_comment": "# Safety\n\nThe caller’s obligations depend on `ASSUME`:\n\n- If `ASSUME.alignment` is set, every reference in the returned `Self` must point to an address properly aligned for its referent type.\n- If `ASSUME.lifetimes` is set, every reference in the returned `Self` must remain within the lifetime of its referent.\n- If `ASSUME.safety` is set, the caller must ensure that every use of the returned `Self` upholds all library safety invariants required by `Self` and cannot cause undefined behavior.\n- If `ASSUME.validity` is set, `src` must have a bit pattern that is valid for `Self`.\n\nThese obligations must be established without assuming that this trait provides any inherent guarantee that the relevant layouts are portable across targets or stable across compiler versions.",
      "diff": "Incorrect\n1. Generated imposes alignment, lifetime, safety-invariant, and validity obligations not stated in ORIGINAL, instead of only requiring the given ASSUMPTIONS to be satisfied.\n2. Generated omits the compiler guarantee of sound union-transmutability from Src to Dst when ASSUMPTIONS are satisfied, including union-transmute padding behavior.\n3. Generated adds an unsupported requirement that callers avoid assuming layout portability or compiler-version stability.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::TransmuteFrom",
        "classification": "Incorrect",
        "items": [
          "Generated imposes alignment, lifetime, safety-invariant, and validity obligations not stated in ORIGINAL, instead of only requiring the given ASSUMPTIONS to be satisfied.",
          "Generated omits the compiler guarantee of sound union-transmutability from Src to Dst when ASSUMPTIONS are satisfied, including union-transmute padding behavior.",
          "Generated adds an unsupported requirement that callers avoid assuming layout portability or compiler-version stability."
        ]
      },
      "original_safety_doc": "If `Dst: TransmuteFrom<Src, ASSUMPTIONS>`, the compiler guarantees that\n`Src` is soundly *union-transmutable* into a value of type `Dst`, provided\nthat the programmer has guaranteed that the given [`ASSUMPTIONS`](Assume)\nare satisfied.\n\nA union-transmute is any bit-reinterpretation conversion in the form of:\n\n```rust\npub unsafe fn transmute_via_union<Src, Dst>(src: Src) -> Dst {\n    use core::mem::ManuallyDrop;\n\n    #[repr(C)]\n    union Transmute<Src, Dst> {\n        src: ManuallyDrop<Src>,\n        dst: ManuallyDrop<Dst>,\n    }\n\n    let transmute = Transmute {\n        src: ManuallyDrop::new(src),\n    };\n\n    let dst = unsafe { transmute.dst };\n\n    ManuallyDrop::into_inner(dst)\n}\n```\n\nNote that this construction is more permissive than\n[`mem::transmute_copy`](super::transmute_copy); union-transmutes permit\nconversions that extend the bits of `Src` with trailing padding to fill\ntrailing uninitialized bytes of `Self`; e.g.:\n\n```rust\n#![feature(transmutability)]\n\nuse core::mem::{Assume, TransmuteFrom};\n\nlet src = 42u8; // size = 1\n\n#[repr(C, align(2))]\nstruct Dst(u8); // size = 2\nlet _ = unsafe {\n    <Dst as TransmuteFrom<u8, { Assume::SAFETY }>>::transmute(src)\n};\n```"
    },
    {
      "target_api_path": "core::mem::align_of_val_raw",
      "source_api_path": "core::mem::align_of_val_raw",
      "llm_generated_comment": "# Safety\n\n- `val` must be a valid raw pointer for `T` and valid for reads: it must be non-null, properly aligned, and point to a valid instance of `T`. If `T` is dynamically sized, its metadata must also be valid for the referenced value.",
      "diff": "Incorrect\n1. Generated requires `val` to be non-null, aligned, and point to a valid readable `T`, but ORIGINAL permits all calls for `Sized` `T`.\n2. Generated requires valid metadata and a valid instance for dynamically sized `T`, but ORIGINAL permits zero-length dynamic tails without those conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::align_of_val_raw",
        "classification": "Incorrect",
        "items": [
          "Generated requires `val` to be non-null, aligned, and point to a valid readable `T`, but ORIGINAL permits all calls for `Sized` `T`.",
          "Generated requires valid metadata and a valid instance for dynamically sized `T`, but ORIGINAL permits zero-length dynamic tails without those conditions."
        ]
      },
      "original_safety_doc": "This function is safe to call if the pointer is safe to reborrow as `&T`\n(in which case you could also call [`align_of_val`]).\nOtherwise, the following conditions must hold:\n\n- If `T` is `Sized`, this function is always safe to call.\n- If the unsized tail of `T` is:\n    - a [slice] `[U]`, `str`, or a [trait object] `dyn Trait`, then the size of the *entire value*\n      (dynamic tail length + statically sized prefix) must fit in `isize`.\n      For the special case where the dynamic tail length is 0, this function\n      is safe to call.\n    - No other kind of unsized tail currently exists that satisfies the trait bounds for this\n      function. If more kinds of unsized tails get introduced in the future, the documentation\n      of this function will have to be extended before it can be used for such types.\n\nHere, *unsized tail* refers to the type obtained by recursively descending through the last\nfield of a tuple or struct until we arrived at a built-in unsized type.\n\nAs a consequence of these rules, it is the case that whenever it is allowed to convert `val`\ninto a shared reference, then it is also allowed to invoke this function.\n\n[trait object]: ../../book/ch17-02-trait-objects.html\n[extern type]: ../../unstable-book/language-features/extern-types.html"
    },
    {
      "target_api_path": "core::mem::alignment::Alignment::new_unchecked",
      "source_api_path": "core::mem::alignment::Alignment::new_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `n` is non-zero.",
      "diff": "Missing\n1. The generated section omits that `align` must be a power of two.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::mem::alignment::Alignment::new_unchecked",
        "classification": "Missing",
        "items": [
          "The generated section omits that `align` must be a power of two."
        ]
      },
      "original_safety_doc": "`align` must be a power of two.\n\nEquivalently, it must be `1 << exp` for some `exp` in `0..usize::BITS`.\nIt must *not* be zero."
    },
    {
      "target_api_path": "core::mem::alignment::Alignment::of_val_raw",
      "source_api_path": "core::mem::alignment::Alignment::of_val_raw",
      "llm_generated_comment": "# Safety\n\nThe metadata of `val` must be valid for `T`. In particular, if `T` has a trait-object tail, the pointer must contain a valid vtable for that trait object; if it has a slice tail, the slice length metadata must be initialized. No validity, alignment, initialization, or dereferenceability requirement is imposed on the pointee itself.",
      "diff": "Missing\n1. It omits that the call is safe when the pointer can safely be reborrowed as `&T`.\n2. It omits that calls for `Sized` `T` are always safe.\n3. It omits that slice-tailed values must fit in `isize`, including the zero-length exception.\n4. It omits that trait-object-tailed values must fit in `isize`.\n5. It omits the extern-type-tail case: always callable, but potentially panicking or returning an incorrect value.\n6. It omits that other unsized tails are conservatively not allowed.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::mem::alignment::Alignment::of_val_raw",
        "classification": "Missing",
        "items": [
          "It omits that the call is safe when the pointer can safely be reborrowed as `&T`.",
          "It omits that calls for `Sized` `T` are always safe.",
          "It omits that slice-tailed values must fit in `isize`, including the zero-length exception.",
          "It omits that trait-object-tailed values must fit in `isize`.",
          "It omits the extern-type-tail case: always callable, but potentially panicking or returning an incorrect value.",
          "It omits that other unsized tails are conservatively not allowed."
        ]
      },
      "original_safety_doc": "This function is safe to call if the pointer is safe to reborrow as `&T`\n(in which case you could also call [`of_val`][Self::of_val]).\nOtherwise, the following conditions must hold:\n\n- If `T` is `Sized`, this function is always safe to call.\n- If the unsized tail of `T` is:\n    - a [slice], then the length of the slice tail must be an initialized\n      integer, and the size of the *entire value*\n      (dynamic tail length + statically sized prefix) must fit in `isize`.\n      For the special case where the dynamic tail length is 0, this function\n      is safe to call.\n    - a [trait object], then the vtable part of the pointer must point\n      to a valid vtable acquired by an unsizing coercion, and the size\n      of the *entire value* (dynamic tail length + statically sized prefix)\n      must fit in `isize`.\n    - an (unstable) [extern type], then this function is always safe to\n      call, but may panic or otherwise return the wrong value, as the\n      extern type's layout is not known. This is the same behavior as\n      [`Alignment::of_val`] on a reference to a type with an extern type tail.\n    - otherwise, it is conservatively not allowed to call this function.\n\n[trait object]: ../../book/ch17-02-trait-objects.html\n[extern type]: ../../unstable-book/language-features/extern-types.html"
    },
    {
      "target_api_path": "core::mem::conjure_zst",
      "source_api_path": "core::mem::conjure_zst",
      "llm_generated_comment": "# Safety\n\n- `T` must be an inhabited type; that is, there must be at least one valid value of `T`.",
      "diff": "Missing\n1. The generated section omits the requirement to use the value only in ways that do not violate the type's safety invariants.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::mem::conjure_zst",
        "classification": "Missing",
        "items": [
          "The generated section omits the requirement to use the value only in ways that do not violate the type's safety invariants."
        ]
      },
      "original_safety_doc": "- `T` must be *[inhabited]*, i.e. possible to construct. This means that types\n  like zero-variant enums and [`!`] are unsound to conjure.\n- You must use the value only in ways which do not violate any *safety*\n  invariants of the type.\n\nWhile it's easy to create a *valid* instance of an inhabited ZST, since having\nno bits in its representation means there's only one possible value, that\ndoesn't mean that it's always *sound* to do so.\n\nFor example, a library could design zero-sized tokens that are `!Default + !Clone`, limiting\ntheir creation to functions that initialize some state or establish a scope. Conjuring such a\ntoken could break invariants and lead to unsoundness."
    },
    {
      "target_api_path": "core::mem::manually_drop::ManuallyDrop::drop",
      "source_api_path": "core::mem::manually_drop::ManuallyDrop::drop",
      "llm_generated_comment": "# Safety\n\nThe value in `slot` must be a valid, initialized `T` that has not already been dropped. After this function returns, the value must not be accessed as a `T` or dropped again.",
      "diff": "Incorrect\n1. The generated section adds an unsupported requirement that `slot` contain an initialized `T`; ORIGINAL requires only a bit-pattern valid for `T` after destruction.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::manually_drop::ManuallyDrop::drop",
        "classification": "Incorrect",
        "items": [
          "The generated section adds an unsupported requirement that `slot` contain an initialized `T`; ORIGINAL requires only a bit-pattern valid for `T` after destruction."
        ]
      },
      "original_safety_doc": "This function runs the destructor of the contained value. Other than changes made by\nthe destructor itself, the memory is left unchanged, and so as far as the compiler is\nconcerned still holds a bit-pattern which is valid for the type `T`.\n\nHowever, this \"zombie\" value should not be exposed to safe code, and this function\nshould not be called more than once. To use a value after it's been dropped, or drop\na value multiple times, can cause Undefined Behavior (depending on what `drop` does).\nThis is normally prevented by the type system, but users of `ManuallyDrop` must\nuphold those guarantees without assistance from the compiler.\n\n[pinned]: crate::pin"
    },
    {
      "target_api_path": "core::mem::manually_drop::ManuallyDrop::take",
      "source_api_path": "core::mem::manually_drop::ManuallyDrop::take",
      "llm_generated_comment": "# Safety\n\n`slot` must contain a live, initialized `T` that has not previously been moved out of or manually dropped.\n\nAfter this call, the value in `slot` must be treated as uninitialized. The caller must not read it, call `ManuallyDrop::drop` or `ManuallyDrop::take` on it, or otherwise access the contained `T` unless `slot` is first reinitialized with a new valid value.",
      "diff": "Incorrect\n1. The generated text permits accessing the container or contained value after reinitialization, whereas ORIGINAL requires ensuring the ManuallyDrop is not used again.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::manually_drop::ManuallyDrop::take",
        "classification": "Incorrect",
        "items": [
          "The generated text permits accessing the container or contained value after reinitialization, whereas ORIGINAL requires ensuring the ManuallyDrop is not used again."
        ]
      },
      "original_safety_doc": "This function semantically moves out the contained value without preventing further usage,\nleaving the state of this container unchanged.\nIt is your responsibility to ensure that this `ManuallyDrop` is not used again."
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::array_assume_init",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::array_assume_init",
      "llm_generated_comment": "# Safety\n\nEvery element of `array` must contain a fully initialized value of type `T` that is valid for `T`.",
      "diff": "Incorrect\n1. GENERATED requires each element to be valid for `T`, adding a condition beyond ORIGINAL's requirement that every element be initialized.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::array_assume_init",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires each element to be valid for `T`, adding a condition beyond ORIGINAL's requirement that every element be initialized."
        ]
      },
      "original_safety_doc": "It is up to the caller to guarantee that all elements of the array are\nin an initialized state."
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init",
      "llm_generated_comment": "# Safety\n\n`self` must contain a fully initialized, valid value of type `T`.",
      "diff": "Incorrect\n1. Generated requires all bytes to be initialized, but ORIGINAL permits some uninitialized bytes when the resulting value is valid for T.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init",
        "classification": "Incorrect",
        "items": [
          "Generated requires all bytes to be initialized, but ORIGINAL permits some uninitialized bytes when the resulting value is valid for T."
        ]
      },
      "original_safety_doc": "It is up to the caller to guarantee that the `MaybeUninit<T>` really is in an initialized\nstate, i.e., a state that is considered [\"valid\" for type `T`][validity]. Calling this when\nthe content is not yet fully initialized causes immediate undefined behavior. The\n[type-level documentation][inv] contains more information about this initialization\ninvariant.\n\nIt is a common mistake to assume that this function is safe to call on integers because they\ncan hold all bit patterns. It is also a common mistake to think that calling this function\nis UB if any byte is uninitialized. Both of these assumptions are wrong. If that is\nsurprising to you, please read the [type-level documentation][inv].\n\n[inv]: #initialization-invariant\n[validity]: ../../reference/behavior-considered-undefined.html#r-undefined.validity\n\nOn top of that, remember that most types have additional invariants beyond merely\nbeing considered initialized at the type level. For example, a `1`-initialized [`Vec<T>`]\nis considered initialized (under the current implementation; this does not constitute\na stable guarantee) because the only requirement the compiler knows about it\nis that the data pointer must be non-null. Creating such a `Vec<T>` does not cause\n*immediate* undefined behavior, but will cause undefined behavior with most\nsafe operations (including dropping it).\n\n[`Vec<T>`]: ../../std/vec/struct.Vec.html"
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_drop",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_drop",
      "llm_generated_comment": "# Safety\n\nThe value (or, for a slice, every element) in `self` must be fully initialized and satisfy all invariants required for a valid `T` at the time of the call. Each value must also be safe to drop and must not have already been dropped.",
      "diff": "Incorrect\n1. Generated additionally requires each value to be safe to drop and not already dropped; ORIGINAL requires initialization and T's invariants only.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_drop",
        "classification": "Incorrect",
        "items": [
          "Generated additionally requires each value to be safe to drop and not already dropped; ORIGINAL requires initialization and T's invariants only."
        ]
      },
      "original_safety_doc": "It is up to the caller to guarantee that the `MaybeUninit<T>` really is\nin an initialized state. Calling this when the content is not yet fully\ninitialized causes undefined behavior.\n\nOn top of that, all additional invariants of the type `T` must be\nsatisfied, as the `Drop` implementation of `T` (or its members) may\nrely on this. For example, setting a `Vec<T>` to an invalid but\nnon-null address makes it initialized (under the current implementation;\nthis does not constitute a stable guarantee), because the only\nrequirement the compiler knows about it is that the data pointer must be\nnon-null. Dropping such a `Vec<T>` however will cause undefined\nbehavior.\n\n[`assume_init`]: MaybeUninit::assume_init"
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_mut",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_mut",
      "llm_generated_comment": "# Safety\n\nThe caller must guarantee that `self` is initialized with a valid value of type `T` (that is, it is in the `value` variant).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_mut",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this when the content is not yet fully initialized causes undefined\nbehavior: it is up to the caller to guarantee that the `MaybeUninit<T>` really\nis in an initialized state. For instance, `.assume_init_mut()` cannot be used to\ninitialize a `MaybeUninit`."
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_read",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_read",
      "llm_generated_comment": "# Safety\n\n- `self` must contain a valid, fully initialized value of type `T`.\n- If `T` is not `Copy`, the value contained in `self` must not subsequently be used or dropped as an initialized `T` after this call, nor may this method be called again on the same initialized value, unless doing so is otherwise valid for `T`; the returned value takes ownership of the value read from `self`.",
      "diff": "Incorrect\n1. Generated forbids subsequently using or dropping the initialized value for non-Copy T, while ORIGINAL requires only that any duplication be valid for the contained data.\n2. Generated limits the duplication condition to non-Copy T, whereas ORIGINAL requires the caller to ensure repeated copies are valid regardless of whether T implements Copy.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_read",
        "classification": "Incorrect",
        "items": [
          "Generated forbids subsequently using or dropping the initialized value for non-Copy T, while ORIGINAL requires only that any duplication be valid for the contained data.",
          "Generated limits the duplication condition to non-Copy T, whereas ORIGINAL requires the caller to ensure repeated copies are valid regardless of whether T implements Copy."
        ]
      },
      "original_safety_doc": "It is up to the caller to guarantee that the `MaybeUninit<T>` really is in an initialized\nstate. Calling this when the content is not yet fully initialized causes undefined\nbehavior. The [type-level documentation][inv] contains more information about\nthis initialization invariant.\n\nMoreover, similar to the [`ptr::read`] function, this function creates a\nbitwise copy of the contents, regardless whether the contained type\nimplements the [`Copy`] trait or not. When using multiple copies of the\ndata (by calling `assume_init_read` multiple times, or first calling\n`assume_init_read` and then [`assume_init`]), it is your responsibility\nto ensure that data may indeed be duplicated.\n\n[inv]: #initialization-invariant\n[`assume_init`]: MaybeUninit::assume_init"
    },
    {
      "target_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_ref",
      "source_api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_ref",
      "llm_generated_comment": "# Safety\n\nEvery element of `self` must be fully initialized and contain a valid value of type `T`.",
      "diff": "Incorrect\n1. Generated changes the subject to every element of `self`, adding an unsupported element-wise condition instead of requiring the `MaybeUninit<T>` content to be initialized.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::maybe_uninit::MaybeUninit::assume_init_ref",
        "classification": "Incorrect",
        "items": [
          "Generated changes the subject to every element of `self`, adding an unsupported element-wise condition instead of requiring the `MaybeUninit<T>` content to be initialized."
        ]
      },
      "original_safety_doc": "Calling this when the content is not yet fully initialized causes undefined\nbehavior: it is up to the caller to guarantee that the `MaybeUninit<T>` really\nis in an initialized state."
    },
    {
      "target_api_path": "core::mem::size_of_val_raw",
      "source_api_path": "core::mem::size_of_val_raw",
      "llm_generated_comment": "# Safety\n\n- If `T` is `Sized`, there are no additional requirements.\n- If the unsized tail of `T` is a slice, the slice-length metadata in `val` must be initialized, and the size of the entire pointee—including any sized prefix—must not exceed `isize::MAX`.\n- If the unsized tail of `T` is a trait object, the vtable metadata in `val` must be a valid vtable for that trait object, obtained through a valid unsizing coercion, and the size of the entire pointee—including any sized prefix—must not exceed `isize::MAX`.\n- If the unsized tail of `T` is an extern type, there are no additional requirements.",
      "diff": "Incorrect\n1. Generated requires initialized slice-length metadata, which is not an ORIGINAL safety condition.\n2. Generated requires a trait-object vtable obtained through a valid unsizing coercion; ORIGINAL imposes no such vtable provenance condition.\n3. Generated states extern tails have no requirements, although ORIGINAL says no such supported tail currently exists and future support requires documentation.\n4. Generated omits ORIGINAL's condition that the pointer is safe to reborrow as `&T`. indeterminate cases are governed by the listed conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::size_of_val_raw",
        "classification": "Incorrect",
        "items": [
          "Generated requires initialized slice-length metadata, which is not an ORIGINAL safety condition.",
          "Generated requires a trait-object vtable obtained through a valid unsizing coercion; ORIGINAL imposes no such vtable provenance condition.",
          "Generated states extern tails have no requirements, although ORIGINAL says no such supported tail currently exists and future support requires documentation.",
          "Generated omits ORIGINAL's condition that the pointer is safe to reborrow as `&T`. indeterminate cases are governed by the listed conditions."
        ]
      },
      "original_safety_doc": "This function is safe to call if the pointer is safe to reborrow as `&T`\n(in which case you could also call [`size_of_val`]).\nOtherwise, the following conditions must hold:\n\n- If `T` is `Sized`, this function is always safe to call.\n- If the *unsized tail* of `T` is:\n    - a [slice] `[U]`, `str`, or a [trait object] `dyn Trait`, then the size of the *entire value*\n      (dynamic tail length + statically sized prefix) must fit in `isize`.\n      For the special case where the dynamic tail length is 0, this function\n      is safe to call.\n    - No other kind of unsized tail currently exists that satisfies the trait bounds for this\n      function. If more kinds of unsized tails get introduced in the future, the documentation\n      of this function will have to be extended before it can be used for such types.\n\nHere, *unsized tail* refers to the type obtained by recursively descending through the last\nfield of a tuple or struct until we arrived at a built-in unsized type.\n\nAs a consequence of these rules, it is the case that whenever it is allowed to convert `val`\ninto a shared reference, then it is also allowed to invoke this function.\n\n[`size_of::<T>()`]: size_of\n[trait object]: ../../book/ch17-02-trait-objects.html\n[extern type]: ../../unstable-book/language-features/extern-types.html"
    },
    {
      "target_api_path": "core::mem::transmutability::TransmuteFrom::transmute",
      "source_api_path": "core::mem::transmutability::TransmuteFrom::transmute",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that reinterpreting the bits of `src` as a value of type `Self` satisfies all language and library invariants of `Self`. In particular, any invariants represented by the trait's `ASSUME` parameter are the caller's responsibility to establish for the resulting value.",
      "diff": "Incorrect\n1. The generated text unconditionally requires all language and library invariants of Self, while ORIGINAL makes safety-invariant obligations conditional on ASSUME.safety.\n2. The generated text makes bit reinterpretation validity unconditional, while ORIGINAL requires bit-validity of src only when ASSUME.validity applies.\n3. The generated text omits the caller's conditional alignment and lifetime guarantees and the absence of inherent portability or stability guarantees.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::transmutability::TransmuteFrom::transmute",
        "classification": "Incorrect",
        "items": [
          "The generated text unconditionally requires all language and library invariants of Self, while ORIGINAL makes safety-invariant obligations conditional on ASSUME.safety.",
          "The generated text makes bit reinterpretation validity unconditional, while ORIGINAL requires bit-validity of src only when ASSUME.validity applies.",
          "The generated text omits the caller's conditional alignment and lifetime guarantees and the absence of inherent portability or stability guarantees."
        ]
      },
      "original_safety_doc": "The safety obligations of the caller depend on the value of `ASSUME`:\n- If [`ASSUME.alignment`](Assume::alignment), the caller must guarantee\n  that the addresses of references in the returned `Self` satisfy the\n  alignment requirements of their referent types.\n- If [`ASSUME.lifetimes`](Assume::lifetimes), the caller must guarantee\n  that references in the returned `Self` will not outlive their\n  referents.\n- If [`ASSUME.safety`](Assume::safety), the returned value might not\n  satisfy the library safety invariants of `Self`, and the caller must\n  guarantee that undefined behavior does not arise from uses of the\n  returned value.\n- If [`ASSUME.validity`](Assume::validity), the caller must guarantee\n  that `src` is a bit-valid instance of `Self`.\n\nWhen satisfying the above obligations (if any), the caller must *not*\nassume that this trait provides any inherent guarantee of layout\n[portability](#portability) or [stability](#stability)."
    },
    {
      "target_api_path": "core::mem::transmute_copy",
      "source_api_path": "core::mem::transmute_copy",
      "llm_generated_comment": "# Safety\n\n- The dynamic size of the value referenced by `src` must be at least `size_of::<Dst>()`.\n- The first `size_of::<Dst>()` bytes of `src` must form a valid, properly initialized representation of `Dst`, including satisfying any validity and lifetime requirements of values contained in `Dst`.\n- Because the bytes are copied without invalidating or moving from `src`, the resulting `Dst` must be able to coexist with `src`; using or dropping either value must not violate aliasing, ownership, provenance, or other safety invariants.",
      "diff": "Incorrect\n1. Generated requires the resulting `Dst` to coexist with `src` without aliasing, ownership, provenance, or other invariant violations; ORIGINAL imposes no such additional condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::transmute_copy",
        "classification": "Incorrect",
        "items": [
          "Generated requires the resulting `Dst` to coexist with `src` without aliasing, ownership, provenance, or other invariant violations; ORIGINAL imposes no such additional condition."
        ]
      },
      "original_safety_doc": "- Requires `size_of_val::<Src>(src) >= size_of::<Dst>()`\n- The first `size_of::<Dst>()` bytes behind `src` must be *readable*\n- The first `size_of::<Dst>()` bytes behind `src` must be *[valid]*\n  when interpreted as a `Dst`.\n\nOn top of that, remember that most types have additional invariants beyond merely\nbeing considered initialized at the type level. For example, a `1`-initialized [`Vec<T>`]\nis considered initialized (under the current implementation; this does not constitute\na stable guarantee) because the only requirement the compiler knows about it\nis that the data pointer must be non-null. Creating such a `Vec<T>` does not cause\n*immediate* undefined behavior, but will cause undefined behavior with most\nsafe operations (including dropping it).\n\n[valid]: ../../reference/behavior-considered-undefined.html#r-undefined.validity\n[`Vec<T>`]: ../../std/vec/struct.Vec.html"
    },
    {
      "target_api_path": "core::mem::transmute_prefix",
      "source_api_path": "core::mem::transmute_prefix",
      "llm_generated_comment": "# Safety\n\nThe first `min(size_of::<Src>(), size_of::<Dst>())` bytes of `src`, followed by uninitialized bytes if `Dst` is larger, must form a valid value of type `Dst`. This includes all initialization, provenance, alignment, aliasing, lifetime, and ownership requirements imposed by `Dst` and any values it contains. Any such requirements that extend beyond this call must remain satisfied for the resulting value’s applicable lifetime.",
      "diff": "Incorrect\n1. GENERATED adds a requirement that safety requirements extending beyond the call remain satisfied for the resulting value's applicable lifetime, which ORIGINAL does not state.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::mem::transmute_prefix",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds a requirement that safety requirements extending beyond the call remain satisfied for the resulting value's applicable lifetime, which ORIGINAL does not state."
        ]
      },
      "original_safety_doc": "If `size_of::<Src>() >= size_of::<Dst>()`, the first `size_of::<Dst>()` bytes\nof `src` must be be *valid* when interpreted as a `Dst`.  (In this case, the\npreconditions are the same as for `transmute_copy(&ManuallyDrop::new(src))`.)\n\nIf `size_of::<Src>() <= size_of::<Dst>()`, the bytes of `src` padded with\nuninitialized bytes afterwards up to a total size of `size_of::<Dst>()`\nmust be *valid* when interpreted as a `Dst`.\n\nIn both cases, any safety preconditions of the `Dst` type must also be upheld."
    },
    {
      "target_api_path": "core::net::ip_addr::Ipv4Addr::backward_unchecked",
      "source_api_path": "core::net::ip_addr::Ipv4Addr::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that stepping backward from `start` by `count` does not underflow or otherwise leave the range of valid `char` values, including the additional backward adjustment over the surrogate range performed when the initial result crosses below `0xE000`. The final resulting value must be a valid Unicode scalar value.",
      "diff": "Incorrect\n1. Generated adds unsupported preconditions about `start`, `count`, valid `char` values, surrogate adjustment, and Unicode scalar validity.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::net::ip_addr::Ipv4Addr::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported preconditions about `start`, `count`, valid `char` values, surrogate adjustment, and Unicode scalar validity."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::net::ip_addr::Ipv4Addr::forward_unchecked",
      "source_api_path": "core::net::ip_addr::Ipv4Addr::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that advancing `start` by `count` does not overflow the range of valid `char` values. In particular, if the initial advancement reaches or passes `0xD800` while `start` is below `0xD800`, the additional advancement by `0x800` performed by this function must also remain within the valid Unicode scalar-value range and must produce a valid `char`.",
      "diff": "Incorrect\n1. GENERATED adds unsupported conditions about `char` values, Unicode scalar validity, and a specific `0xD800` boundary absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::net::ip_addr::Ipv4Addr::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds unsupported conditions about `char` values, Unicode scalar validity, and a specific `0xD800` boundary absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::net::ip_addr::Ipv6Addr::backward_unchecked",
      "source_api_path": "core::net::ip_addr::Ipv6Addr::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that stepping backward from `start` by `count` does not overflow the range representable by `char`. If the initial backward step produces a value below `0xE000` while `start` is at least `0xE000`, the caller must also ensure that stepping backward from that value by `0x800` remains within the valid range of `char`, so that the final result is a valid Unicode scalar value.",
      "diff": "Incorrect\n1. The generated section replaces the original Self range with char and adds unsupported Unicode-scalar and 0xE000/0x800 stepping conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::net::ip_addr::Ipv6Addr::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated section replaces the original Self range with char and adds unsupported Unicode-scalar and 0xE000/0x800 stepping conditions."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::net::ip_addr::Ipv6Addr::forward_unchecked",
      "source_api_path": "core::net::ip_addr::Ipv6Addr::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not advance `start` past `char::MAX` when stepping through Unicode scalar values (excluding the surrogate range `0xD800..=0xDFFF`). Equivalently, with `s = start as u32`:\n\n- if `s < 0xD800`, `count` must be at most `0x10FFFF - s - 0x800`;\n- otherwise, `count` must be at most `0x10FFFF - s`.",
      "diff": "Incorrect\n1. The generated condition applies to Unicode scalar values and char::MAX, whereas the original requires preventing overflow of values supported by Ipv6Addr.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::net::ip_addr::Ipv6Addr::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies to Unicode scalar values and char::MAX, whereas the original requires preventing overflow of values supported by Ipv6Addr."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::num::ZeroablePrimitive",
      "source_api_path": "core::num::ZeroablePrimitive",
      "llm_generated_comment": "# Safety\n\n`NonZeroInner` must have the same layout and representation as `Self` for every nonzero value of `Self`, while treating the zero value of `Self` as an invalid representation (a niche). Thus, every valid `NonZeroInner` value must correspond to a valid, nonzero `Self` value, and every valid nonzero `Self` value must be valid as `NonZeroInner`.",
      "diff": "Incorrect\n1. Generated omits the requirement that implementors be primitives valid when zeroed.\n2. Generated incorrectly treats zero as invalid for `Self`; ORIGINAL requires zeroed `Self` to be valid, with the niche and bit validity belonging to `NonZeroInner`.\n3. Generated replaces the required transmute soundness conditions with an unsupported per-value representation equivalence condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::num::ZeroablePrimitive",
        "classification": "Incorrect",
        "items": [
          "Generated omits the requirement that implementors be primitives valid when zeroed.",
          "Generated incorrectly treats zero as invalid for `Self`; ORIGINAL requires zeroed `Self` to be valid, with the niche and bit validity belonging to `NonZeroInner`.",
          "Generated replaces the required transmute soundness conditions with an unsupported per-value representation equivalence condition."
        ]
      },
      "original_safety_doc": "Types implementing this trait must be primitives that are valid when zeroed.\n\nThe associated `Self::NonZeroInner` type must have the same size+align as `Self`,\nbut with a niche and bit validity making it so the following `transmutes` are sound:\n\n- `Self::NonZeroInner` to `Option<Self::NonZeroInner>`\n- `Option<Self::NonZeroInner>` to `Self`\n\n(And, consequently, `Self::NonZeroInner` to `Self`.)"
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::backward_unchecked",
      "source_api_path": "core::num::nonzero::NonZero::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that moving backward by `count` steps from `start` does not underflow the range of valid `char` values, including the additional `0x800`-value adjustment required when the movement crosses below `0xE000`. The resulting value must be a valid Unicode scalar value.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements about `count`, `start`, `char` values, a `0x800` adjustment, and Unicode scalar validity; ORIGINAL only requires preventing overflow for `Self`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements about `count`, `start`, `char` values, a `0x800` adjustment, and Unicode scalar validity; ORIGINAL only requires preventing overflow for `Self`."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::forward_unchecked",
      "source_api_path": "core::num::nonzero::NonZero::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` by `count` Unicode scalar values, skipping the surrogate range `0xD800..=0xDFFF`, must not go past `char::MAX`.",
      "diff": "Incorrect\n1. The generated condition restricts Unicode-scalar advancement to char::MAX, adding unsupported surrogate-range and parameter-specific requirements instead of the original Self-supported range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition restricts Unicode-scalar advancement to char::MAX, adding unsupported surrogate-range and parameter-specific requirements instead of the original Self-supported range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::from_mut_unchecked",
      "source_api_path": "core::num::nonzero::NonZero::from_mut_unchecked",
      "llm_generated_comment": "# Safety\n\nThe value referenced by `n` must be non-zero.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::from_mut_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The referenced value must not be zero."
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::new_unchecked",
      "source_api_path": "core::num::nonzero::NonZero::new_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `n` is non-zero.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::new_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The value must not be zero."
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::unchecked_add",
      "source_api_path": "core::num::nonzero::NonZero::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe addition of `self.get()` and `other` must not overflow `u128`. Since `self` is nonzero, this also guarantees that the result is nonzero.",
      "diff": "Incorrect\n1. Generated requires avoiding `u128` overflow instead of the original `u8::MAX` boundary and adds an unsupported nonzero-result guarantee.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::unchecked_add",
        "classification": "Incorrect",
        "items": [
          "Generated requires avoiding `u128` overflow instead of the original `u8::MAX` boundary and adds an unsupported nonzero-result guarantee."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u8::MAX`."
    },
    {
      "target_api_path": "core::num::nonzero::NonZero::unchecked_mul",
      "source_api_path": "core::num::nonzero::NonZero::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe product of `self` and `other` must be representable by the underlying integer type; it must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::num::nonzero::NonZero::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u64::MAX`."
    },
    {
      "target_api_path": "core::ops::function::FnPtr::from_ptr",
      "source_api_path": "core::ops::function::FnPtr::from_ptr",
      "llm_generated_comment": "# Safety\n\n`ptr` must be non-null and point to a readable, NUL-terminated sequence of `c_char` values contained within a single allocated object. The first NUL terminator must occur fewer than `isize::MAX` bytes from `ptr`.\n\nThe memory from `ptr` through and including that terminator must remain valid for reads and must not be mutated for the entire lifetime `'a` of the returned `CStr`.",
      "diff": "Incorrect\n1. Generated requires `ptr` to be non-null and reference a readable NUL-terminated `c_char` sequence, unrelated to the required ABI-compatible `FnPtr::as_ptr` provenance.\n2. Generated adds lifetime and mutation restrictions that are absent from the original safety condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::function::FnPtr::from_ptr",
        "classification": "Incorrect",
        "items": [
          "Generated requires `ptr` to be non-null and reference a readable NUL-terminated `c_char` sequence, unrelated to the required ABI-compatible `FnPtr::as_ptr` provenance.",
          "Generated adds lifetime and mutation restrictions that are absent from the original safety condition."
        ]
      },
      "original_safety_doc": "The function pointer must have been obtained\nfrom an [`FnPtr::as_ptr`] call from a function\npointer type that is ABI compatible."
    },
    {
      "target_api_path": "core::ops::range::Bound::get_unchecked",
      "source_api_path": "core::ops::range::Bound::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str` slice, and that slice must remain valid for the resulting pointer to be used.\n- `self.start` and `self.end` must define a range within `slice`: `self.start <= self.end` and `self.end <= slice.len()`.\n- Both `self.start` and `self.end` must lie on UTF-8 character boundaries in `slice`.",
      "diff": "Incorrect\n1. The generated lifetime requirement applies only when the resulting pointer is used, whereas ORIGINAL requires a dangling slice pointer to be invalid even when unused.\n2. The generated UTF-8 character-boundary requirement is an additional condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::Bound::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated lifetime requirement applies only when the resulting pointer is used, whereas ORIGINAL requires a dangling slice pointer to be invalid even when unused.",
          "The generated UTF-8 character-boundary requirement is an additional condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::Bound::get_unchecked_mut",
      "source_api_path": "core::ops::range::Bound::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must be a non-null, properly aligned pointer with correct metadata for a live, initialized `str`; its byte range must belong to a single allocation and contain valid UTF-8.\n\n`self.start` and `self.end` must satisfy `self.start <= self.end <= slice.len()`, and both must lie on UTF-8 code point boundaries of the string.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements that `slice` be non-null, aligned, correctly metadata-bearing, live, initialized, valid UTF-8, and allocation-contained.\n2. Generated adds unsupported UTF-8 code point boundary requirements for `self.start` and `self.end`.\n3. Generated replaces the original dangling-pointer condition with broader pointer validity requirements and omits that dangling `slice` pointers are undefined behavior even when the result is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::Bound::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements that `slice` be non-null, aligned, correctly metadata-bearing, live, initialized, valid UTF-8, and allocation-contained.",
          "Generated adds unsupported UTF-8 code point boundary requirements for `self.start` and `self.end`.",
          "Generated replaces the original dangling-pointer condition with broader pointer validity requirements and omits that dangling `slice` pointers are undefined behavior even when the result is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::Range::get_unchecked",
      "source_api_path": "core::ops::range::Range::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str` slice, and the slice must remain valid for as long as the returned pointer is used.\n- `self.start` and `self.end` must satisfy `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` must each lie on UTF-8 character boundaries within `slice`.",
      "diff": "Incorrect\n1. Generated requires `self.start` and `self.end` to satisfy `self.start <= self.end` and `self.end <= slice.len()`, but ORIGINAL only requires the index be in bounds.\n2. Generated adds UTF-8 character-boundary requirements for `self.start` and `self.end`, which are unsupported by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::Range::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires `self.start` and `self.end` to satisfy `self.start <= self.end` and `self.end <= slice.len()`, but ORIGINAL only requires the index be in bounds.",
          "Generated adds UTF-8 character-boundary requirements for `self.start` and `self.end`, which are unsupported by ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::Range::get_unchecked_mut",
      "source_api_path": "core::ops::range::Range::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must be a valid pointer to a `str` for the duration of the call. The range must satisfy `self.start <= self.end <= (*slice).len()`, and both `self.start` and `self.end` must lie on UTF-8 character boundaries of that string. The allocation must remain valid for any subsequent use of the returned pointer.",
      "diff": "Incorrect\n1. The generated condition requires range endpoints to lie on UTF-8 character boundaries, an additional precondition absent from ORIGINAL.\n2. The generated condition limits allocation validity to subsequent returned-pointer use, omitting ORIGINAL's requirement that a dangling slice pointer is invalid even when the result is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::Range::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires range endpoints to lie on UTF-8 character boundaries, an additional precondition absent from ORIGINAL.",
          "The generated condition limits allocation validity to subsequent returned-pointer use, omitting ORIGINAL's requirement that a dangling slice pointer is invalid even when the result is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeFrom::get_unchecked",
      "source_api_path": "core::ops::range::RangeFrom::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`, and its data must lie within a single allocation. The range `self.start..self.end` must be within that string’s byte range (`self.start <= self.end <= slice.len()`), and both bounds must lie on UTF-8 character boundaries. The allocation must remain valid whenever the returned pointer is dereferenced.",
      "diff": "Incorrect\n1. Generated adds valid-str and single-allocation requirements absent from the original.\n2. Generated adds range-bound and UTF-8-boundary requirements absent from the original.\n3. Generated adds an allocation-lifetime requirement for dereferencing the returned pointer absent from the original.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeFrom::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds valid-str and single-allocation requirements absent from the original.",
          "Generated adds range-bound and UTF-8-boundary requirements absent from the original.",
          "Generated adds an allocation-lifetime requirement for dereferencing the returned pointer absent from the original."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeFrom::get_unchecked_mut",
      "source_api_path": "core::ops::range::RangeFrom::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`, and the indexed range must be contained within that string. The range’s start and end must lie on UTF-8 code point boundaries. These requirements apply even if the returned pointer is never dereferenced.",
      "diff": "Incorrect\n1. The generated requirement that the slice point to a valid `str` adds a condition beyond ORIGINAL's specific prohibition on a dangling pointer.\n2. The generated requirement that range endpoints lie on UTF-8 code point boundaries is unsupported by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeFrom::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated requirement that the slice point to a valid `str` adds a condition beyond ORIGINAL's specific prohibition on a dangling pointer.",
          "The generated requirement that range endpoints lie on UTF-8 code point boundaries is unsupported by ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeFull::get_unchecked",
      "source_api_path": "core::ops::range::RangeFull::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The indexed range must satisfy `start <= end <= slice.len()`, and both bounds must lie on UTF-8 character boundaries. The allocation backing `slice` must remain valid for any later use of the returned pointer.",
      "diff": "Incorrect\n1. Generated requires `slice` to point to a valid `str`, while ORIGINAL only requires avoiding a dangling `slice` pointer.\n2. Generated adds UTF-8 boundary, range-bound, and future-lifetime requirements unsupported by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeFull::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires `slice` to point to a valid `str`, while ORIGINAL only requires avoiding a dangling `slice` pointer.",
          "Generated adds UTF-8 boundary, range-bound, and future-lifetime requirements unsupported by ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeFull::get_unchecked_mut",
      "source_api_path": "core::ops::range::RangeFull::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid raw pointer to a `str`: its data pointer and length metadata must describe an allocated byte range containing valid UTF-8.\n- `self.start` and `self.end` must satisfy `self.start <= self.end <= slice.len()`.\n- Both `self.start` and `self.end` must lie on UTF-8 character boundaries within `slice`.",
      "diff": "Incorrect\n1. Generated adds UTF-8 validity and character-boundary requirements unsupported by the original safety conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeFull::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds UTF-8 validity and character-boundary requirements unsupported by the original safety conditions."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeInclusive::get_unchecked",
      "source_api_path": "core::ops::range::RangeInclusive::get_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `slice` is a valid pointer to a `str` slice and that `self` denotes a range within that slice: `self.start <= self.end` and `self.end <= slice.len()`. Both endpoints of the range must lie on UTF-8 character boundaries. The underlying string data must remain valid for as long as the returned pointer is used.",
      "diff": "Incorrect\n1. Generated adds a UTF-8 character-boundary requirement that is not stated in the original safety conditions.\n2. Generated limits the underlying data validity requirement to the returned pointer's use, whereas the original prohibits a dangling slice pointer even when the pointer is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeInclusive::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds a UTF-8 character-boundary requirement that is not stated in the original safety conditions.",
          "Generated limits the underlying data validity requirement to the returned pointer's use, whereas the original prohibits a dangling slice pointer even when the pointer is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeInclusive::get_unchecked_mut",
      "source_api_path": "core::ops::range::RangeInclusive::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid, properly aligned, non-null pointer to a `str` slice, and the pointed-to string data must remain valid and writable for as long as the returned pointer is used.\n- The range's start and end positions must lie on UTF-8 character boundaries of `slice`, so that the returned subslice is itself a valid `str`.\n- While the returned pointer is used, the caller must uphold Rust's aliasing rules for mutable references: no other access may conflict with the mutable subslice it points to.",
      "diff": "Incorrect\n1. The generated conditions require alignment, non-nullness, a `str` slice, validity, and writability beyond ORIGINAL's dangling-pointer and out-of-bounds conditions.\n2. The generated UTF-8 character-boundary requirement adds a condition absent from ORIGINAL.\n3. The generated aliasing and returned-pointer lifetime requirements add conditions absent from ORIGINAL.\n4. The generated text omits ORIGINAL's explicit condition that an out-of-bounds index is undefined behavior even when the resulting pointer is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeInclusive::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated conditions require alignment, non-nullness, a `str` slice, validity, and writability beyond ORIGINAL's dangling-pointer and out-of-bounds conditions.",
          "The generated UTF-8 character-boundary requirement adds a condition absent from ORIGINAL.",
          "The generated aliasing and returned-pointer lifetime requirements add conditions absent from ORIGINAL.",
          "The generated text omits ORIGINAL's explicit condition that an out-of-bounds index is undefined behavior even when the resulting pointer is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeTo::get_unchecked",
      "source_api_path": "core::ops::range::RangeTo::get_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `slice` is a valid, non-null pointer to a `str` slice whose underlying bytes remain valid for the duration of any use of the returned pointer.\n\nThe range must begin and end on UTF-8 character boundaries within `slice`. The function checks that the range is ordered and within the slice's byte length; if that check fails, the function does not produce a usable pointer.",
      "diff": "Incorrect\n1. Generated adds a valid, non-null `str` pointer and byte-validity lifetime requirement absent from ORIGINAL.\n2. Generated claims the function checks range ordering and bounds, contrary to ORIGINAL's unconditional undefined behavior for out-of-bounds indices.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeTo::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds a valid, non-null `str` pointer and byte-validity lifetime requirement absent from ORIGINAL.",
          "Generated claims the function checks range ordering and bounds, contrary to ORIGINAL's unconditional undefined behavior for out-of-bounds indices."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeTo::get_unchecked_mut",
      "source_api_path": "core::ops::range::RangeTo::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid, properly aligned `str` slice backed by writable memory, and that slice must remain valid and exclusively accessible for as long as the returned pointer is used.\n- `self.start` and `self.end` must designate a range within `slice`: `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` must both be UTF-8 character boundaries in `slice`, so that the selected range is itself a valid `str` slice.",
      "diff": "Incorrect\n1. GENERATED requires a valid, aligned, writable `str` and exclusive validity over the returned pointer's use, adding unsupported memory and lifetime obligations.\n2. GENERATED requires UTF-8 character boundaries, an unsupported condition absent from ORIGINAL.\n3. GENERATED changes the range requirement to `self.start <= self.end <= slice.len()`, while ORIGINAL only requires an out-of-bounds index to be rejected.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeTo::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires a valid, aligned, writable `str` and exclusive validity over the returned pointer's use, adding unsupported memory and lifetime obligations.",
          "GENERATED requires UTF-8 character boundaries, an unsupported condition absent from ORIGINAL.",
          "GENERATED changes the range requirement to `self.start <= self.end <= slice.len()`, while ORIGINAL only requires an out-of-bounds index to be rejected."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeToInclusive::get_unchecked",
      "source_api_path": "core::ops::range::RangeToInclusive::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must represent a valid `str` allocation with valid pointer metadata and provenance. The byte range represented by `self` must be nonempty-end-convertible without overflow, lie entirely within that string, and both its start and exclusive end must be UTF-8 character boundaries. In particular, the inclusive endpoint must be less than the string’s byte length, and the byte immediately after it must be a character boundary.\n\nThe backing allocation must remain valid, and its contents must remain valid UTF-8, whenever the returned pointer is dereferenced.",
      "diff": "Incorrect\n1. Generated makes dangling-pointer validity conditional on dereferencing the returned pointer, but ORIGINAL requires it at call time even when unused.\n2. Generated adds unsupported requirements for valid str metadata, provenance, UTF-8 boundaries, and endpoint conversion.\n3. Generated requires the inclusive endpoint and following byte to satisfy specific bounds, beyond ORIGINAL's general out-of-bounds-index condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeToInclusive::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated makes dangling-pointer validity conditional on dereferencing the returned pointer, but ORIGINAL requires it at call time even when unused.",
          "Generated adds unsupported requirements for valid str metadata, provenance, UTF-8 boundaries, and endpoint conversion.",
          "Generated requires the inclusive endpoint and following byte to satisfy specific bounds, beyond ORIGINAL's general out-of-bounds-index condition."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ops::range::RangeToInclusive::get_unchecked_mut",
      "source_api_path": "core::ops::range::RangeToInclusive::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The inclusive range `..=self.end` must be within that string, and the byte immediately after `self.end` must lie on a UTF-8 character boundary.",
      "diff": "Incorrect\n1. Generated requires `slice` to point to a valid `str`, while ORIGINAL only prohibits a dangling pointer.\n2. Generated requires the inclusive range and following byte to satisfy UTF-8 boundary conditions, adding predicates absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ops::range::RangeToInclusive::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires `slice` to point to a valid `str`, while ORIGINAL only prohibits a dangling pointer.",
          "Generated requires the inclusive range and following byte to satisfy UTF-8 boundary conditions, adding predicates absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::option::Option::unwrap_unchecked",
      "source_api_path": "core::option::Option::unwrap_unchecked",
      "llm_generated_comment": "# Safety\n\n`self` must be `Some`. Calling this method on `None` results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::option::Option::unwrap_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this method on [`None`] is *[undefined behavior]*.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::pin::Pin::get_unchecked_mut",
      "source_api_path": "core::pin::Pin::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid pointer to a `str` slice whose data is valid for reads and mutable access for the duration of any use of the returned pointer.\n- `self.start` must be less than or equal to `self.end`, and `self.end` must be less than or equal to the length of the string slice pointed to by `slice`.\n- The string slice must remain allocated and must not be accessed in a way that violates Rust's aliasing rules while the returned pointer is in use.",
      "diff": "Incorrect\n1. The generated section describes a `slice` pointer and string bounds instead of requiring that the caller never move data out of the returned mutable reference.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::Pin::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated section describes a `slice` pointer and string bounds instead of requiring that the caller never move data out of the returned mutable reference."
        ]
      },
      "original_safety_doc": "This function is unsafe. You must guarantee that you will never move\nthe data out of the mutable reference you receive when you call this\nfunction, so that the invariants on the `Pin` type can be upheld.\n\nIf the underlying data is `Unpin`, `Pin::get_mut` should be used\ninstead."
    },
    {
      "target_api_path": "core::pin::Pin::into_inner_unchecked",
      "source_api_path": "core::pin::Pin::into_inner_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must guarantee that the data pointed to by the returned pointer is not moved out of its current location or otherwise invalidated until it is dropped.",
      "diff": "Incorrect\n1. GENERATED replaces the obligation to treat the pointer as pinned with preventing movement or invalidation, and shortens the required lifetime from drop-handler completion to being dropped.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::Pin::into_inner_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED replaces the obligation to treat the pointer as pinned with preventing movement or invalidation, and shortens the required lifetime from drop-handler completion to being dropped."
        ]
      },
      "original_safety_doc": "This function is unsafe. You must guarantee that you will continue to\ntreat the pointer `Ptr` as pinned after you call this function, so that\nthe invariants on the `Pin` type can be upheld. If the code using the\nresulting `Ptr` does not continue to maintain the pinning invariants that\nis a violation of the API contract and may lead to undefined behavior in\nlater (safe) operations.\n\nNote that you must be able to guarantee that the data pointed to by `Ptr`\nwill be treated as pinned all the way until its `drop` handler is complete!\n\n*For more information, see the [`pin` module docs][self]*\n\nIf the underlying data is [`Unpin`], [`Pin::into_inner`] should be used\ninstead."
    },
    {
      "target_api_path": "core::pin::Pin::map_unchecked",
      "source_api_path": "core::pin::Pin::map_unchecked",
      "llm_generated_comment": "# Safety\n\n`func` must return a reference to data that will remain pinned: the referenced `U` must not be moved out of or otherwise invalidated for as long as the original `T` remains pinned.",
      "diff": "Incorrect\n1. The generated condition requires the referenced data remain valid, while ORIGINAL requires it not move while the argument remains unmoved and forbids moving out of the argument.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::Pin::map_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires the referenced data remain valid, while ORIGINAL requires it not move while the argument remains unmoved and forbids moving out of the argument."
        ]
      },
      "original_safety_doc": "This function is unsafe. You must guarantee that the data you return\nwill not move so long as the argument value does not move (for example,\nbecause it is one of the fields of that value), and also that you do\nnot move out of the argument you receive to the interior function.\n\n[`pin` module]: self#projections-and-structural-pinning"
    },
    {
      "target_api_path": "core::pin::Pin::map_unchecked_mut",
      "source_api_path": "core::pin::Pin::map_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`func` must not move out of, replace, or otherwise invalidate the pinned `T` through the `&mut T` it receives.\n\nThe `U` referenced by the returned `&mut U` must remain at a stable address for the entire lifetime of the returned `Pin`. In particular, it must not be possible for that `U` to move independently while the original `T` remains pinned.",
      "diff": "Incorrect\n1. Generated additionally prohibits replacing or otherwise invalidating the pinned `T`, while ORIGINAL only prohibits moving out of the argument.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::Pin::map_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated additionally prohibits replacing or otherwise invalidating the pinned `T`, while ORIGINAL only prohibits moving out of the argument."
        ]
      },
      "original_safety_doc": "This function is unsafe. You must guarantee that the data you return\nwill not move so long as the argument value does not move (for example,\nbecause it is one of the fields of that value), and also that you do\nnot move out of the argument you receive to the interior function.\n\n[`pin` module]: self#projections-and-structural-pinning"
    },
    {
      "target_api_path": "core::pin::Pin::new_unchecked",
      "source_api_path": "core::pin::Pin::new_unchecked",
      "llm_generated_comment": "# Safety\n\nThe value dereferenced by `n` must remain valid at the same memory location and must not be moved or have its storage invalidated until it is dropped.\n\nAny `Deref` or `DerefMut` implementation used by `T` must continue to refer to that pinned value and must not move it out of its storage. The caller must also ensure that no unpinned mutable reference or other operation can subsequently be used to move the value before it is dropped.",
      "diff": "Incorrect\n1. The generated condition requires the value to remain pinned until dropped without ORIGINAL's exception for types implementing `Unpin`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::Pin::new_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires the value to remain pinned until dropped without ORIGINAL's exception for types implementing `Unpin`."
        ]
      },
      "original_safety_doc": "This constructor is unsafe because we cannot guarantee that the data\npointed to by `pointer` is pinned. At its core, pinning a value means making the\nguarantee that the value's data will not be moved nor have its storage invalidated until\nit gets dropped. For a more thorough explanation of pinning, see the [`pin` module docs].\n\nIf the caller that is constructing this `Pin<Ptr>` does not ensure that the data `Ptr`\npoints to is pinned, that is a violation of the API contract and may lead to undefined\nbehavior in later (even safe) operations.\n\nBy using this method, you are also making a promise about several trait\nimplementations of `Ptr` itself, if they exist. Most importantly, they\nmust not move out of their `self` arguments: `Pin::as_mut` and\n`Pin::as_ref` will call `DerefMut::deref_mut` and `Deref::deref` *on the\npointer type `Ptr`* and expect these methods to uphold the pinning\ninvariants. These requirements are specified in more detail on the\n[`PinSafePointer`] trait, and `Ptr` must abide by the safety\nrequirements of that trait.\n\nMoreover, by calling this method you promise that the reference `Ptr`\ndereferences to will not be moved out of again; in particular, it\nmust not be possible to obtain a `&mut Ptr::Target` and then\nmove out of that reference (using, for example [`mem::swap`]).\n\nFor example, calling `Pin::new_unchecked` on an `&'a mut T` is unsafe because\nwhile you are able to pin it for the given lifetime `'a`, you have no control\nover whether it is kept pinned once `'a` ends, and therefore cannot uphold the\nguarantee that a value, once pinned, remains pinned until it is dropped:\n\n```\nuse std::mem;\nuse std::pin::Pin;\n\nfn move_pinned_ref<T>(mut a: T, mut b: T) {\n    unsafe {\n        let p: Pin<&mut T> = Pin::new_unchecked(&mut a);\n        // This should mean the pointee `a` can never move again.\n    }\n    mem::swap(&mut a, &mut b); // Potential UB down the road ⚠️\n    // The address of `a` changed to `b`'s stack slot, so `a` got moved even\n    // though we have previously pinned it! We have violated the pinning API contract.\n}\n```\nA value, once pinned, must remain pinned until it is dropped (unless its type implements\n`Unpin`). Because `Pin<&mut T>` does not own the value, dropping the `Pin` will not drop\nthe value and will not end the pinning contract. So moving the value after dropping the\n`Pin<&mut T>` is still a violation of the API contract.\n\nSimilarly, calling `Pin::new_unchecked` on an `Rc<T>` is unsafe because there could be\naliases to the same data that are not subject to the pinning restrictions:\n```\nuse std::rc::Rc;\nuse std::pin::Pin;\n\nfn move_pinned_rc<T>(mut x: Rc<T>) {\n    // This should mean the pointee can never move again.\n    let pin = unsafe { Pin::new_unchecked(Rc::clone(&x)) };\n    {\n        let p: Pin<&T> = pin.as_ref();\n        // ...\n    }\n    drop(pin);\n\n    let content = Rc::get_mut(&mut x).unwrap(); // Potential UB down the road ⚠️\n    // Now, if `x` was the only reference, we have a mutable reference to\n    // data that we pinned above, which we could use to move it as we have\n    // seen in the previous example. We have violated the pinning API contract.\n}\n```"
    },
    {
      "target_api_path": "core::pin::PinSafePointer",
      "source_api_path": "core::pin::PinSafePointer",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that moving, copying, or dropping a value of the implementing pointer type does not move its `Deref::Target`, and that no safe operation on the pointer can move the target out of the pointer while it is pinned. The target must remain at a stable location for as long as it is accessed through `Pin`.",
      "diff": "Incorrect\n1. Generated adds obligations about moving, copying, dropping, and extracting the target that are not stated in ORIGINAL.\n2. Generated omits the broader requirement prohibiting malicious implementations of any safe traits used by Pin.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pin::PinSafePointer",
        "classification": "Incorrect",
        "items": [
          "Generated adds obligations about moving, copying, dropping, and extracting the target that are not stated in ORIGINAL.",
          "Generated omits the broader requirement prohibiting malicious implementations of any safe traits used by Pin."
        ]
      },
      "original_safety_doc": "Types that implement this trait must not provide \"malicious\" implementations\nof any safe traits used by [`Pin`]."
    },
    {
      "target_api_path": "core::pointer::add",
      "source_api_path": "core::pointer::add",
      "llm_generated_comment": "# Safety\n\n- `self` must be a pointer into an allocated object (or one past its end), and the pointer computed by advancing it by `count` elements must remain within that same allocated object or be one past its end.\n- The byte offset, `count * size_of::<T>()`, must fit in an `isize`, and adding that byte offset to the address of `self` must not overflow.\n- If `T` is zero-sized, `self` must be non-null and `count` must not exceed `isize::MAX`.",
      "diff": "Incorrect\n1. The generated text requires `self` to point into an allocation unconditionally, whereas ORIGINAL requires provenance only when the computed offset is non-zero.\n2. The generated text requires non-null `self` for zero-sized `T`, but ORIGINAL imposes no non-null requirement.\n3. The generated text requires `count <= isize::MAX` for zero-sized `T`, but ORIGINAL only constrains the zero byte offset.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::add",
        "classification": "Incorrect",
        "items": [
          "The generated text requires `self` to point into an allocation unconditionally, whereas ORIGINAL requires provenance only when the computed offset is non-zero.",
          "The generated text requires non-null `self` for zero-sized `T`, but ORIGINAL imposes no non-null requirement.",
          "The generated text requires `count <= isize::MAX` for zero-sized `T`, but ORIGINAL only constrains the zero byte offset."
        ]
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n\"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() + count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `self.addr()..result`) must be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two. This implies, for\ninstance, that `vec.as_ptr().add(vec.len())` (for `vec: Vec<T>`) is always safe.\n\n[allocation]: crate::ptr#allocation\n\nConsider using [`wrapping_add`](#method.wrapping_add) instead if these constraints are\ndifficult to satisfy. The only advantage of this method is that it\nenables more aggressive compiler optimizations."
    },
    {
      "target_api_path": "core::pointer::as_mut",
      "source_api_path": "core::pointer::as_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned, have valid provenance, and point to a properly initialized value of type `T` that is valid for reads and writes for the entire lifetime `'a`. The pointee must also be exclusively accessible for `'a`, as required for a mutable reference; no other references or accesses that conflict with the returned `&mut T` may occur during that lifetime. For dynamically sized `T`, the pointer metadata and the entire referenced pointee must be valid.",
      "diff": "Incorrect\n1. Generated adds alignment and provenance requirements not stated in ORIGINAL's null-or-convertible condition.\n2. Generated requires validity for reads and writes throughout 'a, strengthening ORIGINAL's requirement without that lifetime boundary.\n3. Generated adds exclusive-access and conflicting-access prohibitions for 'a, which are not stated in ORIGINAL.\n4. Generated adds dynamically sized pointer-metadata and entire-pointee requirements absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds alignment and provenance requirements not stated in ORIGINAL's null-or-convertible condition.",
          "Generated requires validity for reads and writes throughout 'a, strengthening ORIGINAL's requirement without that lifetime boundary.",
          "Generated adds exclusive-access and conflicting-access prohibitions for 'a, which are not stated in ORIGINAL.",
          "Generated adds dynamically sized pointer-metadata and entire-pointee requirements absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either*\nthe pointer is null *or*\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::pointer::as_mut_unchecked",
      "source_api_path": "core::pointer::as_mut_unchecked",
      "llm_generated_comment": "# Safety\n\nFor the duration of the returned reference, the caller must ensure that the contents of this `UnsafeCell` are not accessed through any other reference or pointer in a way that conflicts with the exclusive access represented by the returned `&mut T`.",
      "diff": "Incorrect\n1. Generated adds an unsupported UnsafeCell aliasing condition beyond ORIGINAL's requirement that the pointer be convertible to a reference.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_mut_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported UnsafeCell aliasing condition beyond ORIGINAL's requirement that the pointer be convertible to a reference."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::pointer::as_ref",
      "source_api_path": "core::pointer::as_ref",
      "llm_generated_comment": "# Safety\n\n`self` must point to a properly aligned, initialized, and valid value of type `T`, with valid metadata and provenance. The pointed-to value must remain allocated and valid for reads for the entire lifetime `'a`.\n\nFor the duration of `'a`, the value must not be mutated or accessed through an incompatible mutable reference, except for mutations performed through `UnsafeCell`.",
      "diff": "Incorrect\n1. The generated condition requires `self` to point to a valid value, eliminating the original allowance that the pointer may be null.\n2. The generated text adds unsupported requirements for allocation, readable validity throughout `'a`, and exclusive access or mutation restrictions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_ref",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires `self` to point to a valid value, eliminating the original allowance that the pointer may be null.",
          "The generated text adds unsupported requirements for allocation, readable validity throughout `'a`, and exclusive access or mutation restrictions."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either* the pointer is null *or*\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::pointer::as_ref_unchecked",
      "source_api_path": "core::pointer::as_ref_unchecked",
      "llm_generated_comment": "# Safety\n\nFor the entire lifetime of the returned reference:\n\n- The value contained in this `UnsafeCell` must be initialized and valid for `T`.\n- It must not be mutated or accessed through any reference or pointer in a way that conflicts with the returned shared reference. Mutation through `UnsafeCell` fields nested within `T` is permitted.",
      "diff": "Incorrect\n1. The generated section adds unsupported requirements involving an UnsafeCell, initialization, validity for T, and conflicting access for the returned reference.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_ref_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated section adds unsupported requirements involving an UnsafeCell, initialization, validity for T, and conflicting access for the returned reference."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::pointer::as_uninit_mut",
      "source_api_path": "core::pointer::as_uninit_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned, have valid provenance, and be valid for reads and writes of `size_of::<T>()` bytes. The pointed-to allocation must remain live for the entire lifetime `'a`.\n\nFor the duration of `'a`, the memory referenced by the returned `&mut MaybeUninit<T>` must not be accessed through any other pointer or reference except through pointers derived from that reference. The memory need not contain an initialized `T`.",
      "diff": "Incorrect\n1. Generated requires the allocation to remain live for the entire lifetime 'a, while ORIGINAL only requires pointer-to-reference convertibility without that explicit lifetime condition.\n2. Generated imposes exclusive-access and derived-pointer restrictions for 'a that are not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_uninit_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires the allocation to remain live for the entire lifetime 'a, while ORIGINAL only requires pointer-to-reference convertibility without that explicit lifetime condition.",
          "Generated imposes exclusive-access and derived-pointer restrictions for 'a that are not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either* the pointer is null *or*\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion).\nNote that because the created reference is to `MaybeUninit<T>`, the\nsource pointer can point to uninitialized memory."
    },
    {
      "target_api_path": "core::pointer::as_uninit_ref",
      "source_api_path": "core::pointer::as_uninit_ref",
      "llm_generated_comment": "# Safety\n\n- If `self` is non-null, it must be properly aligned and point to a live `T`-sized allocation that is valid for reads for the entire lifetime `'a`.\n- The memory referenced by the returned `&MaybeUninit<T>` must not be mutated for the duration of `'a`, except through an `UnsafeCell`.",
      "diff": "Incorrect\n1. It requires the allocation to be valid for reads for the entire lifetime, contradicting ORIGINAL's explicit allowance for pointers to uninitialized memory.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_uninit_ref",
        "classification": "Incorrect",
        "items": [
          "It requires the allocation to be valid for reads for the entire lifetime, contradicting ORIGINAL's explicit allowance for pointers to uninitialized memory."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either* the pointer is null *or*\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion).\nNote that because the created reference is to `MaybeUninit<T>`, the\nsource pointer can point to uninitialized memory."
    },
    {
      "target_api_path": "core::pointer::as_uninit_slice",
      "source_api_path": "core::pointer::as_uninit_slice",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, the caller must ensure that:\n\n- Its data pointer is properly aligned for `T`.\n- The pointer denotes a single allocation containing at least `self.len()` contiguous elements’ worth of memory. The memory need not be initialized.\n- The total size of the slice, `self.len() * size_of::<T>()`, does not exceed `isize::MAX` and does not overflow `usize`.\n- The memory is valid for reads for the entire lifetime `'a`.\n- For the duration of `'a`, the referenced memory is not mutated except through an `UnsafeCell`.\n\nFor a zero-length slice, the data pointer must still be non-null and properly aligned, but it need not point to allocated memory.",
      "diff": "Incorrect\n1. Generated requires the memory to be valid for reads for the entire lifetime 'a; ORIGINAL requires validity for the slice's byte range, without this lifetime condition.\n2. Generated says a zero-length data pointer need not point to allocated memory, while ORIGINAL requires the pointer to be valid and does not permit this additional condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_uninit_slice",
        "classification": "Incorrect",
        "items": [
          "Generated requires the memory to be valid for reads for the entire lifetime 'a; ORIGINAL requires validity for the slice's byte range, without this lifetime condition.",
          "Generated says a zero-length data pointer need not point to allocated memory, while ORIGINAL requires the pointer to be valid and does not permit this additional condition."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either* the pointer is null *or*\nall of the following are true:\n\n* The pointer must be [valid] for reads for `ptr.len() * size_of::<T>()` many bytes,\n  and it must be properly aligned. This means in particular:\n\n* The entire memory range of this slice must be contained within a single [allocation]!\n  Slices can never span across multiple allocations.\n\n* The pointer must be aligned even for zero-length slices. One\n  reason for this is that enum layout optimizations may rely on references\n  (including slices of any length) being aligned and non-null to distinguish\n  them from other data. You can obtain a pointer that is usable as `data`\n  for zero-length slices using [`NonNull::dangling()`].\n\n* The total size `ptr.len() * size_of::<T>()` of the slice must be no larger than `isize::MAX`.\n  See the safety documentation of [`pointer::offset`].\n\n* You must enforce Rust's aliasing rules, since the returned lifetime `'a` is\n  arbitrarily chosen and does not necessarily reflect the actual lifetime of the data.\n  In particular, while this reference exists, the memory the pointer points to must\n  not get mutated (except inside `UnsafeCell`).\n\nThis applies even if the result of this method is unused!\n\nSee also [`slice::from_raw_parts`][].\n\n[valid]: crate::ptr#safety\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::pointer::as_uninit_slice_mut",
      "source_api_path": "core::pointer::as_uninit_slice_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned for `MaybeUninit<T>` and point to a contiguous range of `self.len()` elements within a single allocated object. The entire range must be valid for reads and writes as `MaybeUninit<T>` values, and its total size must not exceed `isize::MAX`.\n\nThe pointed-to memory must remain allocated and accessible for the lifetime `'a`, and no other pointer may be used to access it for the duration of the returned mutable slice except through that slice. The elements do not need to contain initialized `T` values.",
      "diff": "Incorrect\n1. GENERATED requires the memory to remain allocated and accessible for lifetime 'a, an explicit lifetime obligation not stated in ORIGINAL.\n2. GENERATED permits elements without initialized T values, adding an initialization condition absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::as_uninit_slice_mut",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires the memory to remain allocated and accessible for lifetime 'a, an explicit lifetime obligation not stated in ORIGINAL.",
          "GENERATED permits elements without initialized T values, adding an initialization condition absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that *either* the pointer is null *or*\nall of the following is true:\n\n* The pointer must be [valid] for reads and writes for `ptr.len() * size_of::<T>()`\n  many bytes, and it must be properly aligned. This means in particular:\n\n    * The entire memory range of this slice must be contained within a single [allocation]!\n      Slices can never span across multiple allocations.\n\n    * The pointer must be aligned even for zero-length slices. One\n      reason for this is that enum layout optimizations may rely on references\n      (including slices of any length) being aligned and non-null to distinguish\n      them from other data. You can obtain a pointer that is usable as `data`\n      for zero-length slices using [`NonNull::dangling()`].\n\n* The total size `ptr.len() * size_of::<T>()` of the slice must be no larger than `isize::MAX`.\n  See the safety documentation of [`pointer::offset`].\n\n* You must enforce Rust's aliasing rules, since the returned lifetime `'a` is\n  arbitrarily chosen and does not necessarily reflect the actual lifetime of the data.\n  In particular, while this reference exists, the memory the pointer points to must\n  not get accessed (read or written) through any other pointer.\n\nThis applies even if the result of this method is unused!\n\nSee also [`slice::from_raw_parts_mut`][].\n\n[valid]: crate::ptr#safety\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::pointer::get_unchecked",
      "source_api_path": "core::pointer::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`, and `self.start..self.end` must be a valid range within it: `self.start <= self.end <= (*slice).len()`. Both `self.start` and `self.end` must lie on UTF-8 character boundaries. The allocation backing `slice` must remain valid for any subsequent use of the returned pointer.",
      "diff": "Incorrect\n1. Generated requires a valid `str`, in-range bounds, and UTF-8 boundaries, adding unsupported preconditions beyond out-of-bounds indexing or a dangling pointer.\n2. Generated limits validity to subsequent pointer use, whereas ORIGINAL states out-of-bounds indexing or a dangling `slice` pointer is undefined behavior even when unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires a valid `str`, in-range bounds, and UTF-8 boundaries, adding unsupported preconditions beyond out-of-bounds indexing or a dangling pointer.",
          "Generated limits validity to subsequent pointer use, whereas ORIGINAL states out-of-bounds indexing or a dangling `slice` pointer is undefined behavior even when unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::pointer::get_unchecked_mut",
      "source_api_path": "core::pointer::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must point to an allocation containing at least `slice.len()` bytes of valid UTF-8. The range `self.start..self.end` must satisfy `self.start <= self.end <= slice.len()`, and both endpoints must lie on UTF-8 character boundaries.",
      "diff": "Incorrect\n1. Generated adds unsupported UTF-8, allocation, and endpoint-boundary requirements, while omitting the original out-of-bounds-index and dangling-pointer conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported UTF-8, allocation, and endpoint-boundary requirements, while omitting the original out-of-bounds-index and dangling-pointer conditions."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::pointer::offset",
      "source_api_path": "core::pointer::offset",
      "llm_generated_comment": "# Safety\n\nIf `T` has nonzero size:\n\n- `self` and the resulting pointer must both be within, or one past the end of, the same allocated object.\n- `count * size_of::<T>()` must fit in an `isize`.\n- Adding that byte offset to the address of `self` must not overflow or wrap around the address space; the mathematical result must fit in a `usize`.\n\nIf `T` is zero-sized, there are no additional safety requirements.",
      "diff": "Incorrect\n1. Generated omits the requirement that `self` be derived from an allocation when the computed offset is non-zero.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::offset",
        "classification": "Incorrect",
        "items": [
          "Generated omits the requirement that `self` be derived from an allocation when the computed offset is non-zero."
        ]
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n\"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() + count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `min(self.addr(), result)..max(self.addr(), result)`)\nmust be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two. This implies, for\ninstance, that `vec.as_ptr().offset(vec.len() as isize)` (for `vec: Vec<T>`) is always safe.\n\n[allocation]: crate::ptr#allocation\n\nConsider using [`wrapping_offset`](#method.wrapping_offset) instead if these constraints are\ndifficult to satisfy. The only advantage of this method is that it\nenables more aggressive compiler optimizations."
    },
    {
      "target_api_path": "core::pointer::offset_from",
      "source_api_path": "core::pointer::offset_from",
      "llm_generated_comment": "# Safety\n\nAt the time of the call:\n\n- If `self` and `origin` have different addresses, they must be derived from the same allocation, and the entire byte range between them must lie within that allocation, including allowing either pointer to be one-past-the-end.\n- The byte distance between `self` and `origin` must be exactly divisible by `size_of::<T>()`, and the resulting element offset must fit in an `isize`.\n\nIf `self` and `origin` have the same address, they need not have the same provenance.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::pointer::offset_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* `self` and `origin` must either\n\n  * point to the same address, or\n  * both be [derived from][crate::ptr#provenance] a pointer to the same [allocation], and the memory range between\n    the two pointers must be in bounds of that object. (See below for an example.)\n\n* The distance between the pointers, in bytes, must be an exact multiple\n  of the size of `T`.\n\nAs a consequence, the absolute distance between the pointers, in bytes, computed on\nmathematical integers (without \"wrapping around\"), cannot overflow an `isize`. This is\nimplied by the in-bounds requirement, and the fact that no allocation can be larger\nthan `isize::MAX` bytes.\n\nThe requirement for pointers to be derived from the same allocation is primarily\nneeded for `const`-compatibility: the distance between pointers into *different* allocated\nobjects is not known at compile-time. However, the requirement also exists at\nruntime and may be exploited by optimizations. If you wish to compute the difference between\npointers that are not guaranteed to be from the same allocation, use\n`(self.addr() as isize - origin.addr() as isize) / size_of::<T>()`.\n\n[`add`]: #method.add\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::pointer::offset_from_unsigned",
      "source_api_path": "core::pointer::offset_from_unsigned",
      "llm_generated_comment": "# Safety\n\n- `self` and `origin` must be derived from the same allocated object, or `origin` and `self` may be one-past-the-end pointers for that same object.\n- `self` must be at or after `origin` within that object.\n- The distance from `origin` to `self`, measured in units of `T`, must be representable as a `usize`.",
      "diff": "Incorrect\n1. The generated section omits the requirement that the pointers differ by at most isize::MAX bytes and instead requires only a usize-representable element distance.\n2. The generated section omits all other safety conditions inherited from offset_from.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::offset_from_unsigned",
        "classification": "Incorrect",
        "items": [
          "The generated section omits the requirement that the pointers differ by at most isize::MAX bytes and instead requires only a usize-representable element distance.",
          "The generated section omits all other safety conditions inherited from offset_from."
        ]
      },
      "original_safety_doc": "- The distance between the pointers must be non-negative (`self >= origin`)\n\n- *All* the safety conditions of [`offset_from`](#method.offset_from)\n  apply to this method as well; see it for the full details.\n\nImportantly, despite the return type of this method being able to represent\na larger offset, it's still *not permitted* to pass pointers which differ\nby more than `isize::MAX` *bytes*.  As such, the result of this method will\nalways be less than or equal to `isize::MAX as usize`."
    },
    {
      "target_api_path": "core::pointer::split_at_mut",
      "source_api_path": "core::pointer::split_at_mut",
      "llm_generated_comment": "# Safety\n\n- `self` must be a valid raw slice pointer: it must be properly aligned and non-null, its length metadata must accurately describe the number of elements in the allocation, and the entire range of that length must be valid for reads and writes as `T`.\n- The allocation described by `self` must be sufficiently sized for the slice, and the pointer arithmetic and resulting slices must remain within the same allocation.",
      "diff": "Incorrect\n1. Generated requires `self` to be properly aligned and non-null, but ORIGINAL only requires dereferenceability and a single allocation spanning at least `mid` elements.\n2. Generated requires the entire slice range to be valid for reads and writes as `T`, an unsupported strengthening beyond ORIGINAL's `mid`-sized dereferenceability requirement.\n3. Generated requires length metadata to accurately describe the allocation's element count, but ORIGINAL states that `len` is not a safety invariant.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::split_at_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires `self` to be properly aligned and non-null, but ORIGINAL only requires dereferenceability and a single allocation spanning at least `mid` elements.",
          "Generated requires the entire slice range to be valid for reads and writes as `T`, an unsupported strengthening beyond ORIGINAL's `mid`-sized dereferenceability requirement.",
          "Generated requires length metadata to accurately describe the allocation's element count, but ORIGINAL states that `len` is not a safety invariant."
        ]
      },
      "original_safety_doc": "`mid` must be [in-bounds] of the underlying [allocation].\nWhich means `self` must be dereferenceable and span a single allocation\nthat is at least `mid * size_of::<T>()` bytes long. Not upholding these\nrequirements is *[undefined behavior]* even if the resulting pointers are not used.\n\nSince `len` being in-bounds is not a safety invariant of `*mut [T]` the\nsafety requirements of this method are the same as for [`split_at_mut_unchecked`].\nThe explicit bounds check is only as useful as `len` is correct.\n\n[`split_at_mut_unchecked`]: #method.split_at_mut_unchecked\n[in-bounds]: #method.add\n[allocation]: crate::ptr#allocation\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::pointer::split_at_mut_unchecked",
      "source_api_path": "core::pointer::split_at_mut_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `mid` is less than or equal to the length of the slice.",
      "diff": "Missing\n1. It omits that `self` must be dereferenceable.\n2. It omits that `self` must span a single allocation at least `mid * size_of::<T>()` bytes long.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::pointer::split_at_mut_unchecked",
        "classification": "Missing",
        "items": [
          "It omits that `self` must be dereferenceable.",
          "It omits that `self` must span a single allocation at least `mid * size_of::<T>()` bytes long."
        ]
      },
      "original_safety_doc": "`mid` must be [in-bounds] of the underlying [allocation].\nWhich means `self` must be dereferenceable and span a single allocation\nthat is at least `mid * size_of::<T>()` bytes long. Not upholding these\nrequirements is *[undefined behavior]* even if the resulting pointers are not used.\n\n[in-bounds]: #method.add\n[out-of-bounds index]: #method.add\n[allocation]: crate::ptr#allocation\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::pointer::sub",
      "source_api_path": "core::pointer::sub",
      "llm_generated_comment": "# Safety\n\nIf `T` is not zero-sized and `count` is nonzero:\n\n- `count * size_of::<T>()` must not exceed `isize::MAX`.\n- Subtracting that byte offset from `self` must not underflow or otherwise wrap around the address space.\n- `self` and the resulting pointer must both be within the bounds of the same allocated object, or one past its end. The calculation must not cross an allocation boundary.\n\nFor zero-sized `T`, or when `count` is zero, there are no additional safety requirements.",
      "diff": "Incorrect\n1. Generated replaces the requirement that `self` be derived from an allocation with an unsupported requirement that both pointers be within the same allocated object.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::pointer::sub",
        "classification": "Incorrect",
        "items": [
          "Generated replaces the requirement that `self` be derived from an allocation with an unsupported requirement that both pointers be within the same allocated object."
        ]
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n  \"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() - count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `result..self.addr()`) must be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two.\n\n[allocation]: crate::ptr#allocation\n\nConsider using [`wrapping_sub`](#method.wrapping_sub) instead if these constraints are\ndifficult to satisfy. The only advantage of this method is that it\nenables more aggressive compiler optimizations."
    },
    {
      "target_api_path": "core::ptr::copy",
      "source_api_path": "core::ptr::copy",
      "llm_generated_comment": "# Safety\n\n- `src` must be valid for reads of `count` values of `T`, and `dst` must be valid for writes of `count` values of `T`, for the duration of the call.\n- Unless `T` is a zero-sized type or `count` is zero, both `src` and `dst` must be non-null and properly aligned for `T`.\n- The source and destination regions may overlap.",
      "diff": "Incorrect\n1. The generated alignment condition exempts zero-sized types and zero counts, but ORIGINAL requires both pointers to be properly aligned even when the copied size is zero.\n2. The generated overlap statement omits ORIGINAL's requirement that `dst` remain valid while overlapping `src` bytes are read.\n3. The generated section omits the safety condition that using both bitwise-copied non-`Copy` values can violate memory safety.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::copy",
        "classification": "Incorrect",
        "items": [
          "The generated alignment condition exempts zero-sized types and zero counts, but ORIGINAL requires both pointers to be properly aligned even when the copied size is zero.",
          "The generated overlap statement omits ORIGINAL's requirement that `dst` remain valid while overlapping `src` bytes are read.",
          "The generated section omits the safety condition that using both bitwise-copied non-`Copy` values can violate memory safety."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `src` must be [valid] for reads of `count * size_of::<T>()` bytes or that number must be 0.\n\n* `dst` must be [valid] for writes of `count * size_of::<T>()` bytes or that number must be 0,\n  and `dst` must remain valid even when `src` is read for `count * size_of::<T>()` bytes. (This\n  means if the memory ranges overlap, the `dst` pointer must not be invalidated by `src` reads.)\n\n* Both `src` and `dst` must be properly aligned.\n\nLike [`read`], `copy` creates a bitwise copy of `T`, regardless of\nwhether `T` is [`Copy`]. If `T` is not [`Copy`], using both the values\nin the region beginning at `*src` and the region beginning at `*dst` can\n[violate memory safety][read-ownership].\n\nNote that even if the effectively copied size (`count * size_of::<T>()`) is\n`0`, the pointers must be properly aligned.\n\n[`read`]: crate::ptr::read\n[read-ownership]: crate::ptr::read#ownership-of-the-returned-value\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::ptr::copy_nonoverlapping",
      "source_api_path": "core::ptr::copy_nonoverlapping",
      "llm_generated_comment": "# Safety\n\n- `src` must be valid for reads of `count` consecutive values of `T`, and `dst` must be valid for writes of `count` consecutive values of `T`.\n- The source and destination memory ranges, each spanning `count * size_of::<T>()` bytes, must not overlap.\n- If `count != 0` and `size_of::<T>() != 0`, both `src` and `dst` must be non-null and properly aligned for `T`.",
      "diff": "Incorrect\n1. Generated requires validity for count values, whereas ORIGINAL requires validity for count * size_of::<T>() bytes, including the zero-byte case.\n2. Generated requires alignment only when count and size_of::<T>() are nonzero, but ORIGINAL requires both pointers aligned even when the copied size is zero.\n3. Generated omits the ORIGINAL safety condition concerning simultaneous use of source and destination values when T is not Copy.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::copy_nonoverlapping",
        "classification": "Incorrect",
        "items": [
          "Generated requires validity for count values, whereas ORIGINAL requires validity for count * size_of::<T>() bytes, including the zero-byte case.",
          "Generated requires alignment only when count and size_of::<T>() are nonzero, but ORIGINAL requires both pointers aligned even when the copied size is zero.",
          "Generated omits the ORIGINAL safety condition concerning simultaneous use of source and destination values when T is not Copy."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `src` must be [valid] for reads of `count * size_of::<T>()` bytes or that number must be 0.\n\n* `dst` must be [valid] for writes of `count * size_of::<T>()` bytes or that number must be 0.\n\n* Both `src` and `dst` must be properly aligned.\n\n* The region of memory beginning at `src` with a size of `count *\n  size_of::<T>()` bytes must *not* overlap with the region of memory\n  beginning at `dst` with the same size.\n\nLike [`read`], `copy_nonoverlapping` creates a bitwise copy of `T`, regardless of\nwhether `T` is [`Copy`]. If `T` is not [`Copy`], using *both* the values\nin the region beginning at `*src` and the region beginning at `*dst` can\n[violate memory safety][read-ownership].\n\nNote that even if the effectively copied size (`count * size_of::<T>()`) is\n`0`, the pointers must be properly aligned.\n\n[`read`]: crate::ptr::read\n[read-ownership]: crate::ptr::read#ownership-of-the-returned-value\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::ptr::drop_in_place",
      "source_api_path": "core::ptr::drop_in_place",
      "llm_generated_comment": "# Safety\n\n- `to_drop` must be non-null, properly aligned for `T`, and point to a valid, initialized value of type `T` for the duration of this call. If `T` is unsized, its pointer metadata must also be valid.\n- The pointed-to value must be exclusively accessible for the duration of this call: no other reference or pointer may be used to access it while it is being dropped.\n- The value must not already have been dropped. After this call returns, the caller must not use `to_drop` to access or drop the value again unless the storage has been properly reinitialized with a new valid value of type `T`.",
      "diff": "Incorrect\n1. Generated omits the requirement that `to_drop` be valid for both reads and writes.\n2. Generated omits type-specific invariants required for the value to be valid for dropping, such as a `Box`'s heap pointer being valid.\n3. Generated adds an unsupported standalone requirement that the value must not already have been dropped.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::drop_in_place",
        "classification": "Incorrect",
        "items": [
          "Generated omits the requirement that `to_drop` be valid for both reads and writes.",
          "Generated omits type-specific invariants required for the value to be valid for dropping, such as a `Box`'s heap pointer being valid.",
          "Generated adds an unsupported standalone requirement that the value must not already have been dropped."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `to_drop` must be [valid] for both reads and writes.\n\n* `to_drop` must be properly aligned, even if `T` has size 0.\n\n* `to_drop` must be nonnull, even if `T` has size 0.\n\n* The value `to_drop` points to must be valid for dropping, which may mean\n  it must uphold additional invariants. These invariants depend on the type\n  of the value being dropped. For instance, when dropping a Box, the box's\n  pointer to the heap must be valid.\n\n* While `drop_in_place` is executing, the only way to access parts of\n  `to_drop` is through the `&mut self` references supplied to the\n  `Drop::drop` methods that `drop_in_place` invokes.\n\nAdditionally, if `T` is not [`Copy`], using the pointed-to value after\ncalling `drop_in_place` can cause undefined behavior. Note that `*to_drop =\nfoo` counts as a use because it will cause the value to be dropped\nagain. [`write()`] can be used to overwrite data without causing it to be\ndropped.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::add",
      "source_api_path": "core::ptr::non_null::NonNull::add",
      "llm_generated_comment": "# Safety\n\n- `self` must point to an element within a single allocated object of type `T`, or to the one-past-the-end address of that object, and the result of advancing it by `count` elements must remain within that same object or be one-past-the-end.\n- The byte offset `count * size_of::<T>()` must fit in an `isize`, and adding that offset to the address represented by `self` must not overflow.",
      "diff": "Incorrect\n1. Generated requires `self` to be within an allocated `T` object even when `count` is zero, unlike ORIGINAL's provenance and bounds requirement only for non-zero offsets.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::add",
        "classification": "Incorrect",
        "items": [
          "Generated requires `self` to be within an allocated `T` object even when `count` is zero, unlike ORIGINAL's provenance and bounds requirement only for non-zero offsets."
        ]
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n\"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() + count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `self.addr()..result`) must be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two. This implies, for\ninstance, that `vec.as_ptr().add(vec.len())` (for `vec: Vec<T>`) is always safe.\n\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_mut",
      "source_api_path": "core::ptr::non_null::NonNull::as_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is not null, the caller must ensure that it is valid for a mutable reference for the entire lifetime `'a`: the pointer must be properly aligned and point to a valid, initialized value of type `T` that is dereferenceable for `'a`. The pointed-to value must not be accessed through any other pointer or reference for the duration of `'a`.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements for mutable-reference validity, initialization, exclusive access, and a specific lifetime; the original only requires pointer convertibility to a reference.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements for mutable-reference validity, initialization, exclusive access, and a specific lifetime; the original only requires pointer convertibility to a reference."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_ref",
      "source_api_path": "core::ptr::non_null::NonNull::as_ref",
      "llm_generated_comment": "# Safety\n\n`self` must point to a properly aligned, initialized, valid value of type `T` that is dereferenceable with valid metadata. The pointee must remain allocated and valid for the entire lifetime `'a`, and it must not be mutated for that lifetime except through an `UnsafeCell`. No mutable reference to the pointee may coexist with the returned reference.",
      "diff": "Incorrect\n1. The generated section adds requirements that the pointee remain valid for 'a, avoid mutation except through UnsafeCell, and have no coexisting mutable reference; ORIGINAL only requires pointer-to-reference convertibility.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_ref",
        "classification": "Incorrect",
        "items": [
          "The generated section adds requirements that the pointee remain valid for 'a, avoid mutation except through UnsafeCell, and have no coexisting mutable reference; ORIGINAL only requires pointer-to-reference convertibility."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_uninit_mut",
      "source_api_path": "core::ptr::non_null::NonNull::as_uninit_mut",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned and point to memory that is valid for mutable access as a `MaybeUninit<T>` for the entire lifetime `'a`. No other references or accesses that conflict with the returned `&mut` may exist during `'a`, and the referenced memory must remain allocated and otherwise valid for that duration.",
      "diff": "Incorrect\n1. Generated adds lifetime, allocation, alignment, and aliasing requirements beyond the original pointer-to-reference convertibility condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_uninit_mut",
        "classification": "Incorrect",
        "items": [
          "Generated adds lifetime, allocation, alignment, and aliasing requirements beyond the original pointer-to-reference convertibility condition."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion).\nNote that because the created reference is to `MaybeUninit<T>`, the\nsource pointer can point to uninitialized memory."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_uninit_ref",
      "source_api_path": "core::ptr::non_null::NonNull::as_uninit_ref",
      "llm_generated_comment": "# Safety\n\nIf `self` is non-null, it must be properly aligned and valid for creating a shared reference to a `MaybeUninit<T>`. The pointed-to storage must remain allocated and dereferenceable for `size_of::<T>()` bytes for the entire lifetime `'a`.\n\nFor the duration of `'a`, the storage must not be mutated or accessed through an exclusive reference except through an `UnsafeCell`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_uninit_ref",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "When calling this method, you have to ensure that\nthe pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion).\nNote that because the created reference is to `MaybeUninit<T>`, the\nsource pointer can point to uninitialized memory."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_uninit_slice",
      "source_api_path": "core::ptr::non_null::NonNull::as_uninit_slice",
      "llm_generated_comment": "# Safety\n\nThe data pointer represented by `self` must be suitably aligned for `MaybeUninit<T>` and must refer to a single allocated object (or one-past-the-end for a zero-length slice) containing at least `self.len()` consecutive `MaybeUninit<T>` elements. The total size of the resulting slice must not exceed `isize::MAX` and must not overflow.\n\nThe pointed-to memory must remain allocated and valid for the returned slice's lifetime, and the returned shared reference's aliasing requirements must be upheld for that lifetime. The elements do not need to be initialized as `T`.",
      "diff": "Incorrect\n1. The generated condition requires the memory to remain allocated and valid for the returned lifetime, while ORIGINAL requires pointer validity for the call and enforces aliasing only while the reference exists.\n2. The generated condition restricts the pointer to a single allocated object or one-past position, whereas ORIGINAL requires the byte range to fit within one allocation and permits aligned dangling pointers for zero-length slices.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_uninit_slice",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires the memory to remain allocated and valid for the returned lifetime, while ORIGINAL requires pointer validity for the call and enforces aliasing only while the reference exists.",
          "The generated condition restricts the pointer to a single allocated object or one-past position, whereas ORIGINAL requires the byte range to fit within one allocation and permits aligned dangling pointers for zero-length slices."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that all of the following is true:\n\n* The pointer must be [valid] for reads for `ptr.len() * size_of::<T>()` many bytes,\n  and it must be properly aligned. This means in particular:\n\n    * The entire memory range of this slice must be contained within a single allocation!\n      Slices can never span across multiple allocations.\n\n    * The pointer must be aligned even for zero-length slices. One\n      reason for this is that enum layout optimizations may rely on references\n      (including slices of any length) being aligned and non-null to distinguish\n      them from other data. You can obtain a pointer that is usable as `data`\n      for zero-length slices using [`NonNull::dangling()`].\n\n* The total size `ptr.len() * size_of::<T>()` of the slice must be no larger than `isize::MAX`.\n  See the safety documentation of [`pointer::offset`].\n\n* You must enforce Rust's aliasing rules, since the returned lifetime `'a` is\n  arbitrarily chosen and does not necessarily reflect the actual lifetime of the data.\n  In particular, while this reference exists, the memory the pointer points to must\n  not get mutated (except inside `UnsafeCell`).\n\nThis applies even if the result of this method is unused!\n\nSee also [`slice::from_raw_parts`].\n\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::as_uninit_slice_mut",
      "source_api_path": "core::ptr::non_null::NonNull::as_uninit_slice_mut",
      "llm_generated_comment": "# Safety\n\nIf the data pointer in `self` is non-null:\n\n- It must be properly aligned for `T`.\n- It must point to `self.len()` consecutive `MaybeUninit<T>` elements within a single allocation, and the entire range must be valid for both reads and writes for the chosen lifetime `'a`.\n- The allocation must remain live for the entire lifetime `'a`.\n- No other pointer may access the referenced memory in a way that conflicts with the returned mutable slice for the duration of `'a`.\n- The total size `self.len() * size_of::<T>()` must not overflow and must not exceed `isize::MAX`.\n\nThe elements need not be initialized. If the data pointer is null, the function returns `None`.",
      "diff": "Incorrect\n1. The generated section adds null-pointer behavior and claims a null data pointer returns None, which is not stated in ORIGINAL.\n2. The generated section requires the size multiplication not to overflow, adding a condition beyond ORIGINAL's stated isize::MAX bound.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::as_uninit_slice_mut",
        "classification": "Incorrect",
        "items": [
          "The generated section adds null-pointer behavior and claims a null data pointer returns None, which is not stated in ORIGINAL.",
          "The generated section requires the size multiplication not to overflow, adding a condition beyond ORIGINAL's stated isize::MAX bound."
        ]
      },
      "original_safety_doc": "When calling this method, you have to ensure that all of the following is true:\n\n* The pointer must be [valid] for reads and writes for `ptr.len() * size_of::<T>()`\n  many bytes, and it must be properly aligned. This means in particular:\n\n    * The entire memory range of this slice must be contained within a single allocation!\n      Slices can never span across multiple allocations.\n\n    * The pointer must be aligned even for zero-length slices. One\n      reason for this is that enum layout optimizations may rely on references\n      (including slices of any length) being aligned and non-null to distinguish\n      them from other data. You can obtain a pointer that is usable as `data`\n      for zero-length slices using [`NonNull::dangling()`].\n\n* The total size `ptr.len() * size_of::<T>()` of the slice must be no larger than `isize::MAX`.\n  See the safety documentation of [`pointer::offset`].\n\n* You must enforce Rust's aliasing rules, since the returned lifetime `'a` is\n  arbitrarily chosen and does not necessarily reflect the actual lifetime of the data.\n  In particular, while this reference exists, the memory the pointer points to must\n  not get accessed (read or written) through any other pointer.\n\nThis applies even if the result of this method is unused!\n\nSee also [`slice::from_raw_parts_mut`].\n\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::get_unchecked_mut",
      "source_api_path": "core::ptr::non_null::NonNull::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid, mutable `str` slice for the duration of any use of the returned pointer.\n- `self.start` and `self.end` must satisfy `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` must each lie on UTF-8 character boundaries within `slice`.",
      "diff": "Incorrect\n1. Generated requires UTF-8 character boundaries, an unsupported condition absent from the original safety requirements.\n2. Generated requires a valid mutable `str` slice for the duration of pointer use, while the original requires non-dangling only even when the result is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires UTF-8 character boundaries, an unsupported condition absent from the original safety requirements.",
          "Generated requires a valid mutable `str` slice for the duration of pointer use, while the original requires non-dangling only even when the result is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::new_unchecked",
      "source_api_path": "core::ptr::non_null::NonNull::new_unchecked",
      "llm_generated_comment": "# Safety\n\nThe pointer `n` must be non-null.",
      "diff": "Incorrect\n1. The generated condition applies to pointer `n`, while ORIGINAL requires the parameter `ptr` to be non-null.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::new_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies to pointer `n`, while ORIGINAL requires the parameter `ptr` to be non-null."
        ]
      },
      "original_safety_doc": "`ptr` must be non-null."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::offset",
      "source_api_path": "core::ptr::non_null::NonNull::offset",
      "llm_generated_comment": "# Safety\n\n- `count * size_of::<T>()` must fit in an `isize`, and adding that byte offset to `self` must not wrap around the address space.\n- If `T` is non-zero-sized and `count` is nonzero, `self` and the resulting pointer must be within, or one past the end of, the same allocated object. This in-bounds relationship must not rely on address-space wraparound.",
      "diff": "Incorrect\n1. The generated condition omits the requirement that `self` be derived from a pointer to an allocation when the computed offset is nonzero.\n2. The generated address condition replaces the mathematical requirement that `self.addr() + offset` fit in `usize` with the different, less precise requirement that address-space addition not wrap.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::offset",
        "classification": "Incorrect",
        "items": [
          "The generated condition omits the requirement that `self` be derived from a pointer to an allocation when the computed offset is nonzero.",
          "The generated address condition replaces the mathematical requirement that `self.addr() + offset` fit in `usize` with the different, less precise requirement that address-space addition not wrap."
        ]
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n\"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() + count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `min(self.addr(), result)..max(self.addr(), result)`)\nmust be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two. This implies, for\ninstance, that `vec.as_ptr().offset(vec.len() as isize)` (for `vec: Vec<T>`) is always safe.\n\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::offset_from",
      "source_api_path": "core::ptr::non_null::NonNull::offset_from",
      "llm_generated_comment": "# Safety\n\n- `self` and `origin` must either have the same address, or both be derived from the same allocation with the entire byte range between them remaining within that allocation (endpoints may be one-past-the-end).\n- The byte distance between `self` and `origin` must be an exact multiple of `size_of::<T>()`.\n- The byte distance must be representable as an `isize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::offset_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* `self` and `origin` must either\n\n  * point to the same address, or\n  * both be *derived from* a pointer to the same [allocation], and the memory range between\n    the two pointers must be in bounds of that object. (See below for an example.)\n\n* The distance between the pointers, in bytes, must be an exact multiple\n  of the size of `T`.\n\nAs a consequence, the absolute distance between the pointers, in bytes, computed on\nmathematical integers (without \"wrapping around\"), cannot overflow an `isize`. This is\nimplied by the in-bounds requirement, and the fact that no allocation can be larger\nthan `isize::MAX` bytes.\n\nThe requirement for pointers to be derived from the same allocation is primarily\nneeded for `const`-compatibility: the distance between pointers into *different* allocated\nobjects is not known at compile-time. However, the requirement also exists at\nruntime and may be exploited by optimizations. If you wish to compute the difference between\npointers that are not guaranteed to be from the same allocation, use\n`(self.addr() as isize - origin.addr() as isize) / size_of::<T>()`.\n\n[`add`]: #method.add\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::offset_from_unsigned",
      "source_api_path": "core::ptr::non_null::NonNull::offset_from_unsigned",
      "llm_generated_comment": "# Safety\n\n- `self` and `origin` must be derived from the same allocated object, or one past the end of the same allocated object.\n- The distance between `origin` and `self`, in bytes, must be no greater than `isize::MAX`.",
      "diff": "Incorrect\n1. The generated condition does not require `self >= origin`; a byte-distance upper bound alone can permit the pointers in the wrong order.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::offset_from_unsigned",
        "classification": "Incorrect",
        "items": [
          "The generated condition does not require `self >= origin`; a byte-distance upper bound alone can permit the pointers in the wrong order."
        ]
      },
      "original_safety_doc": "- The distance between the pointers must be non-negative (`self >= origin`)\n\n- *All* the safety conditions of [`offset_from`](#method.offset_from)\n  apply to this method as well; see it for the full details.\n\nImportantly, despite the return type of this method being able to represent\na larger offset, it's still *not permitted* to pass pointers which differ\nby more than `isize::MAX` *bytes*.  As such, the result of this method will\nalways be less than or equal to `isize::MAX as usize`."
    },
    {
      "target_api_path": "core::ptr::non_null::NonNull::sub",
      "source_api_path": "core::ptr::non_null::NonNull::sub",
      "llm_generated_comment": "# Safety\n\nFor a non-zero-sized `T`, `self` must be a pointer into a single allocated object (or one past its end), and subtracting `count` elements must produce a pointer within that same object or one past its end. The resulting byte offset must be representable by `isize`; in particular, `count * size_of::<T>()` must not overflow `usize` and must be at most `isize::MAX`, and the subtraction must not underflow the address of `self`.\n\nFor a zero-sized `T`, no pointer-arithmetic preconditions are required because the function returns `self` without performing an address calculation.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ptr::non_null::NonNull::sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "If any of the following conditions are violated, the result is Undefined Behavior:\n\n* The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without\n  \"wrapping around\"), must fit in an `isize`.\n\n* Let `result` be `self.addr() - count * size_of::<T>()`, computed on mathematical integers.\nThis must fit in a `usize`.\n\n* If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some\n[allocation], and the entire memory range between `self` and `result`\n(i.e., `result..self.addr()`) must be in bounds of that allocation.\n\nAllocations can never be larger than `isize::MAX` bytes and they can only contain addresses\nrepresentable by `usize`, so technically the last condition implies the first two.\n\n[allocation]: crate::ptr#allocation"
    },
    {
      "target_api_path": "core::ptr::read",
      "source_api_path": "core::ptr::read",
      "llm_generated_comment": "# Safety\n\n- `src` must be non-null, properly aligned for `T`, and valid for reading `size_of::<T>()` bytes.\n- `src` must point to a properly initialized, valid value of type `T`.\n- Because this performs a bitwise copy without modifying `*src`, the caller must ensure that having both the returned value and the value at `src` does not violate `T`’s ownership or validity invariants. If `T` cannot be safely duplicated, the value at `src` must subsequently be treated as moved and must not be used or dropped unless it is first replaced without dropping the old value.",
      "diff": "Incorrect\n1. Generated requires `src` to be non-null and valid for reading bytes, omitting the ORIGINAL exception allowing non-read-valid pointers when `T` is a ZST.\n2. Generated adds ownership and post-read usage or drop requirements for non-duplicable `T`, which are not specified by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::read",
        "classification": "Incorrect",
        "items": [
          "Generated requires `src` to be non-null and valid for reading bytes, omitting the ORIGINAL exception allowing non-read-valid pointers when `T` is a ZST.",
          "Generated adds ownership and post-read usage or drop requirements for non-duplicable `T`, which are not specified by ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `src` must be [valid] for reads or `T` must be a ZST.\n\n* `src` must be properly aligned. Use [`read_unaligned`] if this is not the\n  case.\n\n* `src` must point to a properly initialized value of type `T`.\n\nNote that even if `T` has size `0`, the pointer must be properly aligned."
    },
    {
      "target_api_path": "core::ptr::read_unaligned",
      "source_api_path": "core::ptr::read_unaligned",
      "llm_generated_comment": "# Safety\n\n- `src` must be non-null and valid for reading `size_of::<T>()` bytes for the duration of the call. The read must not race with or otherwise conflict with accesses to the same memory.\n- Those bytes must be fully initialized and represent a valid value of type `T`.\n- If `T` is not `Copy`, the caller must ensure that duplicating the value does not cause it to be used or dropped more than once; after the read, the value remaining at `src` must generally be treated as logically moved out unless it is overwritten without being read or dropped.",
      "diff": "Incorrect\n1. Generated adds non-null, call-duration, and non-racing requirements absent from ORIGINAL.\n2. Generated changes the ownership condition into a caller obligation and adds a logical-move rule not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::read_unaligned",
        "classification": "Incorrect",
        "items": [
          "Generated adds non-null, call-duration, and non-racing requirements absent from ORIGINAL.",
          "Generated changes the ownership condition into a caller obligation and adds a logical-move rule not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `src` must be [valid] for reads.\n\n* `src` must point to a properly initialized value of type `T`.\n\nLike [`read`], `read_unaligned` creates a bitwise copy of `T`, regardless of\nwhether `T` is [`Copy`]. If `T` is not [`Copy`], using both the returned\nvalue and the value at `*src` can [violate memory safety][read-ownership].\n\n[read-ownership]: read#ownership-of-the-returned-value\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::read_volatile",
      "source_api_path": "core::ptr::read_volatile",
      "llm_generated_comment": "# Safety\n\n`src` must be properly aligned for `T` and valid for reading a properly initialized value of type `T`. The memory accessed must not be concurrently mutated in a way that causes a data race while the volatile read occurs.",
      "diff": "Incorrect\n1. GENERATED requires `src` to be valid for reading in all cases, omitting ORIGINAL's exceptions for ZSTs and suitably non-Rust-allocated memory.\n2. GENERATED adds a concurrent-mutation/data-race condition absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::read_volatile",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires `src` to be valid for reading in all cases, omitting ORIGINAL's exceptions for ZSTs and suitably non-Rust-allocated memory.",
          "GENERATED adds a concurrent-mutation/data-race condition absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "Like [`read`], `read_volatile` creates a bitwise copy of `T`, regardless of whether `T` is\n[`Copy`]. If `T` is not [`Copy`], using both the returned value and the value at `*src` can\n[violate memory safety][read-ownership]. However, storing non-[`Copy`] types in volatile memory\nis almost certainly incorrect.\n\nBehavior is undefined if any of the following conditions are violated:\n\n* `src` must be either [valid] for reads, or `T` must be a ZST, or `src` must point to memory\n  outside of all Rust allocations and reading from that memory must:\n  - not trap, and\n  - not cause any memory inside a Rust allocation to be modified.\n\n* `src` must be properly aligned.\n\n* Reading from `src` must produce a properly initialized value of type `T`.\n\nNote that even if `T` has size `0`, the pointer must be properly aligned.\n\n[valid]: self#safety\n[read-ownership]: read#ownership-of-the-returned-value"
    },
    {
      "target_api_path": "core::ptr::replace",
      "source_api_path": "core::ptr::replace",
      "llm_generated_comment": "# Safety\n\n- If `T` is not a zero-sized type, `dst` must be non-null, properly aligned for `T`, and point to a valid, initialized `T` that is valid for both reads and writes for the duration of the call.\n- The storage occupied by the value `src` must not overlap the storage pointed to by `dst`.",
      "diff": "Incorrect\n1. Generated adds an unsupported requirement that `src` storage must not overlap `dst`; ORIGINAL specifies no such condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::replace",
        "classification": "Incorrect",
        "items": [
          "Generated adds an unsupported requirement that `src` storage must not overlap `dst`; ORIGINAL specifies no such condition."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dst` must be [valid] for both reads and writes or `T` must be a ZST.\n\n* `dst` must be properly aligned.\n\n* `dst` must point to a properly initialized value of type `T`.\n\nNote that even if `T` has size `0`, the pointer must be properly aligned.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::swap",
      "source_api_path": "core::ptr::swap",
      "llm_generated_comment": "# Safety\n\nFor the duration of the call:\n\n- `x` must be properly aligned and valid for reading and writing one value of type `T`.\n- `y` must be properly aligned and valid for reading and writing one value of type `T`.\n\nThe memory ranges referenced by `x` and `y` may overlap.",
      "diff": "Incorrect\n1. The generated text permits overlapping ranges without preserving the original requirement that the pointers not be subject to aliasing restrictions relative to each other.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::swap",
        "classification": "Incorrect",
        "items": [
          "The generated text permits overlapping ranges without preserving the original requirement that the pointers not be subject to aliasing restrictions relative to each other."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* Both `x` and `y` must be [valid] for both reads and writes. They must remain valid even when the\n  other pointer is written. (This means if the memory ranges overlap, the two pointers must not\n  be subject to aliasing restrictions relative to each other.)\n\n* Both `x` and `y` must be properly aligned.\n\nNote that even if `T` has size `0`, the pointers must be properly aligned.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::swap_nonoverlapping",
      "source_api_path": "core::ptr::swap_nonoverlapping",
      "llm_generated_comment": "# Safety\n\n- If `size_of::<T>() != 0` and `count != 0`, `x` and `y` must each be non-null and properly aligned for `T`.\n- If `size_of::<T>() != 0` and `count != 0`, the memory ranges beginning at `x` and `y` and spanning `count` consecutive `T` values must each be valid for reads and writes for the duration of the call.\n- The memory ranges beginning at `x` and `y` and spanning `count` consecutive `T` values must not overlap.",
      "diff": "Incorrect\n1. Generated makes alignment conditional on nonzero element size and count, but ORIGINAL requires alignment even when the effectively copied size is zero.\n2. Generated limits read/write validity to nonzero size and count and adds a duration-of-call qualifier, changing ORIGINAL's unconditional validity requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::swap_nonoverlapping",
        "classification": "Incorrect",
        "items": [
          "Generated makes alignment conditional on nonzero element size and count, but ORIGINAL requires alignment even when the effectively copied size is zero.",
          "Generated limits read/write validity to nonzero size and count and adds a duration-of-call qualifier, changing ORIGINAL's unconditional validity requirement."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* Both `x` and `y` must be [valid] for both reads and writes of `count *\n  size_of::<T>()` bytes.\n\n* Both `x` and `y` must be properly aligned.\n\n* The region of memory beginning at `x` with a size of `count *\n  size_of::<T>()` bytes must *not* overlap with the region of memory\n  beginning at `y` with the same size.\n\nNote that even if the effectively copied size (`count * size_of::<T>()`) is `0`,\nthe pointers must be properly aligned.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::write",
      "source_api_path": "core::ptr::write",
      "llm_generated_comment": "# Safety\n\n- `dst` must be valid for writing a `T`: it must have valid provenance for writable memory covering `size_of::<T>()` bytes, and the write must not violate Rust’s aliasing rules.\n- `dst` must be properly aligned for `T`.\n- Even when `T` is zero-sized, `dst` must be non-null and properly aligned.",
      "diff": "Incorrect\n1. Generated requires `dst` to be non-null for zero-sized `T`, but ORIGINAL requires only proper alignment when `T` is zero-sized.\n2. Generated adds specific provenance, writable-byte coverage, and aliasing requirements not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::write",
        "classification": "Incorrect",
        "items": [
          "Generated requires `dst` to be non-null for zero-sized `T`, but ORIGINAL requires only proper alignment when `T` is zero-sized.",
          "Generated adds specific provenance, writable-byte coverage, and aliasing requirements not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dst` must be [valid] for writes or `T` must be a ZST.\n\n* `dst` must be properly aligned. Use [`write_unaligned`] if this is not the\n  case.\n\nNote that even if `T` has size `0`, the pointer must be properly aligned.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::write_bytes",
      "source_api_path": "core::ptr::write_bytes",
      "llm_generated_comment": "# Safety\n\n- If `T` is not a zero-sized type and `count` is nonzero, `dst` must be non-null and properly aligned for `T`.\n- The range of `count * size_of::<T>()` bytes starting at `dst` must be valid for writes for the duration of the operation. When `T` is zero-sized or `count` is zero, no backing memory is required.\n- The bytes written to each `T` must be a valid representation of `T`; otherwise, using the resulting values can cause undefined behavior.",
      "diff": "Incorrect\n1. Generated exempts zero-sized types and zero counts from alignment, but ORIGINAL requires `dst` to be properly aligned even when the written size is zero.\n2. Generated makes valid representation of `T` a precondition of the function, while ORIGINAL says invalid representations may cause undefined behavior only when used later.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::write_bytes",
        "classification": "Incorrect",
        "items": [
          "Generated exempts zero-sized types and zero counts from alignment, but ORIGINAL requires `dst` to be properly aligned even when the written size is zero.",
          "Generated makes valid representation of `T` a precondition of the function, while ORIGINAL says invalid representations may cause undefined behavior only when used later."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dst` must be [valid] for writes of `count * size_of::<T>()` bytes.\n\n* `dst` must be properly aligned.\n\nNote that even if the effectively copied size (`count * size_of::<T>()`) is\n`0`, the pointer must be properly aligned.\n\nAdditionally, note that changing `*dst` in this way can easily lead to undefined behavior (UB)\nlater if the written bytes are not a valid representation of some `T`. For instance, the\nfollowing is an **incorrect** use of this function:\n\n```rust,no_run\nunsafe {\n    let mut value: u8 = 0;\n    let ptr: *mut bool = &mut value as *mut u8 as *mut bool;\n    let _bool = ptr.read(); // This is fine, `ptr` points to a valid `bool`.\n    ptr.write_bytes(42u8, 1); // This function itself does not cause UB...\n    let _bool = ptr.read(); // ...but it makes this operation UB! ⚠️\n}\n```\n\n[valid]: crate::ptr#safety"
    },
    {
      "target_api_path": "core::ptr::write_unaligned",
      "source_api_path": "core::ptr::write_unaligned",
      "llm_generated_comment": "# Safety\n\n`dst` must be valid for writing `size_of::<T>()` bytes. It need not be aligned for `T`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::ptr::write_unaligned",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dst` must be [valid] for writes.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::ptr::write_volatile",
      "source_api_path": "core::ptr::write_volatile",
      "llm_generated_comment": "# Safety\n\n- `dst` must be properly aligned for `T`.\n- If `dst` points into a Rust allocation, the entire `size_of::<T>()` byte range beginning at `dst` must be valid for writes.\n- If `dst` points outside all Rust allocations, a volatile write of `size_of::<T>()` bytes at that address must not trap or otherwise be invalid for the target, and it must not modify memory belonging to any Rust allocation.",
      "diff": "Incorrect\n1. Generated requires the byte range to be valid for writes when `dst` is inside an allocation, omitting ORIGINAL's exception allowing any destination when `T` is a ZST.\n2. Generated adds that an outside-allocation write must not be otherwise invalid for the target; ORIGINAL requires only that it not trap and not modify Rust allocation memory.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::ptr::write_volatile",
        "classification": "Incorrect",
        "items": [
          "Generated requires the byte range to be valid for writes when `dst` is inside an allocation, omitting ORIGINAL's exception allowing any destination when `T` is a ZST.",
          "Generated adds that an outside-allocation write must not be otherwise invalid for the target; ORIGINAL requires only that it not trap and not modify Rust allocation memory."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dst` must be either [valid] for writes, or `T` must be a ZST, or `dst` must point to memory\n  outside of all Rust allocations and writing to that memory must:\n  - not trap, and\n  - not cause any memory inside a Rust allocation to be modified.\n\n* `dst` must be properly aligned.\n\nNote that even if `T` has size `0`, the pointer must be properly aligned.\n\n[valid]: self#safety"
    },
    {
      "target_api_path": "core::range::Range::get_unchecked",
      "source_api_path": "core::range::Range::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`, and `self.start <= self.end <= slice.len()` must hold. Both `self.start` and `self.end` must lie on UTF-8 character boundaries of that string.",
      "diff": "Incorrect\n1. GENERATED requires a valid `str` and UTF-8 character boundaries, adding conditions beyond ORIGINAL's non-dangling pointer and in-bounds index requirements.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::Range::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED requires a valid `str` and UTF-8 character boundaries, adding conditions beyond ORIGINAL's non-dangling pointer and in-bounds index requirements."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::Range::get_unchecked_mut",
      "source_api_path": "core::range::Range::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must describe a valid string allocation, and `self.start <= self.end <= slice.len()` must hold. Both `self.start` and `self.end` must lie on UTF-8 character boundaries of the string.\n\nAny dereference or mutable access through the returned pointer additionally requires that the selected bytes remain allocated and initialized as valid UTF-8 and that the caller has exclusive access to them for the duration of that access.",
      "diff": "Incorrect\n1. Generated requires a valid string allocation and UTF-8 character boundaries, adding unsupported string-specific preconditions beyond the original dangling-pointer and bounds conditions.\n2. Generated limits validity requirements to dereference or mutable access, whereas the original makes out-of-bounds indices and dangling slice pointers undefined even when the returned pointer is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::Range::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires a valid string allocation and UTF-8 character boundaries, adding unsupported string-specific preconditions beyond the original dangling-pointer and bounds conditions.",
          "Generated limits validity requirements to dereference or mutable access, whereas the original makes out-of-bounds indices and dangling slice pointers undefined even when the returned pointer is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeFrom::get_unchecked",
      "source_api_path": "core::range::RangeFrom::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str` slice with valid metadata and an allocation sufficient for its reported length.\n- `self` must designate a non-reversed range wholly within `slice`.\n- Every bound of `self` that cuts into `slice` must lie on a UTF-8 character boundary.",
      "diff": "Incorrect\n1. Generated adds unsupported requirements for a valid `str` allocation, non-reversed range, and UTF-8 boundaries; ORIGINAL requires only in-bounds indexing and a non-dangling `slice` pointer.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeFrom::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported requirements for a valid `str` allocation, non-reversed range, and UTF-8 boundaries; ORIGINAL requires only in-bounds indexing and a non-dangling `slice` pointer."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeFrom::get_unchecked_mut",
      "source_api_path": "core::range::RangeFrom::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid, properly aligned pointer to a valid `str` slice, and the slice's backing memory must remain valid and writable for as long as the returned pointer is used.\n- The range specified by `self` must be within `slice`; in particular, `start <= end` and `end <= slice.len()`.\n- The range's start and end must lie on UTF-8 character boundaries of `slice`, so that the resulting subslice is valid UTF-8.\n- The returned pointer must be used only in accordance with Rust's aliasing and mutability rules.",
      "diff": "Incorrect\n1. The generated lifetime condition makes validity depend on using the returned pointer, whereas ORIGINAL says a dangling slice pointer is undefined behavior even when the result is unused.\n2. The generated requirement that the range satisfy `start <= end` and `end <= slice.len()` adds an unsupported end-bound condition to ORIGINAL's out-of-bounds index requirement.\n3. The generated UTF-8 character-boundary requirement is an unsupported additional condition not present in ORIGINAL.\n4. The generated aliasing and mutability requirement is an unsupported additional condition not present in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeFrom::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated lifetime condition makes validity depend on using the returned pointer, whereas ORIGINAL says a dangling slice pointer is undefined behavior even when the result is unused.",
          "The generated requirement that the range satisfy `start <= end` and `end <= slice.len()` adds an unsupported end-bound condition to ORIGINAL's out-of-bounds index requirement.",
          "The generated UTF-8 character-boundary requirement is an unsupported additional condition not present in ORIGINAL.",
          "The generated aliasing and mutability requirement is an unsupported additional condition not present in ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeInclusive::get_unchecked",
      "source_api_path": "core::range::RangeInclusive::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str`, with accurate length metadata and an allocation covering its bytes.\n- `self.start <= self.end <= (*slice).len()` must hold.\n- Both `self.start` and `self.end` must lie on UTF-8 character boundaries of the pointed-to string.",
      "diff": "Incorrect\n1. The generated text requires `slice` to be a valid `str` with accurate metadata and allocated bytes, adding conditions beyond ORIGINAL's dangling-pointer requirement.\n2. The generated text requires both endpoints to be UTF-8 boundaries, an unsupported additional safety condition.\n3. The generated bound permits `self.end == (*slice).len()`, whereas an inclusive range ending at the length is out of bounds.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeInclusive::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated text requires `slice` to be a valid `str` with accurate metadata and allocated bytes, adding conditions beyond ORIGINAL's dangling-pointer requirement.",
          "The generated text requires both endpoints to be UTF-8 boundaries, an unsupported additional safety condition.",
          "The generated bound permits `self.end == (*slice).len()`, whereas an inclusive range ending at the length is out of bounds."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeInclusive::get_unchecked_mut",
      "source_api_path": "core::range::RangeInclusive::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must describe a valid `str` allocation and have provenance suitable for pointer arithmetic over its entire byte length.\n- The range’s start and end byte offsets must satisfy `start <= end <= slice.len()`.\n- Both offsets must lie on UTF-8 code point boundaries within `slice`.",
      "diff": "Incorrect\n1. The generated UTF-8 code point boundary requirement is an additional safety condition absent from the original.\n2. The generated provenance requirement extends beyond the original, which only identifies dangling slice pointers as unsafe.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeInclusive::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated UTF-8 code point boundary requirement is an additional safety condition absent from the original.",
          "The generated provenance requirement extends beyond the original, which only identifies dangling slice pointers as unsafe."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeToInclusive::get_unchecked",
      "source_api_path": "core::range::RangeToInclusive::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range represented by `self` must satisfy `self.start <= self.end <= slice.len()`, and both bounds must lie on UTF-8 character boundaries of that string. The allocation backing `slice` must remain valid for any subsequent use of the returned pointer.",
      "diff": "Incorrect\n1. The generated text requires the backing allocation to remain valid only for subsequent pointer use, but ORIGINAL requires a non-dangling slice even when the returned pointer is unused.\n2. The generated text adds UTF-8 character-boundary requirements, which are not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeToInclusive::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated text requires the backing allocation to remain valid only for subsequent pointer use, but ORIGINAL requires a non-dangling slice even when the returned pointer is unused.",
          "The generated text adds UTF-8 character-boundary requirements, which are not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::range::RangeToInclusive::get_unchecked_mut",
      "source_api_path": "core::range::RangeToInclusive::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid, non-null, properly aligned pointer to a valid `str` slice for the duration of this call.\n- `self.start..=self.end` must denote a range within `slice`: `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` must both lie on UTF-8 character boundaries of `slice`, so that the resulting subslice is valid UTF-8.\n- The returned pointer must be used only while the referenced bytes remain valid and without violating Rust's aliasing rules for mutable references.",
      "diff": "Incorrect\n1. The returned pointer is restricted by lifetime and aliasing requirements that are not stated in ORIGINAL.\n2. The generated validity requirements for non-nullness, alignment, UTF-8 boundaries, and range ordering add conditions absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::range::RangeToInclusive::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The returned pointer is restricted by lifetime and aliasing requirements that are not stated in ORIGINAL.",
          "The generated validity requirements for non-nullness, alignment, UTF-8 boundaries, and range ordering add conditions absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::result::Result::unwrap_err_unchecked",
      "source_api_path": "core::result::Result::unwrap_err_unchecked",
      "llm_generated_comment": "# Safety\n\nThe `Result` must be `Err`; calling this method on an `Ok` value is undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::result::Result::unwrap_err_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this method on an [`Ok`] is *[undefined behavior]*.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::result::Result::unwrap_unchecked",
      "source_api_path": "core::result::Result::unwrap_unchecked",
      "llm_generated_comment": "# Safety\n\n`self` must be the [`Ok`] variant. Calling this function on an [`Err`] value results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::result::Result::unwrap_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this method on an [`Err`] is *[undefined behavior]*.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::simd::num::float::SimdFloat::to_int_unchecked",
      "source_api_path": "core::simd::num::float::SimdFloat::to_int_unchecked",
      "llm_generated_comment": "# Safety\n\n- Every floating-point lane in `self` must be finite and within the range representable by the corresponding integer type `Int`. In particular, no lane may be `NaN`, positive or negative infinity, or outside the bounds of `Int`.",
      "diff": "Incorrect\n1. Generated requires each lane's untruncated value to lie within Int's bounds, while ORIGINAL permits fractional values whose truncated result is representable.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::simd::num::float::SimdFloat::to_int_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires each lane's untruncated value to lie within Int's bounds, while ORIGINAL permits fractional values whose truncated result is representable."
        ]
      },
      "original_safety_doc": "The value must:\n\n* Not be NaN\n* Not be infinite\n* Be representable in the return type, after truncating off its fractional part\n\nIf these requirements are infeasible or costly, consider using the safe function [cast],\nwhich saturates on conversion.\n\n[cast]: Simd::cast"
    },
    {
      "target_api_path": "core::slice::GetDisjointMutIndex",
      "source_api_path": "core::slice::GetDisjointMutIndex",
      "llm_generated_comment": "# Safety\n\nImplementors must ensure that:\n\n- `is_in_bounds(len)` returns `true` only when `self` can be safely used to index a slice containing `len` elements.\n- `is_overlapping(other)` returns `false` only when `self` and `other` cannot select any of the same slice elements. Zero-length ranges must be classified according to the documented convention: they do not overlap at either endpoint, but do overlap when located within the other range.\n- Cloning an index preserves the slice region it denotes, so the results of these checks remain valid for the clone.",
      "diff": "Incorrect\n1. Generated adds unsupported obligations about cloning and zero-length overlap classification beyond the original safety condition.\n2. Generated changes the original combined sufficiency condition into separate implementor predicates, altering the documented safety obligation.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::GetDisjointMutIndex",
        "classification": "Incorrect",
        "items": [
          "Generated adds unsupported obligations about cloning and zero-length overlap classification beyond the original safety condition.",
          "Generated changes the original combined sufficiency condition into separate implementor predicates, altering the documented safety obligation."
        ]
      },
      "original_safety_doc": "If `is_in_bounds()` returns `true` and `is_overlapping()` returns `false`,\nit must be safe to index the slice with the indices."
    },
    {
      "target_api_path": "core::slice::align_to",
      "source_api_path": "core::slice::align_to",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that the portion of `self` returned as the middle `&[U]` can be safely reinterpreted as values of type `U`, including that every resulting `U` value is valid for the duration of the returned slice.",
      "diff": "Incorrect\n1. Generated adds that every resulting U value must be valid, while ORIGINAL only incorporates transmute's general caveats without specifying this condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::align_to",
        "classification": "Incorrect",
        "items": [
          "Generated adds that every resulting U value must be valid, while ORIGINAL only incorporates transmute's general caveats without specifying this condition."
        ]
      },
      "original_safety_doc": "This method is essentially a `transmute` with respect to the elements in the returned\nmiddle slice, so all the usual caveats pertaining to `transmute::<T, U>` also apply here."
    },
    {
      "target_api_path": "core::slice::align_to_mut",
      "source_api_path": "core::slice::align_to_mut",
      "llm_generated_comment": "# Safety\n\nThe portion of `self` that is returned as the middle `&mut [U]` must be safely reinterpretable as values of type `U`. In particular, it must be initialized and contain valid `U` values whenever they are read, and writes through the returned `&mut [U]` must not create invalid values or otherwise violate the validity, ownership, or destruction invariants of the original `T` elements or of `U`.",
      "diff": "Incorrect\n1. Generated permits the middle slice to be uninitialized or invalid when not read, whereas the transmute caveats require the reinterpreted values to satisfy U's validity requirements.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::align_to_mut",
        "classification": "Incorrect",
        "items": [
          "Generated permits the middle slice to be uninitialized or invalid when not read, whereas the transmute caveats require the reinterpreted values to satisfy U's validity requirements."
        ]
      },
      "original_safety_doc": "This method is essentially a `transmute` with respect to the elements in the returned\nmiddle slice, so all the usual caveats pertaining to `transmute::<T, U>` also apply here."
    },
    {
      "target_api_path": "core::slice::as_ascii_unchecked",
      "source_api_path": "core::slice::as_ascii_unchecked",
      "llm_generated_comment": "# Safety\n\nThe bytes of `self` must all be valid ASCII bytes for the entire lifetime of the returned slice.",
      "diff": "Incorrect\n1. The generated condition adds a requirement that the bytes remain valid ASCII for the returned slice's entire lifetime, beyond ORIGINAL's per-call byte precondition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::as_ascii_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition adds a requirement that the bytes remain valid ASCII for the returned slice's entire lifetime, beyond ORIGINAL's per-call byte precondition."
        ]
      },
      "original_safety_doc": "Every byte in the slice must be in `0..=127`, or else this is UB."
    },
    {
      "target_api_path": "core::slice::as_chunks_unchecked",
      "source_api_path": "core::slice::as_chunks_unchecked",
      "llm_generated_comment": "# Safety\n\n`N` must be non-zero, and `self.len()` must be exactly divisible by `N`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::as_chunks_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This may only be called when\n- The slice splits exactly into `N`-element chunks (aka `self.len() % N == 0`).\n- `N != 0`."
    },
    {
      "target_api_path": "core::slice::as_chunks_unchecked_mut",
      "source_api_path": "core::slice::as_chunks_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `N` must be nonzero.\n- The length of the slice must be exactly divisible by `N`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::as_chunks_unchecked_mut",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This may only be called when\n- The slice splits exactly into `N`-element chunks (aka `self.len() % N == 0`).\n- `N != 0`."
    },
    {
      "target_api_path": "core::slice::assume_init_drop",
      "source_api_path": "core::slice::assume_init_drop",
      "llm_generated_comment": "# Safety\n\nEvery element of `self` must be initialized as a valid value of type `T`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::assume_init_drop",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "It is up to the caller to guarantee that every `MaybeUninit<T>` in the slice\nreally is in an initialized state. Calling this when the content is not yet\nfully initialized causes undefined behavior.\n\nOn top of that, all additional invariants of the type `T` must be\nsatisfied, as the `Drop` implementation of `T` (or its members) may\nrely on this. For example, setting a `Vec<T>` to an invalid but\nnon-null address makes it initialized (under the current implementation;\nthis does not constitute a stable guarantee), because the only\nrequirement the compiler knows about it is that the data pointer must be\nnon-null. Dropping such a `Vec<T>` however will cause undefined\nbehaviour."
    },
    {
      "target_api_path": "core::slice::assume_init_mut",
      "source_api_path": "core::slice::assume_init_mut",
      "llm_generated_comment": "# Safety\n\n`self` must contain a fully initialized, valid value of type `T`. This requirement must already hold when the function is called; the returned reference must not be used to initialize an otherwise uninitialized value.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::assume_init_mut",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this when the content is not yet fully initialized causes undefined\nbehavior: it is up to the caller to guarantee that every `MaybeUninit<T>` in the\nslice really is in an initialized state. For instance, `.assume_init_mut()` cannot\nbe used to initialize a `MaybeUninit` slice."
    },
    {
      "target_api_path": "core::slice::assume_init_ref",
      "source_api_path": "core::slice::assume_init_ref",
      "llm_generated_comment": "# Safety\n\n- All elements of the slice must be initialized with valid values of type `T` before calling this function.\n- Those elements must remain initialized and valid as `T` for the entire lifetime of the returned slice.",
      "diff": "Incorrect\n1. Generated requires elements to remain initialized and valid for the returned slice's entire lifetime, while ORIGINAL only requires initialization when called.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::assume_init_ref",
        "classification": "Incorrect",
        "items": [
          "Generated requires elements to remain initialized and valid for the returned slice's entire lifetime, while ORIGINAL only requires initialization when called."
        ]
      },
      "original_safety_doc": "Calling this when the content is not yet fully initialized causes undefined\nbehavior: it is up to the caller to guarantee that every `MaybeUninit<T>` in\nthe slice really is in an initialized state."
    },
    {
      "target_api_path": "core::slice::clone_to_uninit",
      "source_api_path": "core::slice::clone_to_uninit",
      "llm_generated_comment": "# Safety\n\n- `dest` must be non-null, properly aligned for the value being cloned, and valid for writes to the entire destination allocation required to hold a clone of `self`, including any metadata represented by the slice.\n- The destination region must be uninitialized storage suitable for receiving the clone, and it must not overlap the memory occupied by `self`.\n- The destination must remain valid and appropriately allocated for the duration of the operation.",
      "diff": "Incorrect\n1. Generated requires `dest` to be non-null, but ORIGINAL only requires validity for writes and proper alignment.\n2. Generated requires destination storage to be uninitialized and non-overlapping, but ORIGINAL states neither condition.\n3. Generated imposes a duration and allocation requirement, but ORIGINAL specifies no such lifetime or allocation condition.\n4. Generated requires alignment for the value being cloned, rather than `align_of_val(self)` as stated by ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::clone_to_uninit",
        "classification": "Incorrect",
        "items": [
          "Generated requires `dest` to be non-null, but ORIGINAL only requires validity for writes and proper alignment.",
          "Generated requires destination storage to be uninitialized and non-overlapping, but ORIGINAL states neither condition.",
          "Generated imposes a duration and allocation requirement, but ORIGINAL specifies no such lifetime or allocation condition.",
          "Generated requires alignment for the value being cloned, rather than `align_of_val(self)` as stated by ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dest` must be [valid] for writes for `size_of_val(self)` bytes.\n* `dest` must be properly aligned to `align_of_val(self)`.\n\n[valid]: crate::ptr#safety\n[pointer metadata]: crate::ptr::metadata()"
    },
    {
      "target_api_path": "core::slice::from_mut_ptr_range",
      "source_api_path": "core::slice::from_mut_ptr_range",
      "llm_generated_comment": "# Safety\n\n- `range.start` and `range.end` must be properly aligned pointers into the same allocated object, or one byte past that object, with `range.end` not before `range.start`.\n- `range.start` must be non-null and properly aligned even when the resulting slice is empty.\n- The half-open range `range.start..range.end` must contain a whole number of `T` values, and its total size in bytes must not exceed `isize::MAX`.\n- Every element in that range must be initialized and contain a valid value of type `T`.\n- The memory covered by the range must remain allocated and valid for reads and writes for the entire lifetime `'a`.\n- For the entire lifetime `'a`, the covered memory must not be accessed through any other pointer or reference except through pointers derived from the returned mutable slice.",
      "diff": "Incorrect\n1. The generated text permits `range.start` to be one byte past the allocated object, whereas ORIGINAL requires it to point to the first slice element.\n2. The generated text replaces the required no-address-wrap condition with only a size bound, omitting the requirement that adding the size to `start` must not wrap.\n3. The generated text requires the range to remain allocated and valid for reads and writes for lifetime `'a`, an additional condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::from_mut_ptr_range",
        "classification": "Incorrect",
        "items": [
          "The generated text permits `range.start` to be one byte past the allocated object, whereas ORIGINAL requires it to point to the first slice element.",
          "The generated text replaces the required no-address-wrap condition with only a size bound, omitting the requirement that adding the size to `start` must not wrap.",
          "The generated text requires the range to remain allocated and valid for reads and writes for lifetime `'a`, an additional condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* The `start` pointer of the range must be a non-null, [valid] and properly aligned pointer\n  to the first element of a slice.\n\n* The `end` pointer must be a [valid] and properly aligned pointer to *one past*\n  the last element, such that the offset from the end to the start pointer is\n  the length of the slice.\n\n* The entire memory range of this slice must be contained within a single allocation!\n  Slices can never span across multiple allocations.\n\n* The range must contain `N` consecutive properly initialized values of type `T`.\n\n* The memory referenced by the returned slice must not be accessed through any other pointer\n  (not derived from the return value) for the duration of lifetime `'a`.\n  Both read and write accesses are forbidden.\n\n* The total length of the range must be no larger than `isize::MAX`,\n  and adding that size to `start` must not \"wrap around\" the address space.\n  See the safety documentation of [`pointer::offset`].\n\nNote that a range created from [`slice::as_mut_ptr_range`] fulfills these requirements."
    },
    {
      "target_api_path": "core::slice::from_ptr_range",
      "source_api_path": "core::slice::from_ptr_range",
      "llm_generated_comment": "# Safety\n\n- `range.start` must be non-null and properly aligned for `T`, even when the range is empty.\n- `range.end` must be at or after `range.start` and must be derived from the same allocation as `range.start` (or be the corresponding one-past-the-end pointer). The range must not wrap around the address space.\n- The range must contain only initialized values of type `T`, and the entire resulting slice must remain valid for the lifetime `'a`.\n- The total size of the slice, `size_of::<T>() * (range.end.offset_from(range.start) as usize)`, must not exceed `isize::MAX`.\n- The memory covered by the slice must not be mutated in a way that invalidates the returned shared reference for the duration of `'a`, except through permitted interior mutability such as `UnsafeCell`.",
      "diff": "Incorrect\n1. The generated text does not require `range.end` to be properly aligned for `T`.\n2. The generated size bound checks total bytes, while ORIGINAL bounds the range length and separately requires pointer addition not to wrap.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::from_ptr_range",
        "classification": "Incorrect",
        "items": [
          "The generated text does not require `range.end` to be properly aligned for `T`.",
          "The generated size bound checks total bytes, while ORIGINAL bounds the range length and separately requires pointer addition not to wrap."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* The `start` pointer of the range must be a non-null, [valid] and properly aligned pointer\n  to the first element of a slice.\n\n* The `end` pointer must be a [valid] and properly aligned pointer to *one past*\n  the last element, such that the offset from the end to the start pointer is\n  the length of the slice.\n\n* The entire memory range of this slice must be contained within a single allocation!\n  Slices can never span across multiple allocations.\n\n* The range must contain `N` consecutive properly initialized values of type `T`.\n\n* The memory referenced by the returned slice must not be mutated for the duration\n  of lifetime `'a`, except inside an `UnsafeCell`.\n\n* The total length of the range must be no larger than `isize::MAX`,\n  and adding that size to `start` must not \"wrap around\" the address space.\n  See the safety documentation of [`pointer::offset`].\n\nNote that a range created from [`slice::as_ptr_range`] fulfills these requirements."
    },
    {
      "target_api_path": "core::slice::from_raw_parts",
      "source_api_path": "core::slice::from_raw_parts",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that:\n\n- `data` is non-null and properly aligned for `T`, even when `len == 0` or `T` is zero-sized.\n- `data` points to `len` consecutive, properly initialized values of type `T`, all contained within a single allocated object.\n- The memory range occupied by those values is valid for reads for the entire lifetime `'a`.\n- That memory is not mutated for the entire lifetime `'a`, except through an `UnsafeCell`.\n- `len * size_of::<T>()` does not exceed `isize::MAX`, and computing the end of the range from `data` does not wrap around the address space.",
      "diff": "Incorrect\n1. The generated condition requires the values to remain valid for reads for the entire lifetime, while ORIGINAL only requires the range to be valid for reads for the specified byte count.\n2. The generated condition requires containment within a single allocated object, while ORIGINAL requires containment within a single allocation.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::from_raw_parts",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires the values to remain valid for reads for the entire lifetime, while ORIGINAL only requires the range to be valid for reads for the specified byte count.",
          "The generated condition requires containment within a single allocated object, while ORIGINAL requires containment within a single allocation."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `data` must be non-null, [valid] for reads for `len * size_of::<T>()` many bytes,\n  and it must be properly aligned. This means in particular:\n\n    * The entire memory range of this slice must be contained within a single allocation!\n      Slices can never span across multiple allocations. See [below](#incorrect-usage)\n      for an example incorrectly not taking this into account.\n    * `data` must be non-null and aligned even for zero-length slices or slices of ZSTs. One\n      reason for this is that enum layout optimizations may rely on references\n      (including slices of any length) being aligned and non-null to distinguish\n      them from other data. You can obtain a pointer that is usable as `data`\n      for zero-length slices using [`NonNull::dangling()`].\n\n* `data` must point to `len` consecutive properly initialized values of type `T`.\n\n* The memory referenced by the returned slice must not be mutated for the duration\n  of lifetime `'a`, except inside an `UnsafeCell`.\n\n* The total size `len * size_of::<T>()` of the slice must be no larger than `isize::MAX`,\n  and adding that size to `data` must not \"wrap around\" the address space.\n  See the safety documentation of [`pointer::offset`]."
    },
    {
      "target_api_path": "core::slice::from_raw_parts_mut",
      "source_api_path": "core::slice::from_raw_parts_mut",
      "llm_generated_comment": "# Safety\n\n- `data` must be non-null and properly aligned for `T`, even when `len` is zero.\n- `data` must point to `len` consecutive properly initialized values of type `T` within a single allocation. The entire region must be valid for reads and writes for the duration of `'a`, and the allocation must remain valid for that duration.\n- The total size of the slice, `len * size_of::<T>()`, must not exceed `isize::MAX`, and the region must not wrap around the address space.\n- For the duration of `'a`, the memory referenced by the returned slice must not be accessed through any other pointer or reference, and no other reference may be used to access or mutate it.",
      "diff": "Incorrect\n1. Generated requires the allocation to remain valid for `'a`, an additional condition beyond ORIGINAL's pointer validity and range requirements.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::from_raw_parts_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires the allocation to remain valid for `'a`, an additional condition beyond ORIGINAL's pointer validity and range requirements."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `data` must be non-null, [valid] for both reads and writes for `len * size_of::<T>()` many bytes,\n  and it must be properly aligned. This means in particular:\n\n    * The entire memory range of this slice must be contained within a single allocation!\n      Slices can never span across multiple allocations.\n    * `data` must be non-null and aligned even for zero-length slices or slices of ZSTs. One\n      reason for this is that enum layout optimizations may rely on references\n      (including slices of any length) being aligned and non-null to distinguish\n      them from other data. You can obtain a pointer that is usable as `data`\n      for zero-length slices using [`NonNull::dangling()`].\n\n* `data` must point to `len` consecutive properly initialized values of type `T`.\n\n* The memory referenced by the returned slice must not be accessed through any other pointer\n  (not derived from the return value) for the duration of lifetime `'a`.\n  Both read and write accesses are forbidden.\n\n* The total size `len * size_of::<T>()` of the slice must be no larger than `isize::MAX`,\n  and adding that size to `data` must not \"wrap around\" the address space.\n  See the safety documentation of [`pointer::offset`].\n\n[valid]: ptr#safety\n[`NonNull::dangling()`]: ptr::NonNull::dangling"
    },
    {
      "target_api_path": "core::slice::get_disjoint_unchecked_mut",
      "source_api_path": "core::slice::get_disjoint_unchecked_mut",
      "llm_generated_comment": "# Safety\n\nEvery index in `indices` must be in bounds of `self`, and the portions of `self` selected by the indices must be pairwise disjoint. This requirement applies to all indices, including indices that select ranges or other potentially multi-element outputs.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::get_disjoint_unchecked_mut",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this method with overlapping or out-of-bounds indices is *[undefined behavior]*\neven if the resulting references are not used."
    },
    {
      "target_api_path": "core::slice::get_unchecked",
      "source_api_path": "core::slice::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range `self.start..self.end` must satisfy `self.start <= self.end <= slice.len()`, and both endpoints must lie on UTF-8 character boundaries of that string. The allocation backing `slice` must remain valid for any subsequent use of the returned pointer.",
      "diff": "Incorrect\n1. Generated requires a valid str and UTF-8 boundaries, while ORIGINAL requires only an in-bounds index for the slice.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires a valid str and UTF-8 boundaries, while ORIGINAL requires only an in-bounds index for the slice."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index is *[undefined behavior]*\neven if the resulting reference is not used.\n\nYou can think of this like `.get(index).unwrap_unchecked()`.  It's UB\nto call `.get_unchecked(len)`, even if you immediately convert to a\npointer.  And it's UB to call `.get_unchecked(..len + 1)`,\n`.get_unchecked(..=len)`, or similar.\n\n[`get`]: slice::get\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::get_unchecked_mut",
      "source_api_path": "core::slice::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n- `slice` must point to a valid `str` slice, and `self.start..self.end` must be within that slice (`self.start <= self.end` and `self.end <= slice.len()`).\n- The returned pointer must only be used while the underlying slice remains valid and mutable, and without any conflicting access to the same bytes.",
      "diff": "Incorrect\n1. Generated requires `slice` to point to a valid `str` slice, but ORIGINAL imposes no `str`-specific condition.\n2. Generated requires `self.start..self.end` bounds, changing the API's out-of-bounds index condition to an unsupported range predicate.\n3. Generated requires restricted lifetime and nonconflicting mutable use of the returned pointer, but ORIGINAL only requires an in-bounds call.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires `slice` to point to a valid `str` slice, but ORIGINAL imposes no `str`-specific condition.",
          "Generated requires `self.start..self.end` bounds, changing the API's out-of-bounds index condition to an unsupported range predicate.",
          "Generated requires restricted lifetime and nonconflicting mutable use of the returned pointer, but ORIGINAL only requires an in-bounds call."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index is *[undefined behavior]*\neven if the resulting reference is not used.\n\nYou can think of this like `.get_mut(index).unwrap_unchecked()`.  It's\nUB to call `.get_unchecked_mut(len)`, even if you immediately convert\nto a pointer.  And it's UB to call `.get_unchecked_mut(..len + 1)`,\n`.get_unchecked_mut(..=len)`, or similar.\n\n[`get_mut`]: slice::get_mut\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::index::SliceIndex::get_unchecked",
      "source_api_path": "core::slice::index::SliceIndex::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range `self.start..self.end` must satisfy `self.start <= self.end <= (*slice).len()`, and both bounds must lie on UTF-8 character boundaries of that string. These requirements apply even if the returned pointer is never dereferenced.",
      "diff": "Incorrect\n1. The generated text requires UTF-8 character boundaries, an additional condition absent from the original.\n2. The generated text specifies a range and length predicate instead of the original's general out-of-bounds index condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::index::SliceIndex::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated text requires UTF-8 character boundaries, an additional condition absent from the original.",
          "The generated text specifies a range and length predicate instead of the original's general out-of-bounds index condition."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::index::SliceIndex::get_unchecked_mut",
      "source_api_path": "core::slice::index::SliceIndex::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that:\n\n- `slice` is a valid, non-null pointer to a valid `str` slice, and remains valid for the duration of any use of the returned pointer.\n- `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` are on UTF-8 character boundaries in `slice`, so that the selected range is itself a valid `str`.\n- The returned pointer is used only while the pointed-to range remains valid and mutably accessible, without violating Rust's aliasing rules.",
      "diff": "Incorrect\n1. Generated requires a valid non-null `str` pointer and continued validity, while ORIGINAL specifically prohibits a dangling `slice` pointer regardless of returned-pointer use.\n2. Generated adds UTF-8 boundary requirements, which are unsupported by ORIGINAL.\n3. Generated adds mutable accessibility and aliasing requirements for use of the returned pointer, while ORIGINAL makes out-of-bounds invocation undefined regardless of pointer use.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::slice::index::SliceIndex::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "Generated requires a valid non-null `str` pointer and continued validity, while ORIGINAL specifically prohibits a dangling `slice` pointer regardless of returned-pointer use.",
          "Generated adds UTF-8 boundary requirements, which are unsupported by ORIGINAL.",
          "Generated adds mutable accessibility and aliasing requirements for use of the returned pointer, while ORIGINAL makes out-of-bounds invocation undefined regardless of pointer use."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::split_at_mut_unchecked",
      "source_api_path": "core::slice::split_at_mut_unchecked",
      "llm_generated_comment": "# Safety\n\n`mid` must be less than or equal to `self.len()`.",
      "diff": "Missing\n1. GENERATED omits ORIGINAL's explicit lower-bound condition that `mid` is at least zero.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::slice::split_at_mut_unchecked",
        "classification": "Missing",
        "items": [
          "GENERATED omits ORIGINAL's explicit lower-bound condition that `mid` is at least zero."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index is *[undefined behavior]*\neven if the resulting reference is not used. The caller has to ensure that\n`0 <= mid <= self.len()`.\n\n[`split_at_mut`]: slice::split_at_mut\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::split_at_unchecked",
      "source_api_path": "core::slice::split_at_unchecked",
      "llm_generated_comment": "# Safety\n\n`mid` must be less than or equal to `self.len()`.",
      "diff": "Missing\n1. The generated section omits the ORIGINAL requirement that `mid` be greater than or equal to zero.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::slice::split_at_unchecked",
        "classification": "Missing",
        "items": [
          "The generated section omits the ORIGINAL requirement that `mid` be greater than or equal to zero."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index is *[undefined behavior]*\neven if the resulting reference is not used. The caller has to ensure that\n`0 <= mid <= self.len()`.\n\n[`split_at`]: slice::split_at\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::slice::swap_unchecked",
      "source_api_path": "core::slice::swap_unchecked",
      "llm_generated_comment": "# Safety\n\nBoth `a` and `b` must be less than `self.len()`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::slice::swap_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index is *[undefined behavior]*.\nThe caller has to ensure that `a < self.len()` and `b < self.len()`."
    },
    {
      "target_api_path": "core::str::as_ascii_unchecked",
      "source_api_path": "core::str::as_ascii_unchecked",
      "llm_generated_comment": "# Safety\n\nEvery byte in `self` must be valid ASCII (in the range `0x00..=0x7F`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::str::as_ascii_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Every character in this string must be ASCII, or else this is UB."
    },
    {
      "target_api_path": "core::str::as_bytes_mut",
      "source_api_path": "core::str::as_bytes_mut",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that the bytes are valid UTF-8 whenever the underlying data is accessed through a `str`, and must restore valid UTF-8 before the mutable borrow of the `str` ends.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::str::as_bytes_mut",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The caller must ensure that the content of the slice is valid UTF-8\nbefore the borrow ends and the underlying `str` is used.\n\nUse of a `str` whose contents are not valid UTF-8 is undefined behavior."
    },
    {
      "target_api_path": "core::str::clone_to_uninit",
      "source_api_path": "core::str::clone_to_uninit",
      "llm_generated_comment": "# Safety\n\n`dest` must point to uninitialized, writable storage valid for `self.to_bytes_with_nul().len()` bytes. The destination range must not overlap the memory occupied by `self`.",
      "diff": "Incorrect\n1. Generated requires uninitialized storage, an additional condition not present in ORIGINAL.\n2. Generated uses `self.to_bytes_with_nul().len()` instead of ORIGINAL's `size_of_val(self)` byte count.\n3. Generated omits ORIGINAL's alignment requirement for `dest`.\n4. Generated adds a non-overlap requirement not present in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::str::clone_to_uninit",
        "classification": "Incorrect",
        "items": [
          "Generated requires uninitialized storage, an additional condition not present in ORIGINAL.",
          "Generated uses `self.to_bytes_with_nul().len()` instead of ORIGINAL's `size_of_val(self)` byte count.",
          "Generated omits ORIGINAL's alignment requirement for `dest`.",
          "Generated adds a non-overlap requirement not present in ORIGINAL."
        ]
      },
      "original_safety_doc": "Behavior is undefined if any of the following conditions are violated:\n\n* `dest` must be [valid] for writes for `size_of_val(self)` bytes.\n* `dest` must be properly aligned to `align_of_val(self)`.\n\n[valid]: crate::ptr#safety\n[pointer metadata]: crate::ptr::metadata()"
    },
    {
      "target_api_path": "core::str::from_utf8_unchecked",
      "source_api_path": "core::str::from_utf8_unchecked",
      "llm_generated_comment": "# Safety\n\nThe bytes in `v` must be valid UTF-8.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::str::from_utf8_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "The bytes passed in must be valid UTF-8."
    },
    {
      "target_api_path": "core::str::get_unchecked",
      "source_api_path": "core::str::get_unchecked",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`, and `self.start` and `self.end` must satisfy `self.start <= self.end <= slice.len()`. Both indices must lie on UTF-8 character boundaries of the string.",
      "diff": "Incorrect\n1. Generated requires `slice` to point to a valid `str`, an additional condition not stated in ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::str::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated requires `slice` to point to a valid `str`, an additional condition not stated in ORIGINAL."
        ]
      },
      "original_safety_doc": "Callers of this function are responsible that these preconditions are\nsatisfied:\n\n* The starting index must not exceed the ending index;\n* Indexes must be within bounds of the original slice;\n* Indexes must lie on UTF-8 sequence boundaries.\n\nFailing that, the returned string slice may reference invalid memory or\nviolate the invariants communicated by the `str` type."
    },
    {
      "target_api_path": "core::str::get_unchecked_mut",
      "source_api_path": "core::str::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\n`slice` must point to a valid `str`. The range `self.start..self.end` must be within that string’s byte length (`self.start <= self.end <= len`), and both endpoints must lie on UTF-8 character boundaries. The pointed-to allocation must remain valid for any subsequent use of the returned pointer, and any mutation through it must preserve the UTF-8 validity of the string.",
      "diff": "Incorrect\n1. The generated text adds a requirement that `slice` point to a valid `str`, which is not an ORIGINAL caller precondition.\n2. The generated text adds an allocation-lifetime requirement for subsequent use of the returned pointer, absent from ORIGINAL.\n3. The generated text adds a requirement that mutations preserve UTF-8 validity, beyond ORIGINAL's stated index preconditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::str::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated text adds a requirement that `slice` point to a valid `str`, which is not an ORIGINAL caller precondition.",
          "The generated text adds an allocation-lifetime requirement for subsequent use of the returned pointer, absent from ORIGINAL.",
          "The generated text adds a requirement that mutations preserve UTF-8 validity, beyond ORIGINAL's stated index preconditions."
        ]
      },
      "original_safety_doc": "Callers of this function are responsible that these preconditions are\nsatisfied:\n\n* The starting index must not exceed the ending index;\n* Indexes must be within bounds of the original slice;\n* Indexes must lie on UTF-8 sequence boundaries.\n\nFailing that, the returned string slice may reference invalid memory or\nviolate the invariants communicated by the `str` type."
    },
    {
      "target_api_path": "core::str::next_code_point",
      "source_api_path": "core::str::next_code_point",
      "llm_generated_comment": "# Safety\n\nIf the first byte yielded is non-ASCII, the iterator must yield enough additional bytes for the encoded sequence: at least one additional byte when the first byte is `0x80..=0xDF`, two when it is `0xE0..=0xEF`, and three when it is `0xF0..=0xFF`. Each required call to `next` must return `Some`; otherwise, the function has undefined behavior.",
      "diff": "Incorrect\n1. The generated condition requires only sufficient byte availability, omitting the original requirement that `bytes` produce a valid UTF-8-like string, including valid continuation bytes.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::str::next_code_point",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires only sufficient byte availability, omitting the original requirement that `bytes` produce a valid UTF-8-like string, including valid continuation bytes."
        ]
      },
      "original_safety_doc": "`bytes` must produce a valid UTF-8-like (UTF-8 or WTF-8) string"
    },
    {
      "target_api_path": "core::str::slice_mut_unchecked",
      "source_api_path": "core::str::slice_mut_unchecked",
      "llm_generated_comment": "# Safety\n\n`begin` must be less than or equal to `end`, and `end` must not exceed `self.len()`. Both `begin` and `end` must lie on UTF-8 character boundaries within `self`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::str::slice_mut_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Callers of this function are responsible that three preconditions are\nsatisfied:\n\n* `begin` must not exceed `end`.\n* `begin` and `end` must be byte positions within the string slice.\n* `begin` and `end` must lie on UTF-8 sequence boundaries."
    },
    {
      "target_api_path": "core::str::slice_unchecked",
      "source_api_path": "core::str::slice_unchecked",
      "llm_generated_comment": "# Safety\n\n`begin` and `end` must be byte indices into `self` such that:\n\n- `begin <= end`;\n- `end <= self.len()`; and\n- both indices lie on UTF-8 character boundaries.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::str::slice_unchecked",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Callers of this function are responsible that three preconditions are\nsatisfied:\n\n* `begin` must not exceed `end`.\n* `begin` and `end` must be byte positions within the string slice.\n* `begin` and `end` must lie on UTF-8 sequence boundaries."
    },
    {
      "target_api_path": "core::sync::atomic::Atomic::from_ptr",
      "source_api_path": "core::sync::atomic::Atomic::from_ptr",
      "llm_generated_comment": "# Safety\n\n`ptr` must be non-null and point to an initialized, contiguous C string terminated by a NUL byte less than `isize::MAX` bytes from `ptr`. The entire range from `ptr` through and including that terminator must be valid for reads and contained within a single allocated object.\n\nThe memory in that range must remain valid and must not be mutated for the entire lifetime `'a` of the returned `CStr`.",
      "diff": "Incorrect\n1. Generated requires `ptr` to be non-null and point to a NUL-terminated C string, which is unsupported by the original safety conditions.\n2. Generated requires the range to be initialized and valid only for reads, whereas the original requires alignment and validity for both reads and writes.\n3. Generated requires the memory to remain unmutated for `'a`, whereas the original requires adherence to the atomic memory model, including synchronization for conflicting accesses.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::sync::atomic::Atomic::from_ptr",
        "classification": "Incorrect",
        "items": [
          "Generated requires `ptr` to be non-null and point to a NUL-terminated C string, which is unsupported by the original safety conditions.",
          "Generated requires the range to be initialized and valid only for reads, whereas the original requires alignment and validity for both reads and writes.",
          "Generated requires the memory to remain unmutated for `'a`, whereas the original requires adherence to the atomic memory model, including synchronization for conflicting accesses."
        ]
      },
      "original_safety_doc": "* `ptr` must be aligned to `align_of::<AtomicPtr<T>>()` (note that on some platforms this\n  can be bigger than `align_of::<*mut T>()`).\n* `ptr` must be [valid] for both reads and writes for the whole lifetime `'a`.\n* You must adhere to the [Memory model for atomic accesses]. In particular, it is not\n  allowed to mix conflicting atomic and non-atomic accesses, or atomic accesses of different\n  sizes, without synchronization.\n\n[valid]: crate::ptr#safety\n[Memory model for atomic accesses]: self#memory-model-for-atomic-accesses"
    },
    {
      "target_api_path": "core::task::wake::LocalWaker::new",
      "source_api_path": "core::task::wake::LocalWaker::new",
      "llm_generated_comment": "# Safety\n\n`data` and `vtable` must together form a valid raw-waker representation. Every operation provided by `vtable` must uphold its contract when invoked with `data`, including for any representations produced by those operations, for as long as the resulting `LocalWaker` or any of its clones may be used or dropped.",
      "diff": "Incorrect\n1. GENERATED adds explicit validity requirements for `data` and `vtable` and extends obligations to derived representations and clone/drop lifetimes beyond ORIGINAL's stated vtable contract.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::task::wake::LocalWaker::new",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds explicit validity requirements for `data` and `vtable` and extends obligations to derived representations and clone/drop lifetimes beyond ORIGINAL's stated vtable contract."
        ]
      },
      "original_safety_doc": "The behavior of the returned `Waker` is undefined if the contract defined\nin [`RawWakerVTable`]'s documentation is not upheld."
    },
    {
      "target_api_path": "core::task::wake::Waker::from_raw",
      "source_api_path": "core::task::wake::Waker::from_raw",
      "llm_generated_comment": "# Safety\n\nThe `RawWaker`’s data pointer and vtable must satisfy the `RawWaker` contract for every operation that may be performed through the returned `LocalWaker`, including cloning, waking, and dropping, for as long as the returned `LocalWaker` or any of its clones may be used.",
      "diff": "Incorrect\n1. Generated changes the returned type from `Waker` to `LocalWaker`, which is not stated in the original safety condition.\n2. Generated adds specific requirements about cloning, waking, dropping, and clone-use duration absent from the original contract.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::task::wake::Waker::from_raw",
        "classification": "Incorrect",
        "items": [
          "Generated changes the returned type from `Waker` to `LocalWaker`, which is not stated in the original safety condition.",
          "Generated adds specific requirements about cloning, waking, dropping, and clone-use duration absent from the original contract."
        ]
      },
      "original_safety_doc": "The behavior of the returned `Waker` is undefined if the contract defined\nin [`RawWaker`]'s and [`RawWakerVTable`]'s documentation is not upheld.\n\n(Authors wishing to avoid unsafe code may implement the [`Wake`] trait instead, at the\ncost of a required heap allocation.)\n\n[`Wake`]: ../../alloc/task/trait.Wake.html"
    },
    {
      "target_api_path": "core::task::wake::Waker::new",
      "source_api_path": "core::task::wake::Waker::new",
      "llm_generated_comment": "# Safety\n\n`data` and `vtable` must together form a valid raw waker:\n\n- `data` must remain valid for use by every function in `vtable` until the corresponding waker is consumed or dropped.\n- The clone function must return another independently valid raw waker.\n- The wake function must safely wake the associated task and consume the waker represented by `data`.\n- The wake-by-reference function must safely wake the associated task without consuming or invalidating that waker.\n- The drop function must safely release the resources associated with that waker exactly once.\n- All vtable functions and the resources referenced by `data` must be safe to use from arbitrary threads, including concurrently, because a `Waker` may be sent to and shared between threads.",
      "diff": "Incorrect\n1. Generated adds thread-safety and concurrent-use obligations absent from the original contract.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::task::wake::Waker::new",
        "classification": "Incorrect",
        "items": [
          "Generated adds thread-safety and concurrent-use obligations absent from the original contract."
        ]
      },
      "original_safety_doc": "The behavior of the returned `Waker` is undefined if the contract defined\nin [`RawWakerVTable`]'s documentation is not upheld.\n\n(Authors wishing to avoid unsafe code may implement the [`Wake`] trait instead, at the\ncost of a required heap allocation.)\n\n[`Wake`]: ../../alloc/task/trait.Wake.html"
    },
    {
      "target_api_path": "core::u128::backward_unchecked",
      "source_api_path": "core::u128::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar values between `char::MIN` and `start`. Equivalently, it must satisfy:\n\n- `count <= start as usize` when `start < '\\u{E000}'`;\n- `count <= (start as usize) - 0x800` when `start >= '\\u{E000}'`.",
      "diff": "Incorrect\n1. The generated condition adds unsupported Unicode-specific bounds on count, whereas ORIGINAL requires only that the operation not overflow Self.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u128::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition adds unsupported Unicode-specific bounds on count, whereas ORIGINAL requires only that the operation not overflow Self."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::u128::forward_unchecked",
      "source_api_path": "core::u128::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` by `count` Unicode scalar values must not pass `char::MAX`; equivalently, the resulting value after skipping the surrogate range must be a valid `char`.",
      "diff": "Incorrect\n1. Generated restricts the operation to advancing Unicode scalar values and `char::MAX`, while ORIGINAL applies to overflow across the range supported by `Self`.\n2. Generated omits the ORIGINAL requirement to guarantee no overflow and use `forward` or `forward_checked` when that guarantee is unavailable.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u128::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts the operation to advancing Unicode scalar values and `char::MAX`, while ORIGINAL applies to overflow across the range supported by `Self`.",
          "Generated omits the ORIGINAL requirement to guarantee no overflow and use `forward` or `forward_checked` when that guarantee is unavailable."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::u128::unchecked_add",
      "source_api_path": "core::u128::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must not overflow `u128`; equivalently, `self <= u128::MAX - rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u128::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: u128::checked_add\n[`wrapping_add`]: u128::wrapping_add"
    },
    {
      "target_api_path": "core::u128::unchecked_cast",
      "source_api_path": "core::u128::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in the target integer type `T`; equivalently, `T::is_in_range(self)` must be `true`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::u128::unchecked_cast_from",
      "source_api_path": "core::u128::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable as a `u128`. In particular, when `value` has a signed integer type, it must be nonnegative; values of the supported unsigned integer types are always representable.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::u128::unchecked_disjoint_bitor",
      "source_api_path": "core::u128::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\n`self` and `other` must have no bits set in common; that is, `self & other` must equal `0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::u128::unchecked_div_exact",
      "source_api_path": "core::u128::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be exactly divisible by `rhs`; that is, `self % rhs == 0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::u128::unchecked_funnel_shl",
      "source_api_path": "core::u128::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than `u128::BITS` (128).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_funnel_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u128::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::u128::unchecked_funnel_shr",
      "source_api_path": "core::u128::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than `128`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_funnel_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u128::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::u128::unchecked_mul",
      "source_api_path": "core::u128::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` by `rhs` must not overflow the `u128` range; that is, the mathematical product must be at most `u128::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u128::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: u128::checked_mul\n[`wrapping_mul`]: u128::wrapping_mul"
    },
    {
      "target_api_path": "core::u128::unchecked_shl",
      "source_api_path": "core::u128::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than 128.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: u128::checked_shl"
    },
    {
      "target_api_path": "core::u128::unchecked_shl_exact",
      "source_api_path": "core::u128::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than 128, and shifting `self` left by `rhs` bits must not discard any nonzero bits; equivalently, `self <= u128::MAX >> rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nu128::BITS`\ni.e. when\n[`u128::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u128::unchecked_shr",
      "source_api_path": "core::u128::unchecked_shr",
      "llm_generated_comment": "# Safety\n\nThe shift amount `rhs` must be less than 128.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: u128::checked_shr"
    },
    {
      "target_api_path": "core::u128::unchecked_shr_exact",
      "source_api_path": "core::u128::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be less than 128. Otherwise, the underlying unchecked right-shift operation has undefined behavior.",
      "diff": "Missing\n1. Omits the requirement that `rhs` must not exceed `self.trailing_zeros()`.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_shr_exact",
        "classification": "Missing",
        "items": [
          "Omits the requirement that `rhs` must not exceed `self.trailing_zeros()`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nu128::BITS`\ni.e. when\n[`u128::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u128::unchecked_sub",
      "source_api_path": "core::u128::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe subtraction must not underflow: `rhs` must be less than or equal to `self`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u128::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < u128::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: u128::checked_sub\n[`wrapping_sub`]: u128::wrapping_sub"
    },
    {
      "target_api_path": "core::u16::backward_unchecked",
      "source_api_path": "core::u16::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar-value steps from `start` back to `'\\0'`; equivalently, stepping backward by `count` must produce a valid `char` without underflow.",
      "diff": "Incorrect\n1. Generated restricts `count` to Unicode scalar-value steps from `start` to `\\0`, adding unsupported `char` and boundary conditions instead of the original `Self` range-overflow requirement.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u16::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts `count` to Unicode scalar-value steps from `start` to `\\0`, adding unsupported `char` and boundary conditions instead of the original `Self` range-overflow requirement."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::u16::forward_unchecked",
      "source_api_path": "core::u16::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n- `count` must be small enough that advancing `start as u32` by `count` does not overflow the range representable by `u32`.\n- If `start as u32 < 0xD800` and that advancement reaches or crosses `0xD800`, the resulting value must be small enough that the additional advancement by `0x800` does not overflow.\n- The final advanced value must be a valid Unicode scalar value: it must be at most `0x10FFFF` and must not lie in the surrogate range `0xD800..=0xDFFF`.",
      "diff": "Incorrect\n1. The generated condition uses u32 overflow for advancing start, whereas ORIGINAL requires preventing overflow of the range supported by Self, u16.\n2. The generated Unicode scalar and surrogate-range requirements are additional safety conditions absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u16::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition uses u32 overflow for advancing start, whereas ORIGINAL requires preventing overflow of the range supported by Self, u16.",
          "The generated Unicode scalar and surrogate-range requirements are additional safety conditions absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::u16::unchecked_add",
      "source_api_path": "core::u16::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must be representable as a `u16`; that is, `self + rhs` must not exceed `u16::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u16::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: u16::checked_add\n[`wrapping_add`]: u16::wrapping_add"
    },
    {
      "target_api_path": "core::u16::unchecked_cast",
      "source_api_path": "core::u16::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in `T`; converting it to `T` must not overflow or otherwise be out of range.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::u16::unchecked_cast_from",
      "source_api_path": "core::u16::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `u16`; that is, it must be in the inclusive range `u16::MIN..=u16::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::u16::unchecked_disjoint_bitor",
      "source_api_path": "core::u16::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `self` and `other` have no set bits in common; that is, `self & other == 0` when this function is called.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::u16::unchecked_div_exact",
      "source_api_path": "core::u16::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be non-zero, and `self` must be evenly divisible by `rhs` (that is, `self % rhs == 0`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::u16::unchecked_funnel_shl",
      "source_api_path": "core::u16::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\nThe `shift` argument must be less than the number of bits in `u16` (that is, `shift < 16`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_funnel_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u16::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::u16::unchecked_funnel_shr",
      "source_api_path": "core::u16::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than the number of bits in `Self`; for `u16`, it must be in the range `0..16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_funnel_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u16::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::u16::unchecked_mul",
      "source_api_path": "core::u16::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; their mathematical product must be representable as a `u16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u16::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: u16::checked_mul\n[`wrapping_mul`]: u16::wrapping_mul"
    },
    {
      "target_api_path": "core::u16::unchecked_shl",
      "source_api_path": "core::u16::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than the number of bits in `u16`; that is, `rhs` must be in the range `0..16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: u16::checked_shl"
    },
    {
      "target_api_path": "core::u16::unchecked_shl_exact",
      "source_api_path": "core::u16::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u16::BITS`, and shifting `self` left by `rhs` bits must not discard any nonzero bits. Equivalently, `self <= u16::MAX >> rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nu16::BITS`\ni.e. when\n[`u16::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u16::unchecked_shr",
      "source_api_path": "core::u16::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than the number of bits in `u16`; that is, `rhs < 16`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: u16::checked_shr"
    },
    {
      "target_api_path": "core::u16::unchecked_shr_exact",
      "source_api_path": "core::u16::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\nThe `rhs` argument must be less than 16 (the number of bits in a `u16`). Otherwise, the unchecked shift may cause undefined behavior.",
      "diff": "Incorrect\n1. The generated condition requires rhs < 16, but ORIGINAL also requires rhs <= self.trailing_zeros; it omits this operand-dependent safety condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_shr_exact",
        "classification": "Incorrect",
        "items": [
          "The generated condition requires rhs < 16, but ORIGINAL also requires rhs <= self.trailing_zeros; it omits this operand-dependent safety condition."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nu16::BITS`\ni.e. when\n[`u16::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u16::unchecked_sub",
      "source_api_path": "core::u16::unchecked_sub",
      "llm_generated_comment": "# Safety\n\n`self` must be greater than or equal to `rhs`, so that the subtraction does not underflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u16::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < u16::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: u16::checked_sub\n[`wrapping_sub`]: u16::wrapping_sub"
    },
    {
      "target_api_path": "core::u32::backward_unchecked",
      "source_api_path": "core::u32::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar values preceding `start`. Equivalently:\n\n- if `start as u32 <= 0xD7FF`, `count <= start as u32`;\n- if `start as u32 >= 0xE000`, `count <= (start as u32) - 0x800`.\n\nThis ensures that stepping backward does not underflow and produces a valid `char`.",
      "diff": "Incorrect\n1. GENERATED adds Unicode scalar-value and `char` validity conditions absent from ORIGINAL, which only requires preventing overflow of `Self`'s supported range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u32::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED adds Unicode scalar-value and `char` validity conditions absent from ORIGINAL, which only requires preventing overflow of `Self`'s supported range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::u32::forward_unchecked",
      "source_api_path": "core::u32::forward_unchecked",
      "llm_generated_comment": "# Safety\n\n`count` must not exceed the number of Unicode scalar-value steps from `start` through `char::MAX`; advancing `start` by `count` while skipping the surrogate range must produce a valid `char`. Equivalently:\n\n- if `start < '\\u{D800}'`, `count <= 0x10_FFFF - (start as usize) - 0x800`;\n- otherwise, `count <= 0x10_FFFF - (start as usize)`.",
      "diff": "Incorrect\n1. Generated replaces the original Self-range overflow condition with a Unicode scalar-value and surrogate-skipping bound, adding unsupported semantics and different boundaries.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u32::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated replaces the original Self-range overflow condition with a Unicode scalar-value and surrogate-skipping bound, adding unsupported semantics and different boundaries."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::u32::unchecked_add",
      "source_api_path": "core::u32::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must not exceed `u32::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u32::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: u32::checked_add\n[`wrapping_add`]: u32::wrapping_add"
    },
    {
      "target_api_path": "core::u32::unchecked_cast",
      "source_api_path": "core::u32::unchecked_cast",
      "llm_generated_comment": "# Safety\n\nThe value of `self` must be representable in `T` without truncation or a change in numeric value.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::u32::unchecked_cast_from",
      "source_api_path": "core::u32::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `u32`; converting it to `u32` must not truncate or otherwise change its numeric value.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::u32::unchecked_disjoint_bitor",
      "source_api_path": "core::u32::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\n`self` and `other` must have no set bits in common; that is, `self & other == 0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::u32::unchecked_div_exact",
      "source_api_path": "core::u32::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be evenly divisible by `rhs`; that is, `self % rhs == 0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::u32::unchecked_funnel_shl",
      "source_api_path": "core::u32::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than `u32::BITS` (32).",
      "diff": "Incorrect\n1. The generated condition applies to `shift`, while ORIGINAL applies to `n`; the supplied texts do not establish that these are the same argument.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_funnel_shl",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies to `shift`, while ORIGINAL applies to `n`; the supplied texts do not establish that these are the same argument."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u32::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::u32::unchecked_funnel_shr",
      "source_api_path": "core::u32::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than the number of bits in `Self`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_funnel_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u32::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::u32::unchecked_mul",
      "source_api_path": "core::u32::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; their mathematical product must be representable as a `u32`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u32::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: u32::checked_mul\n[`wrapping_mul`]: u32::wrapping_mul"
    },
    {
      "target_api_path": "core::u32::unchecked_shl",
      "source_api_path": "core::u32::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u32::BITS` (that is, less than 32).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: u32::checked_shl"
    },
    {
      "target_api_path": "core::u32::unchecked_shl_exact",
      "source_api_path": "core::u32::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u32::BITS`, and shifting `self` left by `rhs` bits must not discard any set bits; equivalently, `self <= u32::MAX >> rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nu32::BITS`\ni.e. when\n[`u32::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u32::unchecked_shr",
      "source_api_path": "core::u32::unchecked_shr",
      "llm_generated_comment": "# Safety\n\nThe `rhs` shift amount must be less than 32.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: u32::checked_shr"
    },
    {
      "target_api_path": "core::u32::unchecked_shr_exact",
      "source_api_path": "core::u32::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u32::BITS`, and `self` must be evenly divisible by `2^rhs` (equivalently, the lowest `rhs` bits of `self` must all be zero).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nu32::BITS`\ni.e. when\n[`u32::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u32::unchecked_sub",
      "source_api_path": "core::u32::unchecked_sub",
      "llm_generated_comment": "# Safety\n\n`self - rhs` must not underflow; equivalently, `self` must be greater than or equal to `rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u32::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < u32::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: u32::checked_sub\n[`wrapping_sub`]: u32::wrapping_sub"
    },
    {
      "target_api_path": "core::u64::backward_unchecked",
      "source_api_path": "core::u64::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n- `count` must not exceed the number of valid Unicode scalar values preceding `start`; equivalently, `count` must be no greater than `start as usize` when `start < 0xE000`, and no greater than `start as usize - 0x800` otherwise. This ensures that the backward traversal and any adjustment for the surrogate range do not underflow and that the returned value is a valid `char`.",
      "diff": "Incorrect\n1. GENERATED replaces the original no-overflow obligation with unsupported exact Unicode-scalar and surrogate-range bounds on `count` and `start`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u64::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "GENERATED replaces the original no-overflow obligation with unsupported exact Unicode-scalar and surrogate-range bounds on `count` and `start`."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::u64::forward_unchecked",
      "source_api_path": "core::u64::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `start` by `count` Unicode scalar values must not exceed `char::MAX`; equivalently, the resulting Unicode scalar value must exist and be a valid `char`.",
      "diff": "Incorrect\n1. Generated restricts advancement to Unicode scalar values and valid char results, while ORIGINAL requires only preventing overflow beyond Self's supported value range.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u64::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts advancement to Unicode scalar values and valid char results, while ORIGINAL requires only preventing overflow beyond Self's supported value range."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::u64::unchecked_add",
      "source_api_path": "core::u64::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must be representable as a `u64`; that is, `self + rhs` must not overflow.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u64::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: u64::checked_add\n[`wrapping_add`]: u64::wrapping_add"
    },
    {
      "target_api_path": "core::u64::unchecked_cast",
      "source_api_path": "core::u64::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in the destination type `T`; that is, converting `self` to `T` must not overflow or otherwise fall outside `T`'s valid range.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::u64::unchecked_cast_from",
      "source_api_path": "core::u64::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable as a `u64` without truncation or a change in its mathematical value. In particular, signed values must be nonnegative, and values wider than `u64` must not exceed `u64::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::u64::unchecked_disjoint_bitor",
      "source_api_path": "core::u64::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\nThe set bits of `self` and `other` must be disjoint: no bit may be set in both operands (that is, `self & other == 0`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::u64::unchecked_div_exact",
      "source_api_path": "core::u64::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be nonzero, and `self` must be exactly divisible by `rhs` with no remainder.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::u64::unchecked_funnel_shl",
      "source_api_path": "core::u64::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than `u64::BITS` (64).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_funnel_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u64::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::u64::unchecked_funnel_shr",
      "source_api_path": "core::u64::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n- `shift` must be less than the number of bits in `u64` (that is, `shift < 64`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_funnel_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u64::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::u64::unchecked_mul",
      "source_api_path": "core::u64::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; their mathematical product must be representable as a `u64`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u64::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: u64::checked_mul\n[`wrapping_mul`]: u64::wrapping_mul"
    },
    {
      "target_api_path": "core::u64::unchecked_shl",
      "source_api_path": "core::u64::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than the number of bits in `u64` (that is, `rhs < 64`).",
      "diff": "Incorrect\n1. The generated text imposes rhs < 64, while ORIGINAL requires rhs be less than the number of bits in self.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_shl",
        "classification": "Incorrect",
        "items": [
          "The generated text imposes rhs < 64, while ORIGINAL requires rhs be less than the number of bits in self."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: u64::checked_shl"
    },
    {
      "target_api_path": "core::u64::unchecked_shl_exact",
      "source_api_path": "core::u64::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u64::BITS` (64), and shifting `self` left by `rhs` bits must not discard any nonzero bits; equivalently, `self` must be at most `u64::MAX >> rhs`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nu64::BITS`\ni.e. when\n[`u64::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u64::unchecked_shr",
      "source_api_path": "core::u64::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than the number of bits in `u64` (that is, `rhs < 64`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: u64::checked_shr"
    },
    {
      "target_api_path": "core::u64::unchecked_shr_exact",
      "source_api_path": "core::u64::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\nThe `rhs` argument must be less than the number of bits in `u64` (that is, `rhs < 64`).",
      "diff": "Incorrect\n1. Generated requires only `rhs < 64`, omitting the required `rhs <= self.trailing_zeros()` condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_shr_exact",
        "classification": "Incorrect",
        "items": [
          "Generated requires only `rhs < 64`, omitting the required `rhs <= self.trailing_zeros()` condition."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nu64::BITS`\ni.e. when\n[`u64::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u64::unchecked_sub",
      "source_api_path": "core::u64::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than or equal to `self`. Calling this function with `rhs > self` is undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u64::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < u64::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: u64::checked_sub\n[`wrapping_sub`]: u64::wrapping_sub"
    },
    {
      "target_api_path": "core::u8::as_ascii_unchecked",
      "source_api_path": "core::u8::as_ascii_unchecked",
      "llm_generated_comment": "# Safety\n\nThe string slice must contain only ASCII bytes for the duration of the returned slice's use.",
      "diff": "Incorrect\n1. The generated condition applies to a string slice and its use duration, rather than requiring this byte itself to be valid ASCII.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u8::as_ascii_unchecked",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies to a string slice and its use duration, rather than requiring this byte itself to be valid ASCII."
        ]
      },
      "original_safety_doc": "This byte must be valid ASCII, or else this is UB."
    },
    {
      "target_api_path": "core::u8::backward_unchecked",
      "source_api_path": "core::u8::backward_unchecked",
      "llm_generated_comment": "# Safety\n\n- The backward step from `start` by `count` must not overflow the range of values representable by a `char`.\n- If `start as u32 >= 0xE000` and the first backward step produces a value below `0xE000`, that value must be large enough for the additional backward step by `0x800` to remain within the range of values representable by a `char`.\n- The value produced after any required additional step must be a valid Unicode scalar value, so that it can be passed to `char::from_u32_unchecked`.",
      "diff": "Incorrect\n1. Generated adds char-specific thresholds and an additional 0x800 step absent from ORIGINAL.\n2. Generated requires the result to be a valid Unicode scalar value, an unsupported condition absent from ORIGINAL.\n3. Generated changes the subject from `Self`'s supported range to values representable by `char`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u8::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated adds char-specific thresholds and an additional 0x800 step absent from ORIGINAL.",
          "Generated requires the result to be a valid Unicode scalar value, an unsupported condition absent from ORIGINAL.",
          "Generated changes the subject from `Self`'s supported range to values representable by `char`."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::u8::forward_unchecked",
      "source_api_path": "core::u8::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nAdvancing `count` positions from `start` in the sequence of Unicode scalar values must not pass `char::MAX`. Equivalently:\n\n- if `start < '\\u{D800}'`, `count` must be at most `0x10F7FF - start as usize`;\n- otherwise, `count` must be at most `0x10FFFF - start as usize`.",
      "diff": "Incorrect\n1. Generated restricts the operation to Unicode scalar values and imposes char-specific boundaries, unlike ORIGINAL's general Self-supported range condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u8::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts the operation to Unicode scalar values and imposes char-specific boundaries, unlike ORIGINAL's general Self-supported range condition."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::u8::unchecked_add",
      "source_api_path": "core::u8::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must not exceed `u8::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > u8::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: u8::checked_add\n[`wrapping_add`]: u8::wrapping_add"
    },
    {
      "target_api_path": "core::u8::unchecked_cast",
      "source_api_path": "core::u8::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable in `T`; the conversion must not overflow or truncate the value.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::u8::unchecked_cast_from",
      "source_api_path": "core::u8::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable as a `u8`; that is, it must be in the range `0..=u8::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::u8::unchecked_disjoint_bitor",
      "source_api_path": "core::u8::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\nThe bits set in `self` and `other` must be disjoint: no bit may be set in both operands (`self & other == 0`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::u8::unchecked_div_exact",
      "source_api_path": "core::u8::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be exactly divisible by `rhs`; that is, `self % rhs == 0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::u8::unchecked_funnel_shl",
      "source_api_path": "core::u8::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than the number of bits in `Self`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_funnel_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u8::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::u8::unchecked_funnel_shr",
      "source_api_path": "core::u8::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`shift` must be strictly less than `u8::BITS` (that is, less than 8).",
      "diff": "Incorrect\n1. The generated condition applies to `shift`, while ORIGINAL applies to `n`; the texts do not establish that these are the same subject.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_funnel_shr",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies to `shift`, while ORIGINAL applies to `n`; the texts do not establish that these are the same subject."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`u8::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::u8::unchecked_mul",
      "source_api_path": "core::u8::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow an `u8`; that is, their mathematical product must be at most `u8::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > u8::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: u8::checked_mul\n[`wrapping_mul`]: u8::wrapping_mul"
    },
    {
      "target_api_path": "core::u8::unchecked_shl",
      "source_api_path": "core::u8::unchecked_shl",
      "llm_generated_comment": "# Safety\n\nThe `rhs` shift amount must be less than 8.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: u8::checked_shl"
    },
    {
      "target_api_path": "core::u8::unchecked_shl_exact",
      "source_api_path": "core::u8::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than 8, and `self << rhs` must be representable as a `u8` without discarding any nonzero bits (equivalently, `self <= u8::MAX >> rhs`).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nu8::BITS`\ni.e. when\n[`u8::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u8::unchecked_shr",
      "source_api_path": "core::u8::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `u8::BITS` (that is, less than 8).",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: u8::checked_shr"
    },
    {
      "target_api_path": "core::u8::unchecked_shr_exact",
      "source_api_path": "core::u8::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than `u8::BITS` (that is, `rhs < 8`).",
      "diff": "Missing\n1. Omits the requirement that `rhs` must not exceed `self.trailing_zeros()`.",
      "diff_type": "missing",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_shr_exact",
        "classification": "Missing",
        "items": [
          "Omits the requirement that `rhs` must not exceed `self.trailing_zeros()`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nu8::BITS`\ni.e. when\n[`u8::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::u8::unchecked_sub",
      "source_api_path": "core::u8::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that `rhs` is less than or equal to `self`; otherwise, the subtraction overflows and results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::u8::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < u8::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: u8::checked_sub\n[`wrapping_sub`]: u8::wrapping_sub"
    },
    {
      "target_api_path": "core::usize::backward_unchecked",
      "source_api_path": "core::usize::backward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that stepping backward from `start` by `count` does not underflow the range of valid `char` values, including the additional backward step performed when the result crosses the `U+E000` boundary. In particular, the resulting value passed to `char::from_u32_unchecked` must be a valid Unicode scalar value.",
      "diff": "Incorrect\n1. Generated changes the obligation from preventing overflow of `Self`'s supported range to preventing underflow of valid `char` values and adds unsupported U+E000 and `char::from_u32_unchecked` conditions.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::backward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated changes the obligation from preventing overflow of `Self`'s supported range to preventing underflow of valid `char` values and adds unsupported U+E000 and `char::from_u32_unchecked` conditions."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `backward` or `backward_checked` instead."
    },
    {
      "target_api_path": "core::usize::forward_unchecked",
      "source_api_path": "core::usize::forward_unchecked",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that advancing `start` by `count` valid Unicode scalar values does not exceed `char`'s range. In particular, the resulting value, after skipping the surrogate-code-point range `U+D800..=U+DFFF`, must be no greater than `U+10FFFF`; otherwise the unchecked arithmetic or conversion may produce an invalid `char`.",
      "diff": "Incorrect\n1. Generated restricts the operation to advancing by valid Unicode scalar values and imposes char-range and surrogate-skipping conditions absent from ORIGINAL.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::forward_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated restricts the operation to advancing by valid Unicode scalar values and imposes char-range and surrogate-skipping conditions absent from ORIGINAL."
        ]
      },
      "original_safety_doc": "It is undefined behavior for this operation to overflow the\nrange of values supported by `Self`. If you cannot guarantee that this\nwill not overflow, use `forward` or `forward_checked` instead."
    },
    {
      "target_api_path": "core::usize::get_unchecked",
      "source_api_path": "core::usize::get_unchecked",
      "llm_generated_comment": "# Safety\n\n- `slice` must be a valid pointer to a `str` for the duration of the returned pointer's use.\n- `self.start` and `self.end` must satisfy `self.start <= self.end` and `self.end <= slice.len()`.\n- Both `self.start` and `self.end` must be on UTF-8 character boundaries within `slice`, so that the returned pointer points to a valid `str`.\n- The underlying string data must remain valid and unchanged as a `str` for the duration of the returned pointer's use.",
      "diff": "Incorrect\n1. Generated imposes string-slice range and UTF-8 boundary conditions absent from ORIGINAL.\n2. Generated describes `self.start` and `self.end` rather than the ORIGINAL out-of-bounds index condition.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::get_unchecked",
        "classification": "Incorrect",
        "items": [
          "Generated imposes string-slice range and UTF-8 boundary conditions absent from ORIGINAL.",
          "Generated describes `self.start` and `self.end` rather than the ORIGINAL out-of-bounds index condition."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::usize::get_unchecked_mut",
      "source_api_path": "core::usize::get_unchecked_mut",
      "llm_generated_comment": "# Safety\n\nThe caller must ensure that:\n\n- `slice` is a valid, properly aligned pointer to a `str` slice.\n- `self.start <= self.end` and `self.end <= slice.len()`.\n- `self.start` and `self.end` are on UTF-8 character boundaries of `slice`.\n- The memory referenced by `slice` remains valid and suitably aligned for the duration of any use of the returned pointer, and the caller upholds the aliasing requirements for mutable access.",
      "diff": "Incorrect\n1. The generated conditions incorrectly require `self.start <= self.end` and UTF-8 boundaries, which are not stated in the original safety requirements.\n2. The generated lifetime condition limits validity to uses of the returned pointer, but the original requires a non-dangling slice pointer even when the pointer is unused.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::get_unchecked_mut",
        "classification": "Incorrect",
        "items": [
          "The generated conditions incorrectly require `self.start <= self.end` and UTF-8 boundaries, which are not stated in the original safety requirements.",
          "The generated lifetime condition limits validity to uses of the returned pointer, but the original requires a non-dangling slice pointer even when the pointer is unused."
        ]
      },
      "original_safety_doc": "Calling this method with an out-of-bounds index or a dangling `slice` pointer\nis *[undefined behavior]* even if the resulting pointer is not used.\n\n[undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"
    },
    {
      "target_api_path": "core::usize::unchecked_add",
      "source_api_path": "core::usize::unchecked_add",
      "llm_generated_comment": "# Safety\n\nThe sum of `self` and `rhs` must not exceed `usize::MAX`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_add",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self + rhs > usize::MAX`,\ni.e. when [`checked_add`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_add`]: usize::checked_add\n[`wrapping_add`]: usize::wrapping_add"
    },
    {
      "target_api_path": "core::usize::unchecked_cast",
      "source_api_path": "core::usize::unchecked_cast",
      "llm_generated_comment": "# Safety\n\n`self` must be representable by `T`; equivalently, `self.checked_cast::<T>()` must return `Some(_)`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_cast",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if the integer value of `self` is bigger than `T::MAX`,\nor smaller than `T::MIN`, where `T` is the target type."
    },
    {
      "target_api_path": "core::usize::unchecked_cast_from",
      "source_api_path": "core::usize::unchecked_cast_from",
      "llm_generated_comment": "# Safety\n\n`value` must be representable by `usize`; equivalently, converting it to `usize` with a checked integer conversion must succeed.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_cast_from",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `value` will overflow when\nconverted to this type."
    },
    {
      "target_api_path": "core::usize::unchecked_disjoint_bitor",
      "source_api_path": "core::usize::unchecked_disjoint_bitor",
      "llm_generated_comment": "# Safety\n\n`self` and `other` must have no overlapping set bits: `self & other` must equal `0`. Violating this precondition results in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_disjoint_bitor",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "Requires that `(self & other) == 0`, otherwise it's immediate UB.\n\nEquivalently, requires that `(self | other) == (self + other)`."
    },
    {
      "target_api_path": "core::usize::unchecked_div_exact",
      "source_api_path": "core::usize::unchecked_div_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be nonzero.\n- `self` must be exactly divisible by `rhs`; that is, `self % rhs == 0`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_div_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,\ni.e. when [`checked_div_exact`](Self::checked_div_exact) would return `None`."
    },
    {
      "target_api_path": "core::usize::unchecked_funnel_shl",
      "source_api_path": "core::usize::unchecked_funnel_shl",
      "llm_generated_comment": "# Safety\n\n`shift` must be less than `usize::BITS`.",
      "diff": "Incorrect\n1. The generated condition applies the bound to `shift` instead of the original parameter `n`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_funnel_shl",
        "classification": "Incorrect",
        "items": [
          "The generated condition applies the bound to `shift` instead of the original parameter `n`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`usize::BITS`,\ni.e. when [`funnel_shl`](Self::funnel_shl) would panic."
    },
    {
      "target_api_path": "core::usize::unchecked_funnel_shr",
      "source_api_path": "core::usize::unchecked_funnel_shr",
      "llm_generated_comment": "# Safety\n\n`shift` must not exceed `usize::BITS`.",
      "diff": "Incorrect\n1. Generated permits `shift == usize::BITS`, while ORIGINAL requires the shift value to be strictly less than `usize::BITS`.",
      "diff_type": "incorrect",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_funnel_shr",
        "classification": "Incorrect",
        "items": [
          "Generated permits `shift == usize::BITS`, while ORIGINAL requires the shift value to be strictly less than `usize::BITS`."
        ]
      },
      "original_safety_doc": "This results in undefined behavior if `n` is greater than or equal to\n`usize::BITS`,\ni.e. when [`funnel_shr`](Self::funnel_shr) would panic."
    },
    {
      "target_api_path": "core::usize::unchecked_mul",
      "source_api_path": "core::usize::unchecked_mul",
      "llm_generated_comment": "# Safety\n\nThe multiplication of `self` and `rhs` must not overflow; their mathematical product must be representable as a `usize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_mul",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self * rhs > usize::MAX`,\ni.e. when [`checked_mul`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_mul`]: usize::checked_mul\n[`wrapping_mul`]: usize::wrapping_mul"
    },
    {
      "target_api_path": "core::usize::unchecked_shl",
      "source_api_path": "core::usize::unchecked_shl",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than [`usize::BITS`].",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_shl",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shl`] would return `None`.\n\n[`checked_shl`]: usize::checked_shl"
    },
    {
      "target_api_path": "core::usize::unchecked_shl_exact",
      "source_api_path": "core::usize::unchecked_shl_exact",
      "llm_generated_comment": "# Safety\n\n`rhs` must be less than `usize::BITS`, and shifting `self` left by `rhs` bits must not discard any nonzero bits. Equivalently, `rhs <= self.leading_zeros()`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_shl_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.leading_zeros() || rhs >=\nusize::BITS`\ni.e. when\n[`usize::shl_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::usize::unchecked_shr",
      "source_api_path": "core::usize::unchecked_shr",
      "llm_generated_comment": "# Safety\n\n`rhs` must be strictly less than the number of bits in a `usize`.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_shr",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior if `rhs` is larger than\nor equal to the number of bits in `self`,\ni.e. when [`checked_shr`] would return `None`.\n\n[`checked_shr`]: usize::checked_shr"
    },
    {
      "target_api_path": "core::usize::unchecked_shr_exact",
      "source_api_path": "core::usize::unchecked_shr_exact",
      "llm_generated_comment": "# Safety\n\n- `rhs` must be strictly less than `usize::BITS`.\n- `self` must be exactly divisible by `2^rhs`; equivalently, `self.trailing_zeros() >= rhs`, so no nonzero bits are discarded by the shift.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_shr_exact",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >=\nusize::BITS`\ni.e. when\n[`usize::shr_exact`]\nwould return `None`."
    },
    {
      "target_api_path": "core::usize::unchecked_sub",
      "source_api_path": "core::usize::unchecked_sub",
      "llm_generated_comment": "# Safety\n\nThe value of `rhs` must be less than or equal to `self`; otherwise, the subtraction would underflow and result in undefined behavior.",
      "diff": "Correct",
      "diff_type": "correct",
      "semantic_judgment": {
        "api_path": "core::usize::unchecked_sub",
        "classification": "Correct",
        "items": []
      },
      "original_safety_doc": "This results in undefined behavior when\n`self - rhs < usize::MIN`,\ni.e. when [`checked_sub`] would return `None`.\n\n[`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked\n[`checked_sub`]: usize::checked_sub\n[`wrapping_sub`]: usize::wrapping_sub"
    }
  ]
}