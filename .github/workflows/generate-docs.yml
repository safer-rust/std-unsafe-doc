name: Generate docs/index.md

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust nightly-2025-12-06
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2025-12-06
          components: rust-src

      - name: Generate docs/index.md
        run: |
          mkdir -p docs
          python3 scripts/extract_public_unsafe.py docs/index.md

      - name: Commit and push docs/index.md
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.md
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: regenerate docs/index.md [skip ci]"
            git push
          fi
